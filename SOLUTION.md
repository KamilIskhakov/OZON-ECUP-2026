# Решение

Прогноз суммарного GMV пользователя за 30 дней (**2026-02-14 … 2026-03-15**)
по дневной истории активности. Метрика — RMSLE, 250 000 пользователей.

---

## 1. Постановка

Для пользователя $i$ и даты отсечения $T$ (**якоря**) целевая величина

$$y_i(T) \;=\; \sum_{t=T+1}^{T+30} \mathrm{gmv}_{i,t}$$

Метрика:

$$\mathrm{RMSLE} = \sqrt{\frac{1}{n}\sum_{i=1}^{n}\Big(\log(1+\hat y_i) - \log(1+y_i)\Big)^{2}}$$

Замена $z = \log(1+y)$ превращает её в обычный RMSE, откуда следует всё остальное:

$$\mathrm{RMSLE}(\hat y, y) = \mathrm{RMSE}(\hat z, z)$$

**Оптимальный прогноз** — экспонента условного среднего логарифма, и по неравенству
Йенсена он строго меньше условного среднего самой величины:

$$\hat y^{*}(x) = \exp\big(\mathbb{E}[z \mid x]\big) - 1 \;\le\; \mathbb{E}[y \mid x]$$

Поэтому модель обучается **только в $\log(1+y)$-пространстве**: регрессия в шкале
рублей под этой метрикой систематически завышает.

**Оптимальная константа** — геометрическое среднее, а не арифметическое:

$$c^{*} = e^{\bar z} - 1 = \Big(\textstyle\prod_i (1+y_i)\Big)^{1/n} - 1$$

---

## 2. Правило отбора популяции

Выборка организаторов условна на недавней активности. Восстановлено из данных:
максимальный recency на конец истории равен ровно 29 дням, а доля активных
в трёх последних выровненных 30-дневных блоках составляет ровно 100.00 % против
98–99 % на любом сдвинутом окне.

> **Правило:** событие в каждом из трёх последних 30-дневных блоков.

Формализовано как `selected_users(T)` и применяется **на каждом обучающем якоре**.
Без этого в обучение попадает популяция, которой в тесте не существует: доля нулевого
таргета завышается до 12 процентных пунктов, и классификатор занижает $P(y>0)$.

---

## 3. Нарезка истории на обучающие примеры

Один пример — пара «пользователь, якорь». Якоря берутся с шагом 30 дней назад
от последнего полностью наблюдаемого.

| Компонент | Значение |
| :--- | :--- |
| Окно признаков | $[T - W + 1,\; T]$, где $W \in \{240, 300, 365\}$ |
| Таргет | $[T+1,\; T+30]$ |
| Популяция | `selected_users(T)` |
| Число якорей | 4 или 7 |

Окна признаков соседних якорей перекрываются, таргет-окна — нет, поэтому утечки
ответа между примерами не возникает. Вес примера убывает с возрастом якоря:

$$w(T) = 2^{-\operatorname{age}(T)/90}$$

Глубина окна и число якорей конкурируют за 409 дней истории: при строгом требовании
полной истории и $W = 365$ обучающих якорей не остаётся вовсе.

---

## 4. Двухчастная модель

$$p(x) = P(y > 0 \mid x), \qquad m(x) = \mathbb{E}\big[z \mid x,\, y > 0\big]$$

Поскольку $\log(1+0) = 0$, нулевая компонента вносит в условное среднее ровно ноль,
и разложение **точное**, а не приближённое:

$$\mathbb{E}[z \mid x] = p(x)\,m(x) + \big(1-p(x)\big)\cdot 0 = p(x)\,m(x)$$

$$\boxed{\;\hat y = e^{\,p(x)\,m(x)} - 1\;}$$

| Часть | Реализация | Обучается на |
| :--- | :--- | :--- |
| $p(x)$ | `LGBMClassifier` | вся популяция якоря |
| $m(x)$ | `LGBMRegressor` | только $y > 0$ |

Интуитивная склейка $p \cdot (e^{m} - 1)$ оптимальна для MSE в шкале рублей и
под данной метрикой проигрывает **0.35** при тех же $p$ и $m$.

При передискретизации позитивов с коэффициентом $w$ prior возвращается формулой

$$p = \frac{p_s}{p_s + (1-p_s)\,w}$$

### Гиперпараметры

`lr 0.05`, `num_leaves 63`, `min_child_samples 200`, `feature_fraction 0.8`,
`bagging 0.8`, `lambda_l2 5`. Число деревьев определяется **ранней остановкой**
на 12 % отложенного трейна (оптимум около 194 у классификатора и 197 у регрессии),
после чего модель дообучается на 100 % данных с числом деревьев, отмасштабированным
на $1/(1-\text{eval\_frac})$.

---

## 5. Калибровка уровня: разложение на две маржи

Безусловный уровень окна раскладывается точно:

$$\ell(T) = \bar p(T)\cdot \ell^{+}(T), \qquad
\ell^{+}(T) = \operatorname{mean}_{i:\,y_i>0} z_i(T)$$

Каждая голова нормируется на **свою** маржу:

| Голова | Офсет при обучении | Ручка при предсказании |
| :--- | :--- | :--- |
| Классификатор | `init_score` $= \operatorname{logit}\bar p(T)$ | $\operatorname{logit}\bar p_{\text{цель}} + a_p$ |
| Регрессия | вычитание $\ell^{+}(T)$ | $\ell^{+}_{\text{цель}} + a_m$ |

Обоснование: по якорям $\ell$ гуляет на 14.5 %, а $\ell^{+}$ — на 4.5 %. Регрессия
обучается только на $y>0$, поэтому нормировка её цели на безусловное $\ell$
впрыскивала бы вариацию экстенсивной маржи, к «сколько тратит покупатель»
отношения не имеющую.

**Сдвиг применяется к $m$ до умножения на $p$.** Уровень входит в условное среднее
«сколько, если купит»; при прибавлении к готовому $p\,m$ возникла бы ошибка
$(1-p)\cdot\ell$, тем большая, чем реже пользователь покупает.

Отсюда фактический сдвиг у пользователя равен $p_i a_m$, и кривизна метрики
как функции $a_m$ не единица:

$$\mathrm{RMSLE}^2(a_m) = \mathrm{base}^2 + c\,(a_m - a_m^{*})^2,
\qquad c = \mathbb{E}[p^2]$$

Два замера одной модели при разных $a_m$ позволяют восстановить $p_i$ поэлементно
как $(\hat z_2 - \hat z_1)/(a_{m,2} - a_{m,1})$ и решить уравнение точно:
$\mathbb{E}[p]=0.5450$, $c=0.3899$, $a_m^{*}=+0.1705$.

### Сезонность: где она живёт

Разложение прошлогоднего календарного перехода зависит от популяции:

| Популяция | $a_p$ | $a_m$ | экстенсивная | интенсивная |
| :--- | ---: | ---: | ---: | ---: |
| Без фильтра | $+0.151$ | $+0.066$ | 85 % | 15 % |
| С фильтром активности | $+0.001$ | $+0.050$ | 4 % | **96 %** |

Правая строка релевантна: тестовая популяция условна на активности по построению.
На нефильтрованной популяции $\bar p$ растёт лишь потому, что активных людей
становится больше, — это смена состава, а не изменение поведения. Поэтому
$a_p = 0$, а сезонная поправка кладётся целиком в $\ell^{+}$.

### Исключение календаря якоря

`anchor_month`, `anchor_dow`, `anchor_doy` внутри якоря константны и служат его
идентификатором. Дни года обучающих якорей 199…349 и 14, боевого — 44: дерево
отнесло бы 44 в бин между 14 и 199 и применило произвольную интерполяцию.
Признаки построены, но из обучения исключены.

---

## 6. Признаки (178)

| Семейство | Состав |
| :--- | :--- |
| Оконные суммы | `gmv`, `ord`, `cart`, `srch`, активные дни, дни с покупкой и корзиной за 7/14/30/60/90/180/300 |
| Лаговые окна | те же за $[30,60]$, $[60,90]$, $[90,180]$ |
| Recency | по активности, покупке, корзине, поиску, GMV |
| Каналы | доли Поиска в GMV и днях, конверсии воронки, фантомные дни |
| Отношения | средний чек, $\text{cart}\to\text{ord}$, $\text{srch}\to\text{cart}$, buy rate, плотность |
| Тренды | логарифмические отношения свежего окна к предыдущему |
| EWMA | интенсивности с полураспадом 6.6 / 23 / 138 дней и разности быстрых и медленных |
| Байесовские фильтры | $q$, $c$, характерная сумма |
| Ритм покупок | интервалы между покупками, нормированный recency, тренды ритма |

### Экспоненциальная память

Прямоугольное окно описывает уровень, но не направление: покупка 29 дней назад
весит столько же, сколько вчерашняя, а 31 день назад — ноль. Экспоненциальное
затухание не имеет обрыва на границе, а разность

$$S_{\text{fast}} - S_{\text{slow}}$$

выражает текущую интенсивность **относительно собственного** долгосрочного уровня
пользователя — величину, которую ни одно прямоугольное окно выразить не может.

### Байесовские фильтры

Дисконтированные бета-бернулли оценки раскладывают вероятность покупки
на два механизма:

$$P(\text{покупка в день}) = q \cdot c, \qquad
q = P(\text{активен}), \quad c = P(\text{покупка} \mid \text{активен})$$

Масса успехов и неудач домножается на $\delta$ каждый день, поэтому старая история
забывается плавно, а не обрубается границей окна. Знаменатель для $q$ считается
аналитически как геометрическая сумма $\frac{1-\delta^{H}}{1-\delta}$: отсутствующие
в панели дни в неё входят, хотя строк для них нет.

Обычный `days_since_purchase` состояния «перестал заходить» и «заходит, но перестал
конвертироваться» путает, а будущее у них разное.

---

## 7. Ансамблирование

Усреднение строго в $z$-пространстве — той шкале, в которой считается MSE:

$$\hat z_{\text{ens}} = \frac{1}{N}\sum_{k=1}^{N} \hat z_k$$

При выпуклости квадратичного лосса смесь не хуже среднего участника, а при частично
декоррелированных ошибках строго лучше каждого.

Оси разнообразия по возрастанию силы декорреляции:

| Ось | Варианты |
| :--- | :--- |
| Сид (включая `bagging_seed`, `feature_fraction_seed`) | 42 / 7 / 2026 / … |
| Ёмкость | 63 / 127 листьев |
| Набор якорей | 4 / 7 |
| Глубина окна | 240 / 300 / 365 |
| Тип оценщика | hurdle / direct |

Ёмкость включена как ось сознательно: 63 и 127 листьев дают **одинаковое качество**
(shape 1.68203 против 1.68202), а равное качество при разной структуре деревьев —
идеальный случай для смеси.

**Сопоставление уровней между типами.** У hurdle сдвиг живёт в шкале $\ell^{+}$
и даёт средний сдвиг $\approx \bar p\, a_m$; у direct нормировка идёт на безусловное
$\ell$, поэтому эквивалентный сдвиг подаётся напрямую как $\bar p\, a_m$. Итоговый
уровень ансамбля дополнительно приводится к уровню опорной конфигурации, иначе
direct-модели утаскивают средний $\log(1+y)$ вниз.

---

## 8. Обоснование конструкции экспериментами

Все числа — `shape` $= \operatorname{std}(z - \hat z)$ на валидационном якоре.
Метрика раскладывается на `bias` (чинится одним числом) и `shape` (чинится только
моделью); конфигурации отбираются по `shape`, уровень калибруется отдельно.

### Глубина окна признаков

| Окно | 60 | 90 | 120 | 168 | 240 | **300** | 365 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| shape | 1.76182 | 1.73321 | 1.71968 | 1.71322 | 1.70497 | **1.70215** | 1.70337 |

Монотонное улучшение до 240–300, дальше плато.

### Временное состояние

| Блок | Признаков | Прирост shape |
| :--- | ---: | ---: |
| 12 недельных лагов и разности | 75 | $+0.00054$ |
| EWMA fast/mid/slow | 25 | $+0.00213$ |
| Байесовские фильтры | 21 | $+0.00279$ |

Прямоугольные лаги дублируют уже имеющиеся окна. `p_buy_slow` забирает 0.52 гейна
классификатора, `ewm_gmv_slow` — 0.38 гейна регрессии.

### Число деревьев и ёмкость

Ранняя остановка даёт оптимум 194/197 против фиксированных 600 — модель была
переобучена. Ёмкость от 63 до 511 листьев укладывается в разброс 0.0012.

### Распределение сигнала между головами

| Прогноз | shape |
| :--- | ---: |
| $p(x)\,m(x)$ | 1.68512 |
| $p(x)\cdot\overline{m}$ — только классификатор | 1.77066 |
| $\overline{p}\cdot m(x)$ — только регрессия | 2.06962 |

Заморозка $m$ стоит 0.086, заморозка $p$ — 0.385: классификатор несёт в 4.5 раза
больше сигнала, туда и следует класть ёмкость модели.

### Отрицательные результаты

**Похожесть пользователей.** Item-level данных нет, коллаборативный фильтр
построить не из чего. Траекторные представления (LSA по биграммам дневных токенов,
SVD по недельным магнитудам, исходы 50 ближайших соседей) сами по себе сильны —
пять чисел про соседей дают 94 % от 116 агрегатов — но поверх агрегатов добавляют
$+0.0005$ на GBDT. Причина: все они **неконтролируемые**, то есть сжимают дисперсию,
а не сигнал под задачу.

**Sequence-энкодер.** TCN с причинными расширенными свёртками (dilations 1…32,
рецептивное поле ~127 дней), attention-пулинг по времени и пять голов
(главная $z$, вспомогательные — факт покупки, дни с покупкой, заказы, активные дни).

| Схема | Прирост shape |
| :--- | ---: |
| Один обучающий якорь | $+0.00085$ |
| Три обучающих якоря | $-0.00015$ |

Компоненты специализировались сами: `seq16` и `seq44` берут по четверти гейна
классификатора при нуле в регрессии, `seq38` и `seq28` — наоборот. Сеть нашла
разложение на маржи из данных. Но эмбеддинг **замещает** существующие признаки,
а не добавляет к ним. Проверена одна конфигурация: ни длина окна, ни размер
скрытого слоя, ни веса вспомогательных задач не подбирались.

---

## 9. Планка бейзлайнов

Валидационный якорь, популяция с фильтром отбора (233 152 пользователя).

| Подход | RMSLE |
| :--- | ---: |
| Все нули | 3.2036 |
| Арифметическое среднее $y$ | 3.1750 |
| Оптимальная константа $c^{*} = 8.41$ | 2.2883 |
| GMV за прошлые 30 дней | 2.1951 |
| $\exp(0.70\cdot\log(1+x_{90}/3))-1$ | 1.8361 |
| Биннинг-hurdle на трёх признаках | 1.7468 |
| **GBDT-hurdle с признаками состояния** | **1.6932** |

Оптимальное сжатие прошлого GMV даёт показатель $a^{*} \approx 0.73 < 1$ для всех
окон — регрессия к среднему, которую модель обязана выучить.

---

## 10. Методические ограничения оценок

**Валидационный `bias` не переносится на боевое окно.** На валидации самый свежий
обучающий якорь декабрьский, и его уровень подставляется январскому окну; при
реальном прогнозе уровень непосредственно предшествующего окна известен точно.
Переносится только `shape`.

**Скрининг на одном якоре завышает эффект регуляризации примерно вчетверо** —
трейн там в шесть раз меньше, и переобучение сильнее.

**Линейная абляция занижает ценность нелинейных признаков.** Дерево не проверяет
$a - b > c$ одним сплитом; на эмбеддингах линейная проба дала $+0.0026$, GBDT — $+0.0005$.

**Разрешение валидации.** Эффекты меньше 0.0005 по `shape` она не различает.

---

## 11. Отрицательные результаты: согласование оценщика с метрикой

Пять гипотез целили в разное — функцию потерь, форму калибровки, входную
supervision, состав обучающих примеров. Все проверены на одинаковой временной
схеме, обучение и оценка на разных якорях.

| Гипотеза | Δ shape |
| :--- | ---: |
| Metric-aligned регрессия под лосс ^2$ | 569JNRXghkl0.0051$ |
| Двумерная калибровка (p, m)$ малым GBDT | 569JNRXghkl0.0016$ |
| Изотоническая калибровка внутри квартилей $ | 569JNRXghkl0.0004$ |
| Дискретный hazard по неделям вместо бинарного $ | 569JNRXghkl0.0002$ |
| Phase ensemble (сдвинутые сетки якорей) | 569JNRXghkl0.0008$ |
| Одномерная изотоническая (p m)$ | 1.0002$ |

### Metric-aligned

Теория: произведение $\hat p\hat m$ не оптимизировано под ^2$,
поскольку головы учатся логлоссом и MSE на позитивах. Реализовано как
custom objective с градиентом wp(p(f+\ell)-z)$ и гессианом wp^2$,
аналитическим стартовым скором ^* = \sum wp(z-p\ell)/\sum wp^2$.

Не сработало, вероятно, по трём причинам: вес ^2$ фактически выбрасывает
низковероятных пользователей, оптимум ^* = pm/	ilde p$ впитывает ошибки
классификатора, которые на тесте другие, а неявная цель /p$ хуже обусловлена
на хвосте.

### Калибровка

Карта остатков показала, что смещение зависит от $: от 569JNRXghkl0.113$ у нижнего
квартиля до 569JNRXghkl0.215$ у верхнего, что даёт оценку потолка 1.0005$. Оценка
оказалась неверной по существу: 569JNRXghklзависимое смещение **не переносится**
между якорями. Деградация растёт с гибкостью калибратора — подпись
переобучения под несуществующую закономерность.

### Hazard

$	au$ — день первой покупки в окне, четыре условных модели на вложенных
подвыборках доживших, {30} = 1 - \prod_k (1-h_k)$. AUC совпал с бинарным
классификатором до четвёртого знака, корреляция оценок /bin/zsh.998$. Тайминг покупки
предсказывается теми же признаками, что и её факт, поэтому разложение по времени
перепараметризует то же самое, а не добавляет измерение.

### Phase ensemble

Фазы $arphi \in \{0,7,15,22\}$, внутри каждой якоря через 30 дней, поэтому
таргет-окна не пересекаются. Качество монотонно падает с добавлением фаз:
1.68394 → 1.68422 → 1.68471 → 1.68474. Причина в том, что фазы **не
равнокачественны**: последние якоря отстоят на 0, 7, 15 и 22 дня от целевого
окна, и участники систематически слабеют. Равные веса оптимальны только при
равном качестве — здесь это условие впервые нарушено.

### Карта остатков

Разложение полного SSE по сегментам валидации:

| Квартиль $ | Доля пользователей | bias | RMSE | Доля SSE |
| :--- | ---: | ---: | ---: | ---: |
|  < 0.33$ | 25 % | 569JNRXghkl0.113$ | 1.292 | 14.6 % |
| /bin/zsh.33$–/bin/zsh.61$ | 25 % | 569JNRXghkl0.186$ | 1.903 | **31.6 %** |
| /bin/zsh.61$–/bin/zsh.86$ | 25 % | 569JNRXghkl0.213$ | 1.992 | **34.7 %** |
|  > 0.86$ | 25 % | 569JNRXghkl0.215$ | 1.479 | 19.1 % |

Две трети ошибки лежат в средних квартилях при половине пользователей: при
$ близко к 0 или 1 исход почти детерминирован, а в середине — настоящая
неопределённость. Самая дорогая ячейка —  \in q_3$ и 3–7 дней с покупкой
за 90 дней: 25 % SSE при 18 % пользователей.

---

## 12. Направления, не доведённые до проверки

- **Взвешенный ансамбль.** Модели входят с весом $1/N$ при разном качестве;
  неотрицательный МНК с регуляризацией к равным весам.
- **Hazard-постановка для $p$.** Классификатор несёт 4.5× сигнала, а моделируется
  бинарной классификацией «купит за 30 дней»; дискретный hazard по дням
  с последующей агрегацией — другой индуктивный сдвиг.
- **Гиперпараметры за пределами ёмкости:** `min_child_samples`, `lambda_l1`,
  `feature_fraction`, `max_bin`.
- **Обе версии recency для фантомных дней** — 15.2 % строк с `search = 0` и
  `cat = 0`, решение «считать ли день активным» меняет признак.
- **Понедельная декомпозиция таргета** — четыре недельные суммы вместо одной.
- **Перебор гиперпараметров sequence-энкодера.**
