# Текущее решение

Прогноз суммарного GMV пользователя за **2026-02-14 … 2026-03-15**.
Метрика — RMSLE, 250 000 пользователей.

---

## Результат

| Сабмит | Уровень | Public LB |
| :--- | :--- | ---: |
| [`artifacts/submission_v4.csv`](artifacts/submission_v4.csv) | $a_m=+0.18$ | **1.6536732753** |
| [`artifacts/submission_v3.csv`](artifacts/submission_v3.csv) | $a_m=+0.07$, признаки состояния | 1.6548526206 |
| [`artifacts/submission_v2.csv`](artifacts/submission_v2.csv) | $a_m=+0.07$ | не отправлялся |
| [`artifacts/submission.csv`](artifacts/submission.csv) | $\delta=+0.12$ к $m$, старая схема | 1.6554595508 |
| [`artifacts/submission_noseason.csv`](artifacts/submission_noseason.csv) | $\delta=0$, старая схема | 1.6562739926 |
| Лидер | — | 1.6485982058 |

> **Отставание от лидера — 0.00508**, и оно целиком в качестве модели: калибровка уровня закрыта.

### Калибровка уровня закрыта — с доказательством

Сдвиг применяется к $m$ до умножения на $p$, поэтому фактический сдвиг у пользователя равен $p_i a_m$, а кривизна параболы не единица:

$$\mathrm{RMSLE}^2(a_m) = \mathrm{base}^2 + c\,(a_m - a_m^{*})^2, \qquad c = \mathbb{E}[p^2]$$

Две точки **одной и той же модели** (v3 и v4) позволяют восстановить $p_i$ поэлементно как $(\hat z_{v4} - \hat z_{v3})/0.11$ и решить уравнение точно:

| Величина | Значение |
| :--- | ---: |
| $\mathbb{E}[p]$ | 0.5450 |
| $\mathbb{E}[p^2] = c$ | 0.3899 |
| $a_m^{*}$ | $+0.1705$ |
| Применено в v4 | $+0.18$ |
| Остаточный выигрыш | $0.00001$ |

Дальше двигать уровень бессмысленно.

### Почему валидация обещала $-0.011$, а паблик дал $-0.0006$

Это главный методологический урок проекта.

| Источник обещанного выигрыша | Валидация | Переносится? |
| :--- | ---: | :--- |
| Рефакторинг марж и удаление календаря якоря | $-0.0093$ | **нет** |
| Признаки состояния | $-0.0018$ | да |

Почти весь выигрыш рефакторинга на валидации был снижением **смещения уровня**, а это смещение — артефакт устройства холдаута, а не свойство модели. На валидации самый свежий обучающий якорь 2025-12-15, и декабрьский уровень подставляется январскому окну — ошибка около 0.28. В боевом сабмите такой ошибки нет: самый свежий якорь 2026-01-14, его уровень известен точно, и обе схемы брали именно его.

Переносится только **shape**. Счёт сходится:

$$\underbrace{-0.0016}_{\text{shape от состояния}} + \underbrace{+0.0012}_{\text{потеря на недооценённом уровне}} = -0.0004 \;\approx\; -0.0006\ \text{наблюдаемых}$$

> **Практическое следствие.** Сырой валидационный RMSLE здесь вводит в заблуждение: холдаут навязывает заведомо неверный уровень. Конфигурации отбирать строго по `shape`, уровень калибровать отдельно по лидерборду.

> **Про порог значимости.** $\pm 0.011$ ($2\sigma$) относится к сравнению *независимых* выборок. Сабмиты оцениваются на одних и тех же 50 000 пользователей, корреляция прогнозов $>0.997$ — поэтому парные разницы в тысячных информативны.

---

## Архитектура

```text
src/ecup/
├── config.py     константы, SplitConfig (бюджет якорей), ModelConfig
├── data.py       загрузка + даункаст, правило отбора, таргет
├── features.py   ~116 признаков на якорь
├── metrics.py    RMSLE, склейка hurdle, поправка prior
├── model.py      HurdleGBDT, DirectGBDT
├── state.py      EWMA и байесовские фильтры активности/конверсии
├── similarity.py эмбеддинги траекторий и исходы соседей (измеренный ноль)
├── dataset.py    многооконная сборка, веса якорей
└── pipeline.py   валидация, свипы, сабмит
```

```python
from ecup import load_panel, run_validation, make_submission, SplitConfig

df = load_panel()
run_validation(df=df, split=SplitConfig(max_history=300, n_train_anchors=6))
make_submission(split=SplitConfig(max_history=300, n_train_anchors=6),
                a_p=0.0, a_m=0.07)
```

Зависимости сверх окружения `ml`: `polars`, `lightgbm`, `llvm-openmp` — на macOS LightGBM без `libomp` не грузится.

---

## Схема обучения

**Якорь** $T$ — дата отсечения: признаки только по $d \le T$, таргет

$$y_i(T) \;=\; \sum_{t=T+1}^{T+30} \mathrm{gmv}_{i,t}$$

| Компонент | Значение |
| :--- | :--- |
| Боевой якорь | 2026-02-13 → окно 02-14 … 03-15 |
| Валидационный | 2026-01-14 → окно 01-15 … 02-13 (последний с полным таргетом) |
| Обучающие | шаг 30 дней назад, 6 штук: 2025-07-18 … 2025-12-15 |
| Окно признаков | 300 дней |
| Популяция якоря | `selected_users(T)` — правило отбора организаторов |

Окна признаков соседних якорей перекрываются, таргет-окна — нет, утечки ответа не возникает. Финальное обучение добавляет валидационный якорь: 7 якорей, 1 180 173 примера.

Веса примеров падают вдвое каждые 90 дней возраста якоря.

---

## Модель

Двухчастная:

$$
p(x) = P(y > 0 \mid x), \qquad
m(x) = \mathbb{E}\big[\log(1+y) \mid x,\, y > 0\big], \qquad
\hat y = e^{\,p(x)\,m(x)} - 1
$$

| Часть | Реализация | Обучается на |
| :--- | :--- | :--- |
| $p(x)$ | `LGBMClassifier` | вся популяция якоря |
| $m(x)$ | `LGBMRegressor` | только $y > 0$ |

Склейка **точная**, а не приближённая: $\log(1+0) = 0$, поэтому

$$\mathbb{E}\big[\log(1+y) \mid x\big] = p(x)\,m(x) + \big(1-p(x)\big)\cdot 0 = p(x)\,m(x)$$

Интуитивная склейка $p \cdot (e^{m} - 1)$ оптимальна для MSE в рублях и на валидации проигрывает **0.35** при тех же $p$ и $m$.

Гиперпараметры: `lr 0.05`, `num_leaves 63`, `min_child_samples 200`,
`feature_fraction 0.8`, `bagging 0.8`, `lambda_l2 5`. Число деревьев не
фиксировано — **ранняя остановка** на 12 % отложенного трейна, оптимум около
194 деревьев у классификатора и 197 у регрессии. Ёмкость проверена (63–511
листьев) и на качество почти не влияет.

### Калибровка уровня: две маржи, а не один скаляр

Так как $\log(1+0)=0$, безусловный уровень раскладывается точно:

$$\ell(T) = \bar p(T)\cdot \ell^{+}(T), \qquad \ell^{+}(T) = \operatorname{mean}_{i:\,y_i>0}\log(1+y_i)$$

Каждая голова нормируется на **свою** маржу:

| Голова | Офсет при обучении | Ручка при предсказании |
| :--- | :--- | :--- |
| Классификатор | `init_score` $= \operatorname{logit}\bar p(T)$ | $\operatorname{logit}\bar p_{\text{цель}} + a_p$ |
| Регрессия | вычитание $\ell^{+}(T)$ | $\ell^{+}_{\text{цель}} + a_m$ |

Раньше регрессия нормировалась на безусловное $\ell$, хотя обучается только на $y>0$.
Разброс по якорям: $\ell$ гуляет на 14.5 %, $\ell^{+}$ — на 4.5 %. Нормировка на $\ell$
впрыскивала в цель регрессора вариацию экстенсивной маржи, к «сколько тратит
покупатель» отношения не имеющую.

**Календарь якоря удалён из признаков.** `anchor_month`, `anchor_dow`, `anchor_doy`
внутри якоря константны, то есть работают его идентификатором. Дни года обучающих
якорей — 199, 229, …, 349, 14; боевого — 44. Дерево отнесёт 44 в бин между 14 и 199
и применит произвольную январско-июльскую интерполяцию. Замер: с ними смещение
$+0.2408$, без них $+0.1821$, выигрыш **0.0073**.

**Сезонная поправка на боевое окно: $a_p = 0$, $a_m = +0.07$.** Обоснование —
в [ANALYSIS.md](ANALYSIS.md), раздел про сезонность: на популяции, условной на
активности (а наша именно такая), сезонный подъём почти целиком интенсивный.

---

## Признаки (~116)

| Семейство | Состав |
| :--- | :--- |
| Оконные суммы | `gmv`, `ord`, `cart`, `srch`, активные дни, дни с покупкой и корзиной за 7/14/30/60/90/180/300 дней |
| Лаговые окна | те же за $[30,60]$, $[60,90]$, $[90,180]$ — база для трендов |
| Recency | по активности, покупке, корзине, поиску, GMV |
| Каналы | доли Поиска в GMV и в днях, конверсии воронки, фантомные дни |
| Отношения | средний чек, $\text{cart} \to \text{ord}$, $\text{srch} \to \text{cart}$, buy rate, плотность |
| Тренды | логарифмические отношения свежего окна к предыдущему |
| BTYD-lite | recency, нормированный на собственный медианный интервал визитов |
| Глубина истории | `avail_history` — сколько дней реально стояло за агрегатами |
| **Состояние: EWMA** | интенсивности с полураспадом 6.6 / 23 / 138 дней по 5 каналам плюс разности быстрых и медленных |
| **Состояние: фильтры** | $q=P(\text{активен})$, $c=P(\text{покупка}\mid\text{активен})$, характерная сумма — дисконтированные бета-бернулли |

Календарь якоря (`anchor_month`, `anchor_dow`, `anchor_doy`) построен, но
**исключён из обучения**: внутри якоря это константы, то есть его идентификатор.

---

## Проверенные гипотезы

### 1. Окно признаков: 168 дней — не оптимум

Четыре обучающих якоря зафиксированы (811 818 примеров), меняется только глубина.

| Окно, дней | hurdle | Без смещения | К 168 дням |
| ---: | ---: | ---: | ---: |
| 60 | 1.76182 | 1.74733 | $+0.049$ |
| 90 | 1.73321 | 1.71967 | $+0.020$ |
| 120 | 1.71968 | 1.70607 | $+0.006$ |
| 168 | 1.71322 | 1.69787 | — |
| 240 | 1.70497 | 1.68956 | $-0.008$ |
| **300** | **1.70215** | 1.68574 | $\mathbf{-0.011}$ |
| 365 | 1.70337 | 1.68479 | $-0.010$ |

Улучшение монотонное до 240–300, дальше плато.

Число 168 взято у победителя GA Customer Revenue и под нашу длину истории не подходит: у нас 409 дней, и глубина окна конкурирует с числом якорей за один ресурс. В строгом режиме (полная история у каждого якоря) при окне 365 обучающих якорей **не остаётся вовсе**, при 300 их два, при 168 — семь.

### 2. Число якорей: больше данных ≠ лучше

Окно 300 дней.

| Якорей | Примеров | hurdle | Без смещения |
| ---: | ---: | ---: | ---: |
| 1 | 218 400 | 1.70312 | 1.69452 |
| **2** | 423 749 | **1.69868** | 1.68694 |
| 3 | 621 128 | 1.69967 | 1.68615 |
| 4 | 811 818 | 1.70215 | 1.68574 |
| **6** | 1 180 173 | 1.70418 | **1.68499** |
| 9 | 1 710 113 | 1.71199 | 1.68569 |

Колонки расходятся: по сырой метрике лучше 2 якоря, по метрике без смещения — 6. Больше данных улучшает **ранжирование**, но портит **уровень**, потому что оценка уровня берётся с последнего якоря и уползает при добавлении старых окон.

Выбраны 6: при реальном предсказании оценка уровня лучше той, что доступна валидации.

### 3. Hurdle против прямой регрессии — ничья

| Конфигурация | hurdle | direct |
| :--- | ---: | ---: |
| 300 дней, 6 якорей (исправленная схема) | **1.69493** | 1.69858 |
| 300 дней, 6 якорей (старая схема) | 1.70426 | 1.70216 |
| 300 дней, 2 якоря (старая схема) | 1.69868 | 1.69658 |

Разница внутри шума. Это ожидаемо: GBDT с MSE на $\log(1+y)$ уже оценивает
$\mathbb{E}[z \mid x] = p\,m$, то есть direct — не наивный бейзлайн, а корректный
оценщик. Hurdle оставлен не ради точности, а ради **двух раздельно калибруемых
ручек**: вся схема уровня выше построена именно на этом.

Смесь в $z$-пространстве $\alpha\hat z_D + (1-\alpha)\hat z_H$ проверена: оптимум
$\alpha=0.4$ даёт shape 1.68448 против 1.68512 — выигрыш $0.0006$, в сабмит не включён.

### 4. Временное состояние: экспоненциальная память работает, прямоугольные лаги нет

Обучение на 2025-12-15, оценка на 2026-01-14, прирост к shape:

| Блок признаков | Число | Прирост shape |
| :--- | ---: | ---: |
| 12 недельных лагов и их разности | 75 | $+0.00054$ |
| EWMA fast/mid/slow | 25 | $+0.00213$ |
| Байесовские фильтры активности и конверсии | 21 | $+0.00279$ |
| EWMA и фильтры вместе | 46 | $+0.00334$ |

Прямоугольные лаги дают почти ноль: окна 7/14/30/…/300 уже покрывают ось времени,
ещё 12 таких же ящиков — это больше того же самого. Экспоненциальная память
принципиально другая: затухание без обрыва на границе, и разность
$S_{\text{fast}} - S_{\text{slow}}$ выражает направление изменения относительно
собственного уровня пользователя — то, чего ни одно прямоугольное окно не выражает.

Байесовские фильтры раскладывают вероятность покупки на два механизма:

$$P(\text{покупка в день}) = q \cdot c, \qquad q = P(\text{активен}), \quad c = P(\text{покупка} \mid \text{активен})$$

`days_since_purchase` эти состояния путает — «перестал заходить» и «заходит,
но перестал конвертироваться» дают одинаковый recency при разном будущем.

Новые признаки не просто добавляют, а **занимают верх важности**: `p_buy_slow`
забирает 0.52 гейна классификатора, `ewm_gmv_slow` — 0.38 гейна регрессии.

На полном шестиякорном пайплайне прирост скромнее, но подтверждается:

| Конфигурация | dim | hurdle | shape | direct |
| :--- | ---: | ---: | ---: | ---: |
| Без признаков состояния | 113 | 1.69493 | 1.68512 | 1.69858 |
| **С признаками состояния** | 159 | **1.69316** | **1.68355** | 1.69607 |

### 5. Модель была переобучена, а не недообучена

Гипотеза была обратной: раз классификатор несёт 4.5× сигнала, он должен
недообучаться на консервативных 63 листьях. Ранняя остановка показала обратное —
фиксированные 600 деревьев это сильно больше нужного.

Скрининг на одном якоре (обучение 2025-12-15, оценка 2026-01-14):

| Конфигурация | dim | Деревьев клф/рег | shape |
| :--- | ---: | ---: | ---: |
| Без ритма, фикс. 600 | 159 | 600/600 | 1.69291 |
| Без ритма, ранняя остановка | 159 | **149/87** | 1.68657 |
| С ритмом, фикс. 600 | 178 | 600/600 | 1.69181 |
| С ритмом, ранняя остановка | 178 | **176/131** | **1.68607** |

Ранняя остановка одна даёт $-0.0063$, ритм покупок — ещё $-0.0005$.

Ёмкость почти не влияет: 63, 127, 255 и 511 листьев укладываются в разброс
0.0012, лучший вариант выигрывает у текущего 0.0006 — в пределах шума.

**Но на полной схеме эффект вчетверо меньше.** С 1.18 млн примеров вместо 218 тысяч
600 деревьев переобучают куда слабее:

| Конфигурация | dim | Деревьев | shape |
| :--- | ---: | ---: | ---: |
| Состояние без ритма, фикс. 600 | 159 | 600/600 | 1.68355 |
| $+$ ритм покупок, фикс. 600 | 178 | 600/600 | 1.68284 |
| $+$ ранняя остановка | 178 | 194/197 | **1.68203** |

Итого за этот заход $-0.0015$ к shape. Урок методический: **скрининг на одном
якоре завышает эффект регуляризации**, потому что там трейн в шесть раз меньше.
Отбирать по нему можно, а величину прироста — только на полной схеме.

### 6. Похожесть пользователей — измеренный ноль

Item-level данных нет, поэтому классический коллаборативный фильтр построить не
из чего. Похожесть строилась на траекториях: LSA по биграммам дневных токенов,
SVD по недельным магнитудам, исходы 50 ближайших соседей.

| Представление | $R^2$ сам по себе | Прирост к 116 агрегатам (GBDT) |
| :--- | ---: | ---: |
| Токен-LSA, 16 чисел | 0.3633 | $+0.0003$ |
| Недельный SVD, 8 чисел | 0.4201 | $+0.0018$ |
| Исходы соседей, 5 чисел | 0.4291 | $+0.0011$ |
| Всё вместе на GBDT | — | $+0.0005$ |

Пять чисел про соседей дают 94 % того, что дают 116 признаков — отличная
компрессия, но нулевая добавка. Все представления выведены из тех же дневных
агрегатов, и у них общий информационный потолок. Это понижает априор и для
обученного нейросетевого энкодера: он читает те же данные.

### 7. Сигнал сидит в классификаторе — теперь измерено

Заморозка одной компоненты при сохранении второй:

| Прогноз | shape RMSE |
| :--- | ---: |
| $p(x)\,m(x)$ — полный | 1.68512 |
| $p(x)\cdot\overline{m}$ — только классификатор | 1.77066 |
| $\overline{p}\cdot m(x)$ — только регрессия | 2.06962 |

Заморозка $m$ стоит 0.086, заморозка $p$ — 0.385. Классификатор важнее в 4.5 раза.
Раньше этот вывод опирался на сравнение диапазонов (8× против 1.83×), что
некорректно: относительный размах не равен вкладу в дисперсию.

---

## Планка

Валидационный якорь, популяция с фильтром отбора (233 152 пользователя).

Метрика раскладывается на **bias** $=\operatorname{mean}(z-\hat z)$ (чинится одним
числом) и **shape** $=\operatorname{std}(z-\hat z)$ (чинится только моделью).
Конфигурации выбираются по shape, уровень решается отдельно.

| Подход | RMSLE |
| :--- | ---: |
| Все нули | 3.2036 |
| Оптимальная константа | 2.2883 |
| Прошлые 30 дней (= `sample_submit`) | 2.1951 |
| $\exp(0.70 \cdot \log(1+x_{90}/3)) - 1$ | 1.8361 |
| Биннинг-hurdle на 3 признаках | 1.7468 |
| GBDT-hurdle, старая схема | 1.7043 |
| **GBDT-hurdle, исправленная схема** | **1.6949** |
| Он же, shape (без ошибки уровня) | 1.6851 |

---

## Что дальше

Разрыв $0.0069$ в RMSLE это $0.0203$ в MSE, то есть надо снять 0.74 % дисперсии.
Порядок работ — от самого обоснованного данными:

- [x] ~~Блок недельных лагов~~ — проверено, $+0.0005$, не окупается.
- [x] ~~Признаки состояния (EWMA и байесовские фильтры)~~ — сделано, $+0.0018$
      на полном пайплайне, верх важности в обеих головах.
- [x] ~~Похожесть и эмбеддинги траекторий~~ — проверено, около нуля.
- [x] ~~Признаки ритма покупок~~ — сделано, $-0.0007$ на полной схеме.
- [x] ~~Ранняя остановка~~ — сделано, $-0.0008$ на полной схеме;
      ёмкость проверена и не решает.
- [ ] **Гиперпараметры за пределами ёмкости:** Ни один не подбирался. Ёмкость
      класть в классификатор (он несёт 4.5× сигнала): меньше `lr`, больше
      листьев, ранняя остановка на отложенном якоре.
- [ ] **Абляция трендовых признаков на GBDT, а не на линейной модели.** Дерево
      не выводит $a-b>c$ одним сплитом, поэтому нулевой линейный $\Delta R^2$
      для них не приговор.
- [ ] **Валидация на нескольких якорях.** Сейчас единственное окно —
      самый глубокий сезонный провал в данных, а боевое окно на подъёме.
- [ ] **Sequence-энкодер** с multi-task головами, эмбеддинг → признаки GBDT.
      Делать после того, как недельные лаги исчерпают дешёвый временной сигнал.
