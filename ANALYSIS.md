# Выжимка анализа данных

Полный разбор — [notebooks/01_eda.ipynb](notebooks/01_eda.ipynb), отчёт с выводами формул — [notebooks/02_report.ipynb](notebooks/02_report.ipynb).
Здесь только числа и следствия.

---

## Данные

30 631 006 строк · 250 000 пользователей · 409 дней (2025-01-01 … 2026-02-13).

| Уровень | Строк | Доля сетки |
| :--- | ---: | ---: |
| Полная сетка пользователь $\times$ день | 102 250 000 | 100 % |
| Есть строка — пользователь заходил | 30 631 006 | **30.0 %** |
| День с добавлением в корзину | 11 314 851 | 11.07 % |
| День с покупкой ($\mathrm{gmv} > 0$) | 4 736 907 | **4.63 %** |

Даункаст типов: 4090 → 1139 MB, в 3.6 раза.

**Инварианты.** $\mathrm{gmv} = \mathrm{gmv\_search} + \mathrm{gmv\_cat}$ точно, `to_cart` и `to_ord` раскладываются по каналам точно. Ключ `(user_id, d)` уникален, пропусков нет. Суммарные колонки линейно зависимы от частных — подавать в модель либо частные, либо суммарные плюс доли.

**Фантомные строки.** 4 667 981 (15.2 %) имеют `search = 0` и `cat = 0`. Не мусор: несут 0.54 % всего GMV. Решение, считать ли такой день активным, меняет recency у заметной доли пользователей.

**Концентрация.** Джини $= 0.700$, топ-10 % дают 53 % GMV, топ-1 % — 15 %. 12.3 % пользователей не купили ничего за 409 дней.

> Метрика этой концентрации не видит: в $\log(1+y)$-шкале вклад топового и рядового пользователя в ошибку одинаков.

---

## Находка 1 · Выборка отобрана по недавней активности

Максимальный recency на 2026-02-13 — ровно **29 дней**, пользователей с 30 и более — **ноль**.

| 30-дневное окно | Доля активных |
| :--- | ---: |
| 15.01 … 13.02.2026 | **100.00 %** |
| 16.12 … 14.01.2026 | **100.00 %** |
| 16.11 … 15.12.2025 | **100.00 %** |
| 17.10 … 15.11.2025 | 93.26 % |
| любое со сдвигом на 5 дней | 98–99 % |

> **Правило:** событие в каждом из трёх последних 30-дневных блоков.
> Проверка: `selected_users(408)` возвращает ровно 250 000 из 250 000.

Последствия:

- «Мёртвых» пользователей в выборке нет. Машинерия BTYD вокруг $P(\text{alive})$ бьёт по отсутствующей цели; полезна только идея нормировки recency на собственный ритм.
- На якоре 2025-06-18 правилу удовлетворяют ~72 %. Обучение на всех подряд даёт популяцию, которой в тесте не существует: доля нулей завышается до 12 п.п., классификатор занижает $P(y>0)$.
- Валидационный якорь тоже требует фильтра: там проходят 93.3 % (233 152 из 250 000).

---

## Находка 2 · `sample_submit.csv` — это факт предыдущего окна

Точное совпадение с GMV за 2026-01-15 … 02-13: $\mathrm{RMSLE} = 2\cdot 10^{-8}$, 100 % строк совпадают.

Значит это готовый бейзлайн «прошлые 30 дней» ($\mathrm{RMSLE} = 2.1951$ на валидации) и ориентир по масштабу сабмита — доля ненулевых и средний $\log(1+y)$.

---

## Метрика

$$\mathrm{RMSLE} = \sqrt{\frac{1}{n}\sum_{i=1}^{n}\Big(\log(1+\hat y_i) - \log(1+y_i)\Big)^2} = \mathrm{RMSE}(\hat z, z), \quad z = \log(1+y)$$

Ошибка зависит только от отношения $\dfrac{1+\hat y}{1+y}$ — промах «200 вместо 100» и «20 000 вместо 10 000» стоят одинаково.

| Величина | Значение |
| :--- | ---: |
| Оптимальная константа $c^{*} = e^{\overline{z}} - 1$ | **8.41** |
| Арифметическое среднее $y$ | 84.03 |
| $\mathrm{RMSLE}(c^{*})$ | 2.2883 |
| $\mathrm{RMSLE}(\overline{y})$ | 3.1750 |
| $\mathrm{RMSLE}(0)$ | 3.2036 |

Оптимальный прогноз по Йенсену:

$$\hat y^{*}(x) = \exp\big(\mathbb{E}[\log(1+y) \mid x]\big) - 1 \;\le\; \mathbb{E}[y \mid x]$$

Регрессия в исходной шкале рублей под RMSLE гарантированно завышает — учиться только в $\log(1+y)$.

**Шум public.** Бутстрап 2000 подвыборок по 50 000 даёт $\sigma = 0.0055$, то есть $\pm 0.011$ ($2\sigma$) для сравнения *независимых* выборок. Для двух сабмитов на одних и тех же пользователях парная разница информативна на порядок точнее.

---

## Находка 3 · Hurdle склеивается точно

Так как $\log(1+0) = 0$, нулевая компонента вносит в условное среднее ровно ноль:

$$\mathbb{E}\big[\log(1+y) \mid x\big] = p(x)\,m(x) \qquad\Longrightarrow\qquad \hat y^{*} = e^{\,p m} - 1$$

Биннинг-модель на трёх признаках, обучена на 2025-12-15, применена на 2026-01-14:

| Склейка | RMSLE |
| :--- | ---: |
| $e^{\,p m} - 1$ | **1.7468** |
| $p \cdot (e^{m} - 1)$ | 2.1207 |
| $e^{m} - 1$ без классификатора | 2.6993 |

> Разница **0.37** при идентичных $p$ и $m$ — одна строка кода.

Поправка при передискретизации позитивов с коэффициентом $w$:

$$p = \frac{p_s}{p_s + (1-p_s)\,w}$$

Без неё растёт AUC и падает RMSLE.

---

## Весь сигнал в «купит ли», не в «на сколько»

Карта recency $\times$ частота покупок, одни и те же ячейки:

| Величина | Диапазон | Разброс |
| :--- | :--- | ---: |
| $p(x) = P(y>0)$ | $0.124 \to 0.998$ | **8.0×** |
| $m(x) = \mathbb{E}[\log(1+y) \mid y>0]$ | $3.39 \to 6.19$ | **1.83×** |

Сравнение диапазонов само по себе ничего не доказывает: относительный размах
не равен вкладу в дисперсию. Прямая абляция — заморозить одну компоненту,
оставив вторую:

| Прогноз | shape RMSE |
| :--- | ---: |
| $p(x)\,m(x)$ — полный | 1.68512 |
| $p(x)\cdot\overline{m}$ — только классификатор | 1.77066 |
| $\overline{p}\cdot m(x)$ — только регрессия | 2.06962 |

Заморозка $m$ стоит 0.086, заморозка $p$ — 0.385: классификатор несёт в 4.5 раза
больше сигнала. Ёмкость модели — туда.

Доля нулевого таргета: 45.9 % на всех, 43.8 % на отфильтрованной популяции, по якорям 37.5–43.8 %.

Оптимальное сжатие прошлого GMV $\hat y = \exp(a \cdot \log(1+x)) - 1$ даёт $a^{*} \approx 0.73$ для всех окон — регрессия к среднему, которую модель обязана выучить.

---

## Сезонность окон

Уровень 30-дневного окна относительно тренда ($+4.2$ % за окно, включая взросление панели):

| Окно | GMV | Отклонение |
| :--- | ---: | ---: |
| 16.11 … 15.12.2025 | 25.52 млн | **$+8.3$ %** |
| 16.12 … 14.01.2026 | 25.36 млн | $+3.6$ % |
| **15.01 … 13.02.2026** | 21.01 млн | **$-19.3$ %** |

Валидационное окно — самый глубокий провал за всю историю.

**Календарный аналог боевого окна годом ранее.** 15.01–13.02.2025 $=$ 14.39 млн $\to$ 14.02–15.03.2025 $=$ 16.73 млн: отношение $1.163$, из них тренд $1.042$, чистая сезонность $1.116$. Средний $\log(1+y)$ вырос с $1.5396$ до $1.7154$, доля нулей упала с 63.3 % до 59.7 %. Это не всплеск, а равномерно приподнятый месяц с пиком перед 8 марта.

**Цена ошибки уровня.** Смещение входит квадратично:

$$\mathrm{RMSLE}^2_{\text{смещ}} = \mathrm{RMSLE}^2_{\text{база}} + \delta^2$$

| $\delta$ | Цена |
| ---: | ---: |
| $0.12$ | $+0.005$ |
| $0.176$ | $+0.010$ |
| $0.40$ | $+0.039$ |

### Где живёт сезонность — зависит от популяции

Разложим прошлогодний переход по маржам, $\ell = \bar p \cdot \ell^{+}$:

| Популяция | $a_p = \Delta\operatorname{logit}\bar p$ | $a_m = \Delta\ell^{+}$ | экстенсивная | интенсивная |
| :--- | ---: | ---: | ---: | ---: |
| Без фильтра, все 250 000 | $+0.151$ | $+0.066$ | **85 %** | 15 % |
| С фильтром активности | $+0.001$ | $+0.050$ | 4 % | **96 %** |

Выводы противоположны, и правая строка — правильная. Без фильтра $\bar p$ растёт
с 0.367 до 0.403 просто потому, что в феврале-марте активных людей больше:
это смена состава, а не изменение поведения. Стоит обусловиться на активности —
и вероятность покупки не двигается вообще ($0.5107 \to 0.5110$), весь подъём идёт
через условную сумму.

**Наша тестовая популяция условна на активности по построению** (раздел про правило
отбора), поэтому для нас сезонность почти целиком интенсивная: поправку надо класть
в $\ell^{+}$, а не в $\bar p$.

Это же объясняет, почему лидерборд просил вдвое меньше исторической оценки: полный
сезонный сдвиг уровня на сопоставимой популяции $+0.0265$, а не $+0.176$.

---

## Признаки: что помогает, что мешает

База — recency, активные дни за 90 дней, $\mathrm{gmv}_{90}$: $R^2 = 0.3653$ на 233 152 пользователях.

| Признак | Сырая $r$ | $\Delta R^2$ | Коэф. | Вывод |
| :--- | ---: | ---: | ---: | :--- |
| Средний чек за 90д | $+0.420$ | **0.0528** | $\mathbf{-0.883}$ | **переворот знака** |
| Дней с покупкой за 90д | $+0.638$ | 0.0459 | $+1.311$ | несёт своё |
| GMV за 30д | $+0.552$ | 0.0183 | $+0.212$ | несёт своё |
| GMV за 365д | $+0.563$ | 0.0158 | $+0.212$ | несёт своё |
| Корзины за 90д | $+0.504$ | 0.0057 | $+0.216$ | умеренный вклад |
| Recency покупки | $-0.195$ | 0.0055 | $-0.108$ | умеренный вклад |
| Запросы за 90д | $+0.460$ | 0.0035 | $+0.221$ | умеренный вклад |
| Доля фантомных дней | $-0.132$ | 0.0015 | $-0.527$ | балласт |
| Стаж | $+0.180$ | 0.0010 | $+0.244$ | балласт |
| Тренд GMV 30 / 60 | $+0.023$ | 0.0006 | $+0.026$ | балласт |
| Доля Поиска за 90д | $-0.007$ | 0.0003 | $+0.261$ | только с контролями |
| Доля выходных за 90д | $-0.015$ | 0.0000 | $-0.057$ | балласт |

> **Средний чек** — самый сильный кандидат и одновременно ловушка. В одиночку связь положительная, при контроле активности отрицательная: при фиксированных GMV и активности высокий чек означает, что покупок было **мало**. Работает только рядом с контролями по объёму, интерпретировать в изоляции нельзя.

**Тренд 30/60** бесполезен для *линейной* модели: она выведет отношение сама
из $\beta_1 a + \beta_2 b$.

> **Важная оговорка ко всей таблице.** $\Delta R^2$ здесь измерен линейной регрессией,
> и для GBDT это не приговор. Дерево не проверяет $a - b > c$ одним сплитом — ему
> нужен $O(\text{глубина})$ сплитов на аппроксимацию, тогда как явная разность
> берётся одним. Признаки, «избыточные» по линейной абляции, надо перепроверять
> абляцией на самом бустинге.

---

## Причинные проверки

### Промо переносят спрос

Отношение «14 дней после / 14 дней до» пикового дня:

| Пик | Отношение |
| :--- | ---: |
| 2025-10-15 | 0.985 |
| 2025-11-11 | 1.116 |
| 2025-12-03 | 1.142 |
| **2025-12-27** | **0.715** |

Новогодний пик крадёт у следующих недель. Признак $\mathrm{gmv}_{30}$ на декабрьском окне завышен; длинные окна страдают меньше.

### Конфаундинг переворачивает знак

Эффект высокой интенсивности Поиска на будущий GMV:

| Оценка | $\Delta$ mean $\log(1+y)$ |
| :--- | ---: |
| Наивная разница | $\mathbf{-0.2549}$ |
| IPW (ATT) | $+0.1332$ |
| Матчинг 1:1 | $+0.0055$, 95 % CI $[-0.021,\ +0.036]$ |

Весь видимый «отрицательный эффект Поиска» — конфаундинг общей активностью. Доля
канала маркирует тип пользователя, а не влияет на GMV.

> **Это не основание выбрасывать признак.** Causal-анализ отвечает на вопрос «если
> изменить интенсивность Поиска, изменится ли GMV», а модель решает другой —
> «содержит ли интенсивность Поиска информацию о будущем GMV». Даже чистый
> конфаундер бывает отличным предиктором: маркер латентного типа пользователя
> это ровно то, что нужно прогнозу. Ни один признак по ATT не удалялся.

Отдельно: чистого «каталожного» сегмента не существует — медианная доля поисковых дней $0.90$, порог «$\ge 0.75$ против $\le 0.25$» даёт 99.7 % против 0.3 %.

### Не-GMV-сигналы не дублируют историю покупок

Недельная панель, 30 000 пользователей, 4 лага, фиксированные эффекты пользователя:

| Модель | within-$R^2$ | Прирост |
| :--- | ---: | ---: |
| Только лаги GMV | 0.0163 | — |
| $+$ лаги корзин | 0.0245 | $+0.0082$ |
| $+$ лаги запросов | 0.0268 | $+0.0105$ |
| $+$ лаги активных дней | 0.0251 | $+0.0088$ |
| Всё вместе | 0.0284 | $+0.0121$ |

Прирост того же порядка, что вся база.

> Вместе с таблицей признаков это даёт конкретное указание: корзины и запросы подавать **лагами и трендами**, а не ещё одной 90-дневной суммой. Как суммы они дублируют базу, как динамика — несут своё. Это же положительный предтест для sequence-моделей.

---

## Прочее

- Недельная сезонность есть и глобально, и у отдельного пользователя, но медианная доля мощности в недельной полосе всего $0.036$ — Фурье-признаки проигрывают простому профилю дня недели.
- Энтропия дневных поведенческих токенов коррелирует с $\log(1+y)$ на $0.511$ — дёшево, но слабо.
- Интервалы между визитами: медиана 1 день, среднее 3.0, $p_{90} = 6$, максимум 338.
- Активных дней на пользователя: $p_{10} = 26$, медиана 102, $p_{90} = 254$ из 409.
