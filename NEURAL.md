# Нейросетевая ветка: Gap-GRU на остатке ансамбля

Первое направление за долгое время, давшее переносимый выигрыш. Цепочка `v16` → `v20` довела результат с **1.6502841** до **1.6487893**,
то есть **+0.00149** — вчетверо больше, чем дало добавление второго
семейства бустинга. Сам Gap-GRU дал первые $+0.00094$, остальное —
ансамбль сидов, комбинация направлений и годовой календарный аналог.

---

## 1. Постановка: сеть не предсказывает, а исправляет

Уже есть сильный прогноз $z_0$. Сеть учит поправку:

$$\hat z = z_0 + \alpha\,\Delta z, \qquad \Delta z = f_\theta(\text{история}, z_0)$$

Голова, порождающая $\Delta z$, **инициализирована нулями**. На первом шаге
обучения $\Delta z \equiv 0$, то есть модель тождественно равна текущему
ансамблю, и единственный способ уменьшить лосс — объяснить его ошибку.
Заново открывать recency, GMV за 90 дней, EWMA, $(q, c)$ и средний чек
не требуется: всё это уже в $z_0$.

Это главное отличие от провалившегося TCN, который предсказывал $z$ с нуля
и тратил ёмкость на воспроизведение того, что деревья и так знают.

$\alpha$ **не учится вместе с сетью**: при нулевой инициализации головы
произведение $\alpha \cdot \Delta z$ вырождено — сеть просто отмасштабирует
$\Delta z$. Вместо этого $\alpha$ находится после обучения как вершина
параболы на отдельном якоре:

$$\mathrm{MSE}(\alpha) = \mathrm{MSE}(0) - 2\alpha C + \alpha^{2} D,
\qquad C = \mathbb{E}[e\,\Delta z], \quad D = \mathbb{E}[\Delta z^{2}],
\qquad \alpha^{*} = C/D$$

---

## 2. Представление истории: событийные токены

Один токен = **один день, в котором есть строка активности**, а не один
календарный день. Медиана активных дней в окне 300 — 83, то есть дневное
представление на три четверти состоит из нулей.

Пауза кодируется явно отдельным каналом:

$$\Delta t_j = t_j - t_{j-1}$$

Отсутствие пользователя 17 дней — это одно число $\Delta t = 17$, а не
семнадцать одинаковых нулей. Для нас это даже естественнее, чем в GRU-D:
там пропуск наблюдения, у нас — факт поведения.

20 признаков на токен: десять числовых через $\log(1+x)$ (`gmv`, `gmv_search`,
`gmv_cat`, `to_cart`, `to_ord`, `searches`, `search_to_cart`, `search_to_ord`,
`cat_to_cart`, `cat_to_ord`), шесть бинарных (наличие каналов и переходов),
четыре временных (пауза, возраст события, синус и косинус дня недели).
Разделение `search`/`cat` сквозное — задача называется Search LTV.

Выравнивание по **правому** краю, `max_len = 192` покрывает 89.5 %
пользователей целиком; у остальных теряется старая история, не свежая.

---

## 3. Gap-GRU: обучаемый многомерный EWMA

Лучшие семьи признаков у нас — состояния с забыванием: EWMA с тремя
фиксированными $\delta$ дала $+0.0021$, дисконтированные байесовские фильтры
$+0.0028$. Обе имеют вид $S_t = \delta S_{t-1} + x_t$ с **заданной наперёд**
скоростью забывания.

Перед каждым шагом рекуррентности состояние затухает по паузе:

$$\gamma_j = \exp\bigl[-\mathrm{ReLU}(W_\gamma \phi(\Delta t_j) + b_\gamma)\bigr],
\qquad \bar h_{j-1} = \gamma_j \odot h_{j-1}, \qquad h_j = \mathrm{GRU}(x_j, \bar h_{j-1})$$

`ReLU` внутри экспоненты гарантирует $\gamma \le 1$: на паузе состояние может
только забываться, но не расти. Существенно, что $\gamma_j \in \mathbb{R}^{96}$,
а не скаляр — разные координаты памяти забывают с разной скоростью. Одна может
держать денежный масштаб месяцами, другая — намерение купить несколько дней.

---

## 4. Четыре запроса вместо одного пулинга

Разложение взято из BTYD как inductive bias, но параметры не постулируются.
Вместо одного attention-пулинга заведены четыре обучаемых запроса —
`freq`, `amount`, `intent`, `activity`. Каждый складывается из собственного
вектора и проекции $[h_n, \text{prior}]$, то есть спрашивает «какие эпизоды
прошлого релевантны **этому фактору** при том, что модель предсказывает сейчас».

Полного self-attention нет сознательно: при 192 токенах он добавил бы
параметры и дисперсию, не добавив конкретного смещения.

**Признак-приор** — не $[p_0, m_0, z_0]$, а $[z_0, \; z_0^{\text{LGB}} -
z_0^{\text{CB}}, \; \log(1+L)]$. Средний член — расхождение семейств, уже
измеренное как индикатор эпистемической неопределённости (RMSE растёт
$1.293 \to 1.977$ по квинтилям расхождения). Он прямо указывает сети, **где
базовый прогноз ненадёжен**, то есть где поправку вообще имеет смысл делать.

---

## 5. Честный базовый прогноз

$z_0$ на каждом обучающем якоре получен моделью, **не видевшей этого якоря**
(leave-one-anchor-out, шесть переобучений пары LightGBM + CatBoost). Если взять
production-предсказание, остаток окажется искусственно мал и будет содержать
следы подгонки, а не непокрытый сигнал: сеть выучит их, покажет прекрасную
валидацию и рассыплется.

Уровень при предсказании берётся собственный для якоря — уровень окна
калибруется отдельно и точно, и остаток должен нести только форму.

---

## 6. Два этапа обучения

**Этап A — многозадачный претрейн.** Срезы каждые 14 дней, цели на горизонтах
7 и 30 дней: $z$, признак покупки, число дней с покупкой, число заказов.
Остаток ансамбля слишком шумная цель, чтобы в одиночку учить энкодер
($R^2$ по сырому ряду измерен как $+0.0008$).

**Пуржинг:** срез $T$ допустим, только если $T + 30 \le A$, где $A$ — якорь
подбора $\alpha$ этого фолда. Отступ равен **горизонту**, а не шагу нарезки;
проверяется `assert`-ом. На смешении этих величин уже была поймана утечка
в 0.005.

**Этап B — остаточное дообучение.** Энкодер заморожен первые две эпохи: остаток
шумный, и ранняя разморозка стёрла бы выученное поведение градиентами по шуму.

---

## 7. Критерий, заданный до запуска

Два фолда с непересекающимися целевыми окнами:

| Фолд | Обучение | Подбор $\alpha$ | Оценка |
| ---: | :--- | ---: | ---: |
| 0 | 198…288 | 318 | 348 |
| 1 | 198…318 | 348 | 378 |

Поправка принимается, только если выигрыш **положителен на обоих** фолдах
и превышает 0.0002.

---

## 8. Результат основного прогона (`ARCH=evt`, 12 эпох)

| Фолд | Оценка на | base | стало | Δ shape | $\alpha$ |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 348 | 1.75166 | 1.75139 | $+0.00026$ | $+0.3499$ |
| 1 | 378 | 1.68112 | 1.68024 | $+0.00088$ | $+0.3604$ |

**Убедительнее выигрыша — согласованность $\alpha$.** Два независимых подбора
на разных якорях, по моделям с разными обучающими наборами, дали $0.3499$ и
$0.3604$: расхождение 3 %. То же по компонентам параболы: $D = 0.0159$ против
$0.0166$, $C = 0.00555$ против $0.00599$.

**Поправка ортогональна ансамблю.** Сеть училась исправлять OOF-пару, а сабмит
собран из ансамблей и сильнее. Проверка на якоре 378:

| База | shape | с поправкой | Δ |
| :--- | ---: | ---: | ---: |
| OOF-пара (на ней обучение) | 1.68112 | 1.68024 | $+0.00088$ |
| ансамбль CatBoost (11) | 1.67999 | 1.67905 | $+0.00094$ |
| смесь 0.4/0.6 — рецепт v15 | 1.67988 | 1.67904 | $+0.00084$ |

Корреляция $\Delta z$ с ошибкой почти не меняется при усилении базы:
$+0.0330$ против $+0.0321$. Ансамбль эту информацию не поглощает.

**Лидерборд:** $1.6502841 \to 1.6493404$, то есть $+0.00094$ при валидационных
$+0.00084$ — перенос 112 %. Проекция поправки на реальную ошибку **больше**,
чем на валидационном якоре: $C = 0.00733$ на ЛБ против $\approx 0.006$
на валидации.

**Вес закрыт аналитически** двумя точками лидерборда: $D = 0.016454$
считается из файлов, отсюда $C = 0.007328$ и $\alpha^{*} = 0.445$ при
применённых $0.35$. Вершина даёт лишь $+0.000045$ — из полного ресурса
направления $C^2/D = 0.000989$ взято **95 %**.

---

## 9. Что дальше выигрыша не даст, и почему

Дальше нужен **другой** $\Delta z$, а не другой вес. Проверенные тупики:

**Обучать дольше — нет.** Прогон на 40 эпохах показал, что переносимый сигнал
максимален на 4-й эпохе и монотонно умирает, тогда как лосс продолжает падать:

| эпоха | 2 | 4 | 8 | 14 | 20 | 30 | 40 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| выигрыш | $+0.00044$ | $+0.00055$ | $+0.00051$ | $+0.00020$ | $+0.00003$ | $+0.00003$ | $+0.00002$ |
| $\alpha$ | $+1.158$ | $+0.571$ | $+0.475$ | $+0.114$ | $+0.047$ | $+0.014$ | $+0.012$ |
| $\mathrm{std}(\Delta z)$ | 0.037 | 0.083 | 0.091 | 0.187 | 0.340 | 0.602 | 0.773 |

Сеть строит всё более крупную поправку, всё менее связанную с настоящей
ошибкой. Лосс падает, потому что мерит запоминание обучающих якорей.
Отсюда практический вывод: **штраф $\lambda\|\Delta z\|^2 = 0.05$ слишком слаб** —
при $\mathrm{std}(\Delta z) = 0.77$ штрафной член даёт 0.03 против основного
лосса 2.6.

**Выбор лучшей эпохи по якорю подбора — тоже нет.** Пик кривой лежит в пределах
шума ($+0.00044 / +0.00055 / +0.00052 / +0.00051$), и выбор максимума
оказывается выбором шума: восстановленная эпоха 4 дала на тестовом якоре
$-0.00074$.

---

## 9a. Методическая ошибка в прогоне `ARCH=cyc`

В нём изменены сразу три вещи: архитектура (добавлены токены покупочных
циклов, 306 293 параметра против 229 557), эпохи этапа A (15 против 6) и эпохи
этапа B (40 против 12). Разделить вклады нельзя. Корректное сравнение требует
прогона `ARCH=cyc` при `EPOCHS_A=6 EPOCHS_B=12` — тогда разница будет
относиться только к циклам.

---

## 9b. Что было после: сиды, комбинация, годовой аналог

**Ансамбль сидов.** Три боевые модели на **всех семи** якорях (в фолдовой
схеме 348 и 378 тратятся на подбор $\alpha$ и оценку, то есть два самых
свежих окна не участвуют в обучении). Направления усредняются **после
нормировки** — масштаб каждого произволен, его поглотит $\alpha$, поэтому
подогнанный скаляр остаётся ровно один.

Попарная корреляция направлений между сидами $0.863\ldots0.882$. Для
сравнения: у деревьев смена сида давала $0.998$ и почти ничего. Результат
$1.6493404 \to 1.6491770$.

**Комбинация направлений.** Три замера лидерборда определяют оптимальную
пару весов без подгонки: матрица Грама считается целиком из csv, каждый
одиночный замер даёт свою компоненту вектора ковариаций.
$b^{*} = G^{-1}c = (0.631, 0.706)$, предсказание $1.6490540$, замерено
$1.6490671$.

**Годовой аналог** — третье направление, почти ортогональное обоим
нейронным ($\rho = 0.083$ и $0.040$). Подробности в
[EXPERIMENTS.md](EXPERIMENTS.md). Итог $1.6487893$.

Оптимальная **тройка** весов $(0.558, 0.727, 0.446)$ против применённых
$(0.631, 0.706, 0.452)$ даёт $+0.0000021$ — ось комбинации исчерпана.

---

## 10. Веса

`artifacts/neural/weights/`:

| Файл | Параметров | Что это |
| :--- | ---: | :--- |
| `gapgru_evt_ckpt_fold0.pt` | 229 557 | этап B, фолд 0, эпоха 12 |
| `gapgru_evt_ckpt_fold1.pt` | 229 557 | **этап B, фолд 1 — из него сделан `v16`** |
| `pretrain_evt_fold0.pt` | 229 557 | этап A, фолд 0, 6 эпох |
| `pretrain_evt_fold1.pt` | 229 557 | этап A, фолд 1, 6 эпох |
| `gapgru_cyc_ckpt_fold*.pt` | 306 293 | прогон с циклами (см. § 9a) |
| `pretrain_cyc_fold*.pt` | 306 293 | этап A с циклами, 15 эпох |

Загрузка:

```python
import torch
from ecup.gapgru import GapGRUConfig, make_model
from ecup.tokens import TOKEN_FEATURES

cfg = GapGRUConfig(n_features=len(TOKEN_FEATURES) - 2, max_len=192)
model = make_model(cfg)
model.load_state_dict(torch.load(
    "artifacts/neural/weights/gapgru_evt_ckpt_fold1.pt",
    map_location="cpu")["model"])
```

---

## 11. Производительность: девятикратное ускорение одной строкой

Профиль на A4000 показал, что обратный проход занимает **96.2 %** времени
и в 32 раза дороже прямого при норме около двух.

Причина: `x[:, t]` и `gamma[:, t]` внутри рекуррентного цикла — отдельные
операции автографа, и обратный проход каждой создаёт **полноразмерный**
нулевой тензор $(B, L, H)$, чтобы рассеять в него градиент одного шага.
При 192 шагах на слой это 192 аллокации по 151 МБ.

`unbind(1)` делает то же разбиение одной операцией, обратный проход
которой — один `stack`:

| | прямой | обратный | всего |
| :--- | ---: | ---: | ---: |
| нарезка внутри цикла | 43.4 мс | 1142.9 мс | 1186.3 мс |
| `unbind` перед циклом | 43.4 мс | **78.5 мс** | **122.0 мс** |

На реальном обучении: эпоха 52 с против 485 с. До правки использовалось
**0.6 %** пика карты — замена железа на более быстрое дала бы почти ноль.

---

## 12. Запуск

```bash
bash scripts/vast_launch.sh search        # найти инстанс
bash scripts/vast_launch.sh create <id>
bash scripts/vast_launch.sh upload <id>
# на инстансе:
bash scripts/vast_bootstrap.sh
ARCH=evt EPOCHS_A=6 EPOCHS_B=12 bash scripts/run_cluster.sh
```

Наблюдение за обучением:

```bash
bash scripts/tensorboard.sh               # туннель на localhost:6006
```

Смотреть надо на `holdout/gain`, а **не** на `loss`: лосс включает
вспомогательные головы и падает даже тогда, когда поправка перестала
улучшаться — именно это и показал прогон на 40 эпохах.

Регламент развёртывания и ловушки — в [CLUSTER.md](CLUSTER.md).
