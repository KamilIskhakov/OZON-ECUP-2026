# Обучение на GPU-инстансе

Конвейер `Gap-GRU → query-attention → остаток к ансамблю`. Всё, что не требует
GPU, считается локально; на инстанс уезжает только то, что упирается в
последовательный проход по 192 токенам.

---

## 1. Что везти на инстанс

| Что | Размер | Зачем |
| :--- | ---: | :--- |
| репозиторий | ~3 МБ | код |
| `data_start/train.parquet` | 172 МБ | панель, из неё собираются токены |
| `artifacts/neural/oof_a*.npz` | ~20 МБ | честный базовый прогноз |

Токены **не везти**. Полный набор — 8 якорей × 233 тыс. пользователей ×
192 токена × 20 признаков в fp16 = 13.7 ГБ; собрать их на месте из parquet
быстрее, чем залить.

```bash
rsync -az --exclude '.git' --exclude 'artifacts/tokens' \
      ./ root@<host>:/workspace/ecup/ -e "ssh -p <port>"
```

Требования к инстансу: CUDA-образ (`pytorch/pytorch:*-cuda*-runtime`),
**диск от 60 ГБ** (25 ГБ токены + 10 ГБ кэш срезов + запас), любая карта
от 12 ГБ видеопамяти. Модель — 180 тыс. параметров, память съедают активации
рекуррентного прохода, а не веса.

---

## 2. Запуск

```bash
bash scripts/vast_bootstrap.sh      # проверка GPU, зависимости, целостность данных
bash scripts/run_cluster.sh         # полный конвейер
```

Уменьшенный прогон для проверки на дешёвой карте:

```bash
MAXLEN=96 USERS=80000 EPOCHS_A=3 EPOCHS_B=6 bash scripts/run_cluster.sh
```

Все три шага идемпотентны: токены и срезы кэшируются на диск, чекпоинты
пишутся каждую эпоху. После прерывания spot-инстанса достаточно запустить
`run_cluster.sh` повторно.

---

## 3. Этапы

### Токены

`scripts/build_tokens.py` — один токен на день с активностью, выравнивание
по правому краю. Медиана 83 токена, `max_len=192` покрывает 89.5 %
пользователей целиком; у остальных теряется старая история, не свежая.

### Этап A — многозадачный претрейн

`scripts/pretrain_gapgru.py`. Срезы каждые 14 дней, цели на горизонтах 7 и 30
дней: $z$, признак покупки, число дней с покупкой, число заказов. Энкодер
учит модель поведения, а не шумный остаток.

**Пуржинг.** Срез $T$ допустим, только если $T + 30 \le A$, где $A$ — якорь
подбора $\alpha$ соответствующего фолда. Отступ равен ГОРИЗОНТУ, а не шагу
нарезки: на смешении этих двух величин уже была поймана утечка в 0.005
(см. [EXPERIMENTS.md](EXPERIMENTS.md)). Скрипт проверяет условие `assert`-ом.

### Этап B — остаток к ансамблю

`scripts/train_gapgru.py`. Цель — $z - z_0^{OOF}$, где $z_0$ на каждом якоре
получен моделью, не видевшей этого якоря. Голова поправки инициализирована
нулями, поэтому на первом шаге $\hat z \equiv z_0$ и единственный способ
уменьшить лосс — объяснить ошибку ансамбля. Энкодер заморожен первые две
эпохи: остаток шумный, и ранняя разморозка стёрла бы выученное поведение.

---

## 4. Критерий, заданный до запуска

Два фолда с непересекающимися целевыми окнами:

| Фолд | Обучение | Подбор $\alpha$ | Оценка |
| ---: | :--- | ---: | ---: |
| 0 | 198…288 | 318 | 348 |
| 1 | 198…318 | 348 | 378 |

$\alpha$ считается вне сети как вершина параболы
$\mathrm{MSE}(\alpha) = \mathrm{MSE}(0) - 2\alpha C + \alpha^{2}D$,
где $D = \mathbb{E}[\Delta z^{2}]$, $C = \mathbb{E}[e\,\Delta z]$.

**Поправка принимается, только если выигрыш положителен на ОБОИХ фолдах
и превышает 0.0002.** Плюс на одном и минус на другом — шум. Скрипт печатает
долю переноса: сколько процентов выигрыша якоря подбора дожило до тестового
якоря. Низкий перенос при большом $\alpha$ означает, что $\alpha$ описал
особенность одного окна.

---

## 5. Ожидаемая отдача

Предтест — $R^2$ остатка по сырому дневному ряду через GBDT — дал $+0.00081$
против $-0.00124$ на перемешанном контроле. В шкале метрики:

| $R^2$ остатка | shape | выигрыш |
| ---: | ---: | ---: |
| 0.00081 (замер) | 1.68108 | $+0.00068$ |
| 0.0015 | 1.68050 | $+0.00126$ |
| 0.0030 | 1.67924 | $+0.00252$ |

Это нижняя граница: GBDT видит «день −37» независимым признаком, у него нет
ни трансляционной инвариантности, ни явного $\Delta t$, ни многомасштабного
пулинга — то есть ровно того, ради чего берётся рекуррентность с затуханием.

---

## 6. Бюджет

| Шаг | GPU-часов (порядок) |
| :--- | ---: |
| Токены | 0.3 |
| Этап A, два фолда | 3 |
| Этап B, два фолда | 2 |
| **Полный прогон** | **~5.5** |

При $0.35/час это около $2 за прогон. Оценка получена масштабированием
локального замера на MPS (24 тыс. примеров при `max_len=32` — 7.5 с/эпоха)
и потому груба; сверьте по первой эпохе и остановите инстанс, если время
эпохи выше ожидаемого втрое.

Основное узкое место — последовательный проход по 192 шагам: работа на шаге
мала, и время определяется запусками ядер, а не арифметикой. Отсюда большой
`BS` (2048 по умолчанию) — он амортизирует накладные расходы.
