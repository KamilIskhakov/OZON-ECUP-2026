# Журнал гипотез

Что проверено, с каким результатом и каким механизмом объясняется. Отрицательные
результаты здесь не менее важны положительных: они закрывают двери, в которые
иначе заходят повторно.

**Критерий.** Везде `shape` $= \operatorname{std}(z - \hat z)$, где $z = \log(1+y)$.
Метрика раскладывается на `bias` (чинится одним числом) и `shape` (чинится только
моделью). Отбор идёт по `shape`: уровень окна калибруется отдельно, и валидационный
`bias` на боевое окно не переносится (см. § «Что переносится»).

**Схема проверки.** Обучение на одних якорях, оценка на другом, более позднем.
Прогноз на якорь всегда делает модель, не видевшая его таргета.

---

## Сработало

| Гипотеза | Δ shape | Механизм |
| :--- | ---: | :--- |
| Точная склейка $e^{pm}-1$ вместо $p(e^m-1)$ | **$+0.35$** | $\log(1+0)=0$ делает разложение $\mathbb{E}[z\mid x]=pm$ тождеством |
| Правило отбора популяции на каждом якоре | ~$+0.01$ по bias | без него в трейне популяция, которой в тесте нет |
| Раздельная нормировка марж $\bar p$ и $\ell^{+}$ | $+0.009$ по bias | $\ell$ гуляет на 14.5 %, $\ell^{+}$ на 4.5 % |
| Байесовские фильтры $q\cdot c$ | $+0.0028$ | разделяют «перестал заходить» и «перестал конвертироваться» |
| EWMA fast/mid/slow | $+0.0021$ | $S_{\text{fast}}-S_{\text{slow}}$ выражает направление, окно — нет |
| Окно признаков 300 вместо 168 | $+0.0011$ | монотонный рост до 240–300, дальше плато |
| Ранняя остановка | $+0.0008$ | оптимум 194/197 деревьев против фиксированных 600 |
| Ритм покупок (интервалы между покупками) | $+0.0007$ | сигнал сидит в покупках, а не в визитах |
| Удаление календаря якоря из признаков | $+0.0073$ | `anchor_doy` — идентификатор якоря, экстраполируется вслепую |
| Равновесное усреднение разнообразных моделей | $+0.0019$ суммарно | снижение дисперсии, шесть подтверждений подряд |
| Дообучение на 100 % после ранней остановки | $+0.0001$ | возврат 12 % отложенных строк |
| CatBoost как отдельное семейство в ансамбле | $+0.00109$ | симметричные деревья + упорядоченный бустинг дают ниже $\rho$ ошибок, чем любая настройка LightGBM |

---

## Не сработало

| Гипотеза | Δ shape | Почему |
| :--- | ---: | :--- |
| Metric-aligned регрессия под $(pf-z)^2$ | $-0.0051$ | вес $p^2$ выбрасывает низковероятных; $f^{*}=pm/\tilde p$ впитывает ошибки классификатора |
| Двумерная калибровка $g(p,m)$ | $-0.0016$ | $p$-зависимое смещение не переносится между якорями |
| Phase ensemble (сдвиг 0/7/15/22) | $-0.0008$ | фазы неравнокачественны: последние якоря на 0/7/15/22 дня от цели |
| Percentile и robust-z признаки | $-0.0003$ | дрейф между якорями в основном уровневый, уже снят нормировкой марж |
| Дискретный hazard по неделям | $-0.0002$ | тайминг предсказывается теми же признаками, что и факт; AUC совпал до 4-го знака |
| Недельные лаги (12 лагов, 75 признаков) | $+0.0005$ | окна 7/14/30/…/300 уже покрывают ось времени |
| Ёмкость GBDT (63 → 511 листьев) | $\pm 0.0012$ | разброс внутри шума; всё решает число деревьев |
| Неконтролируемые представления траекторий | $+0.0005$ | LSA, SVD, kNN сжимают дисперсию, а не сигнал под задачу |
| Sequence-энкодер TCN + attention | $-0.0002$ | замещает существующие признаки; проверена одна конфигурация |
| Персональный residual effect | $+0.0002$ | автокорреляция остатков $-0.010\ldots-0.018$, знак отрицательный |
| Аффинная калибровка $p$ и spread $z$ | знак не переносится | $b_p$: 1.021 на одной паре якорей, 0.941 на другой |
| Глобальный стекинг (12 весов) | $+0.0016$ вал., **$-0.0003$ паблик** | веса подогнаны под шум одного якоря |
| Условный стекинг (15 сегментных весов) | $-0.0001$ | структура реальна (знак совпал 13/15), но дисперсия оценки её съедает |

---

## Что переносится между окнами, а что нет

Главный методический вывод проекта. Все подгонки упорядочены по числу степеней
свободы:

| Настройка | Параметров | Перенос на боевое окно |
| :--- | ---: | :--- |
| Уровень $a_m$ | 1 | **98 %** — точно по восстановленной параболе |
| Аффинная калибровка | 2 | знак меняется между парами якорей |
| Веса стекинга | 12 | $+0.0016$ на валидации → $-0.0003$ на паблике |
| Сегментные веса | 15 | знак переносится, величина нет |

> Переносится одномерная поправка уровня и **знак** сегментных эффектов.
> Не переносится их величина — а именно она нужна для выигрыша.

Отсюда практическое правило: **равновесное усреднение разнообразных моделей
надёжнее любой подгонки весов.**

---

## Диагностика, не приведшая к действию

**Карта остатков.** Две трети SSE лежат в средних квартилях $p$ при половине
пользователей: при $p$ около 0 или 1 исход почти детерминирован, в середине —
настоящая неопределённость. Самая дорогая ячейка: $p\in q_3$ и 3–7 дней с покупкой
за 90 дней — 25 % SSE при 18 % пользователей.

**Разложение ошибки.** Непокупатели дают 49.5 % SSE, покупатели 50.5 %. Внутри
покупателей: промах по сумме 24.3 %, цена хеджирования $m^2(1-p)^2$ — 32.0 %.

Важная оговорка: **это не доля ошибки классификатора**. Член $p(1-p)m^2$ есть
дисперсия бернуллиева исхода и неустраним при любой модели. Оценка неустранимой
части $\mathbb{E}[\mathrm{Var}(Z\mid X)] = p\,\mathrm{Var}(A\mid C{=}1,X) + p(1-p)m^2$
дала величину порядка наблюдаемого MSE — то есть запас над байесовским шумом мал.

**Калибровка $p$.** По 20 бинам отклонение от факта от $-0.036$ в центре до
$-0.0001$ на краю, суммарная взвешенная ошибка $0.0006$.

**Расхождение моделей как эпистемическая неопределённость.** RMSE ошибки растёт
монотонно по квинтилям $\operatorname{std}_k \hat z_k$: 1.293 → 1.977, корреляция
с $|e|$ равна $+0.24$. Индикатор рабочий, но под квадратичным лоссом оптимальная
точечная оценка есть условное среднее независимо от уверенности в нём, поэтому
прямого способа использовать не найдено.

---

## Предтесты, закрывшие направления до дорогой реализации

| Предтест | Результат | Что закрыл |
| :--- | :--- | :--- |
| $R^2$ остатка $z$ по сырому дневному ряду | $+0.0008$ против $-0.0012$ на перемешанном | понизил априор для sequence-моделей |
| $R^2$ остатка $C-\hat p$ по сырому ряду | $+0.0008$ против $-0.0005$ | $P(C{=}1\mid X,\text{seq}) \approx P(C{=}1\mid X)$ |
| Автокорреляция персональных остатков | $\rho = -0.010\ldots-0.018$ | персональный random effect, DBN над ним |
| Granger-стиль на недельной панели | $+0.012$ within-$R^2$ | подтвердил ценность поведенческих сигналов **до** их внедрения |
| Корреляция остатка с календарным аналогом год назад | $\rho = +0.021$, частная $+0.041$; линейный эксперт даёт $+0.0004$ | YoY-эксперт как отдельная модель; признак оставлен в общем пуле |

---

## Не проверено

| Направление | Почему отложено |
| :--- | :--- |
| GRU/Transformer на остатке ансамбля | три предтеста дали около нуля; требует GPU для осмысленного перебора |
| Токенизация покупочными циклами | зависит от результата предыдущего пункта |
| Self-supervised предобучение энкодера | второй нейросетевой этап, не первый |
| Гиперпараметры сверх ёмкости (`min_child_samples`, `lambda_l1`, `max_bin`) | ожидаемая отдача мала на фоне десяти отрицательных результатов |
| Adversarial validation и importance weights | после отрицательного результата по percentile-признакам ковариатный сдвиг выглядит малым |

---

## CatBoost: почему второе семейство, а не третий сид

Выигрыш равновесной смеси $K$ моделей с равными дисперсиями ошибок и попарной
корреляцией $\rho$ равен

$$\sigma^2_{\text{ens}} = \sigma^2\Big(\rho + \frac{1-\rho}{K}\Big)$$

то есть при $\rho \to 1$ ансамбль не даёт ничего независимо от числа участников.
Поэтому единственная величина, которую имеет смысл мерить у кандидата в ансамбль,
это $\rho$ его остатка с уже имеющимися, а не его собственное качество.

Замер на валидационном якоре, окно 300 дней:

| Пара | $\rho$ остатков | shape смеси | выигрыш |
| :--- | ---: | ---: | ---: |
| LightGBM + LightGBM (другой сид) | 0.99837 | 1.68224 | $+0.00067$ |
| LightGBM + CatBoost | **0.99774** | **1.68131** | **$+0.00161$** |

Смена сида меняет только реализацию бэггинга и подвыборки признаков; алгоритм
роста дерева остаётся тем же жадным листовым разбиением по тому же критерию,
поэтому модели ошибаются почти одинаково. CatBoost меняет сам алгоритм:
симметричные деревья (одно разбиение на весь уровень), упорядоченный бустинг
(градиент объекта считается моделью, не видевшей этого объекта) и другая
дискретизация признаков.

Одиночные модели, все шесть конфигураций:

| Окно | LightGBM 63 листа | CatBoost depth 6 | LightGBM 127 листьев | CatBoost depth 7 |
| ---: | ---: | ---: | ---: | ---: |
| 240 д | 1.68679 | **1.68591** | 1.68834 | **1.68571** |
| 300 д | 1.68291 | **1.68160** | 1.68504 | **1.68158** |
| 365 д | 1.68315 | **1.68059** | 1.68615 | **1.68067** |

Два вывода. Первый: CatBoost выигрывает **6 из 6**, значит преимущество не
артефакт одной конфигурации. Второй, менее очевидный: при росте ёмкости
LightGBM теряет 0.0016–0.0030, а CatBoost не теряет ничего. Симметричное дерево
не может выделить узкий лист под шумную подгруппу, поэтому лишняя ёмкость
не конвертируется в переобучение — и ось ёмкости у CatBoost можно раскрывать
шире, получая разнообразие бесплатно.

Ансамбли по шесть моделей в каждом семействе (окно × ёмкость × сид):

| Ансамбль | shape |
| :--- | ---: |
| только LightGBM | 1.68100 |
| только CatBoost | 1.68016 |
| смесь, доля CatBoost 0.6 | **1.67992** |

Кривая по доле CatBoost плоская на $[0.5, 0.8]$ (разброс 0.00008, ниже порога
разрешения валидации 0.0005), поэтому доля выбрана 0.6, а не по минимуму:
подгонять параметр внутри плоского участка означает подгонять шум.

---

## Доля CatBoost: закрыта аналитически двумя точками лидерборда

Сабмиты лежат на прямой в $z$-пространстве: $z(a) = z_{v10} + a\,d$, где
$d = z_{\text{cb}} - z_{v10}$. Тогда ошибка строго квадратична по $a$:

$$\mathrm{MSE}(a) = \mathrm{MSE}(0) - 2aC + a^{2}D, \qquad
C = \mathbb{E}[e_{v10}\,d], \quad D = \mathbb{E}[d^{2}]$$

Существенно, что **кривизна $D$ считается прямо из двух csv и не требует
ни одного замера** — она зависит только от расхождения прогнозов, не от
истины. Поэтому двух точек лидерборда достаточно, чтобы восстановить $C$
и найти вершину $a^{*} = C/D$; третий сабмит на подбор веса не нужен.

Замеры: $\mathrm{RMSLE}(0) = 1.6508203$, $\mathrm{RMSLE}(0.6) = 1.6502841$,
$D = 0.004099$. Отсюда $C = 0.002705$, $a^{*} = 0.66$.

| $a$ | 0.0 | 0.4 | 0.5 | 0.6 | 0.66 | 0.8 | 1.0 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| RMSLE | 1.6508203 | 1.6503635 | 1.6503114 | **1.6502841** | 1.6502797 | 1.6503041 | 1.6504234 |

Вершина даёт $+0.0000044$ к уже отправленному $a = 0.6$ — пересылать нечего.

**Главное следствие — про исчерпание направления.** Максимум, извлекаемый
из одного направления $d$, равен $C^{2}/D = 0.00179$ в шкале MSE, то есть
ровно те $0.00054$ RMSLE, что уже получены. Дальнейший выигрыш требует
не другого веса, а **другого $d$** — модели, отличающейся и от v10, и от
ансамбля CatBoost.

Побочный результат: предсказание, что оптимум сместится к $0.5$ из-за
превосходства v10 над шестимодельным LightGBM, оказалось неверным —
вершина легла правее $0.6$, а не левее.

---

## Плотность якорей: почему первый замер дал противоположный знак

Уплотнение якорей (`stride` 30 → 15 → 10) — это многооконная нарезка,
дающая втрое больше обучающих строк из той же истории. Первый замер показал
**минус 0.0057** у LightGBM, второй, после двух исправлений, — **плюс 0.0005**.
Разбор нужен, потому что обе ошибки относятся к классу, который не виден
в коде и виден только в диагностике.

**Ошибка первая — утечка целевого окна.** `train_anchors` отступал от
валидационного якоря на `stride`. При `stride = H = 30` это совпадает
с горизонтом и безопасно. При `stride = 15` старшим обучающим якорем
становится 363, чьё целевое окно 364…393 пересекается с валидационным
379…408 на пятнадцати днях: модель обучалась на половине того, что
предсказывала. Отступ должен равняться **горизонту**, а не шагу.

**Ошибка вторая — слепая ранняя остановка.** После починки утечки замер
всё ещё давал минус, и число деревьев везде упиралось в лимит: (3000, 1919),
(3000, 2999), (2500, 2500). Механизм тот же по природе: при перекрывающихся
целевых окнах строка из отложенных 12 % имеет в обучении «сестру» того же
пользователя, покрывающую половину тех же дней таргета. Отложенная выборка
перестаёт быть отложенной, остановка не срабатывает, модель уходит далеко
за оптимум. Сравнивались не «6 якорей против 12», а «6 якорей в оптимуме
против 12 сильно за оптимумом».

Профиль по валидационному якорю при правильном числе деревьев:

| | stride 30 | stride 15 | stride 10 |
| :--- | ---: | ---: | ---: |
| LightGBM | 1.68173 (200, 100) | 1.68155 (250, 150) | **1.68122** (250, 150) |
| CatBoost | 1.68138 (1150, 700) | 1.68132 (950, 1550) | — |

Вывод: плотная временная разметка несёт информацию, но для GBDT почти вся
она съедается зависимостью соседних окон. Для энкодера, который использует
срезы как примеры перехода состояния, а не как дополнительные строки,
ограничение другое — отсюда претрейн на срезах с шагом 14 дней.

**Общий урок.** Оба раза наблюдение было верным, а объяснение — нет.
Диагностика «деревьев (600, 600)» правильно говорила, что лимит связывающий;
неверен был вывод о недообученности. Прежде чем объяснять число, стоит
проверить, что измеряется именно то, что кажется.

---

## Gap-GRU на остатке ансамбля: первый положительный результат нейросетевой ветки

Критерий был задан **до** запуска: плюс требуется на обоих временных фолдах
и выше 0.0002. Пройден.

| Фолд | Обучение | Оценка на | base | стало | Δ shape | $\alpha$ |
| ---: | :--- | ---: | ---: | ---: | ---: | ---: |
| 0 | 198…288 | 348 | 1.75166 | 1.75139 | $+0.00026$ | $+0.3499$ |
| 1 | 198…318 | 378 | 1.68112 | 1.68024 | $+0.00088$ | $+0.3604$ |

**Убедительнее выигрыша — согласованность $\alpha$.** Два независимых подбора
на разных якорях, по моделям с разными обучающими наборами, дали $0.3499$ и
$0.3604$: расхождение 3 %. То же по компонентам параболы: $D = 0.0159$ против
$0.0166$, $C = 0.00555$ против $0.00599$. Совпадают и длина направления
$\Delta z$, и его проекция на остаток. Ни одна из тринадцати провалившихся
гипотез этого не показывала: знак иногда переносился, величина — никогда.

**Поправка ортогональна ансамблю.** Главная опасность была в том, что сеть
училась исправлять OOF-пару LightGBM+CatBoost, а боевой сабмит собран из
ансамблей и сильнее. Проверка на якоре 378:

| База | shape | с поправкой | Δ | оптимальная $\alpha$ |
| :--- | ---: | ---: | ---: | ---: |
| OOF-пара (на ней обучение) | 1.68112 | 1.68024 | $+0.00088$ | $+0.363$ |
| ансамбль LightGBM (6) | 1.68100 | 1.68032 | $+0.00069$ | $+0.295$ |
| ансамбль CatBoost (11) | 1.67999 | 1.67905 | $+0.00094$ | $+0.374$ |
| смесь 0.4/0.6 — рецепт v15 | 1.67988 | 1.67904 | $+0.00084$ | $+0.343$ |

Корреляция $\Delta z$ с ошибкой почти не меняется при усилении базы:
$+0.0330$ у OOF-пары против $+0.0321$ у смеси. Ансамбль эту информацию
не поглощает.

**Что отличает эту попытку от провалившегося TCN.** Четыре вещи, и все
проверяемые по отдельности: рекуррентное состояние вместо фиксированного
рецептивного поля; явная пауза $\Delta t$ между событиями вместо цепочки
нулей; четыре запроса к памяти по поведенческим факторам вместо одного
пулинга; обучение исправлять уже сильную модель вместо повторения её с нуля.
Голова поправки инициализирована нулями, поэтому на первом шаге прогноз
тождественно равен ансамблю и единственный способ уменьшить лосс — объяснить
его ошибку.

**Масштаб решает.** Локальный пилот (без претрейна, 64 токена, 4 эпохи) дал
$\mathrm{std}(\Delta z) = 0.041$ и потолок $+0.00010$ на якоре подбора —
то есть направление почти ортогонально остатку. Полная конфигурация дала
$0.123$ и $+0.00043$. Отрицательный результат пилота был результатом
недообучения, а не отсутствия сигнала.

---

## Смена критерия: маржинальный ресурс вместо сольного выигрыша

После того как комбинирование направлений стало точным, вопрос «даёт ли
кандидат $+0.0003$ сам по себе» перестал быть правильным. Правильный —
**сколько нового MSE он объясняет после проекции на уже найденные
направления**:

$$d^{\perp} = d - D(D^{\top}D)^{-1}D^{\top}d, \qquad
G = \frac{\operatorname{Cov}(e, d^{\perp})^{2}}{\operatorname{Var}(d^{\perp})}$$

Реализовано в `src/ecup/directions.py`. Через `marginal_gain` теперь проходит
каждый кандидат.

**Смена критерия сразу отменила одно из моих решений.** Годовой календарный
аналог был закрыт с вердиктом «$+0.0004$, ниже порога различимости». Ошибка
была не в замере, а в критерии: при работающей квадратичной комбинации важна
не величина, а ортогональность.

| Вариант эксперта | $\rho(e)$ | сольно | маржинально | ортогональная доля |
| :--- | ---: | ---: | ---: | ---: |
| только аналог год назад | $+0.007$ | $+0.00005$ | $+0.00004$ | 0.992 |
| $+$ индивидуальный рост | $+0.025$ | $+0.00052$ | $+0.00050$ | 0.996 |
| $+$ рост $+$ свежие 30 дней | $+0.028$ | $+0.00068$ | $+0.00066$ | 0.998 |

Работает не уровень год назад, а **разность** текущего темпа и темпа год
назад: простой аналог даёт $+0.00005$, с индивидуальным ростом — вдесятеро
больше. При переносе коэффициентов с якоря 348 на 378 остаётся $+0.00037$,
знаки всех трёх коэффициентов сохраняются.

Итоговые корреляции трёх найденных направлений:

| | $\rho$ |
| :--- | ---: |
| GRU-одиночная $\sim$ GRU-сиды | $+0.714$ |
| GRU-одиночная $\sim$ годовой аналог | $+0.083$ |
| GRU-сиды $\sim$ годовой аналог | $+0.040$ |

Два нейронных направления — по сути одно, найденное дважды. Независимых
механизма **два**: последовательностный сигнал и календарный аналог.

---

## Отрицательные результаты нейросетевой ветки

Все — при идентичном протоколе, менялась ровно одна вещь.

| Гипотеза | Δ фолд 0 | Δ фолд 1 | базовый прогон | Вывод |
| :--- | ---: | ---: | ---: | :--- |
| Токены покупочных циклов | $+0.00024$ | $+0.00044$ | $+0.00026 / +0.00088$ | хуже на обоих; $\alpha$ между фолдами разъезжается вдвое (0.354 против 0.604) там, где у базового совпадает до 3 % |
| Направленный лосс $-\mathrm{Cov}/\sqrt{\mathrm{Var}}$ | $-0.00000$ | $+0.00058$ | то же | математика верна — масштаб действительно выбрасывается, но из этого не следует, что оптимизировать надо именно эту величину |
| Усреднение направлений по чекпоинтам | $-0.00012$ | $+0.00032$ | $-0.00000 / +0.00058$ | предпосылка «ранние эпохи дают зашумлённую версию того же направления» неверна: они дают **другое, худшее** направление |
| Обучение 40 эпох вместо 12 | — | — | — | переносимый сигнал максимален на 4-й эпохе и монотонно умирает при падающем лоссе |

**Больше сидов — тоже нет, и это считается заранее.** Из корреляции
направлений между сидами $\rho = 0.87$ следует
$\mathrm{Var}(n)/\mathrm{Var}(s) = 0.149$, а выигрыш $\propto 1/(1 + 0.149/K)$:

| $K$ | 1 | 3 | 7 | $\infty$ |
| :--- | ---: | ---: | ---: | ---: |
| относительный выигрыш | 1.000 | 1.095 | 1.125 | 1.148 |

Переход с трёх сидов на семь даёт $+0.00003$, бесконечное число — $+0.00006$.
Ось исчерпана на трёх.

---

## Аудит устройства выборки

Прецедент был сильным: правило отбора популяции и тождество
`sample_submit` $=$ GMV предыдущего окна дали больше, чем весь последующий ML.
Проверка ещё нескольких гипотез того же класса:

| Проверка | Результат |
| :--- | :--- |
| `sample_submit` $=$ GMV окна 379…408 | подтверждено: max расхождение 0.0016, совпадений 100 % |
| правило отбора воспроизводит тестовую популяцию | 250 000 из 250 000 |
| `gmv` $=$ `gmv_search` $+$ `gmv_cat` | тождество, доля точных 1.000000 |
| данные за якорем 408 | отсутствуют, максимальный день ровно 408 |

Третья строка содержательна: **каналы исчерпывающие**, скрытого источника
GMV нет. Четвёртая закрывает подглядывание в будущее. Новых механизмов
не найдено — прайор на «третью структурную находку» снижен.

Побочно: валидационный якорь 378 — вторник, боевой 408 — четверг. В токенах
есть фаза дня недели, значит между валидацией и тестом небольшой ковариатный
сдвиг.

---

## Про масштаб цели

До лидера $0.00293$. До 1.63 нужно снять $0.0625$ MSE из $2.719$, то есть
**2.3 % остаточной дисперсии**. Одному ортогональному направлению для этого
нужна корреляция с остатком $\rho \approx 0.152$; у Gap-GRU измерено $0.033$,
у годового аналога $0.028$. Требуется механизм в 4.6 раза сильнее всего
найденного — складыванием поправок по $0.0003$ туда не дойти.

---

## Кросс-пользовательский retrieval и когортный перенос: оба отрицательны

Две попытки сменить условное пространство — с «функции от собственной
истории» на «что было у похожих».

### Когортная карта систематической ошибки

Идея опиралась на верное различение: отрицательная автокорреляция личных
остатков ($\rho = -0.010\ldots-0.018$) не запрещает устойчивому
$\mathbb{E}[r \mid S = s]$ существовать — шум отдельного пользователя при
усреднении когорты падает как $\sqrt{n_s}$.

Сетка 150 ячеек по $(z_0, \text{recency}, \text{дни с покупкой})$, усадка
$n_s/(n_s+\lambda)$, скользящая схема.

| обучение → оценка | $\rho(e, d)$ |
| :--- | ---: |
| 318 → 348 | $-0.0169$ |
| 348 → 378 | $+0.0123$ |

**Знак переворачивается между окнами.** Величина $C^2/D$ при этом
положительна на обоих фолдах, и без явной проверки знака это выглядело бы
как перенос $+0.0002$. Та же сигнатура, что у аффинной калибровки.

Отсюда правка в harness: `marginal_gain` обязан отдавать **знак** $C$,
а совпадение знака между фолдами — отдельное условие допуска, не выводимое
из величины ресурса.

### Retrieval по 2.4 млн исторических состояний

Память строится **без учителя-GBDT**: для любого среза $T$ с наблюдаемым
будущим сохраняется $(h_{u,T},\, Z^{30},\, C^{30},\, N^{\text{ord}})$. Это
снимает ограничение, из-за которого заглохли плотные остаточные срезы —
там на каждый срез требовался OOF-учитель, а он требует трёх обучающих
якорей, которых для ранних $T$ нет.

12 срезов, 2 391 957 состояний, whitening + PCA до 32, FAISS по косинусу,
жёсткий пуржинг $T + 30 \le A$, тот же `user_id` исключён (остаточная доля
0.0004).

| $k$ | $\rho(e)$ | маржинально |
| ---: | ---: | ---: |
| 32 | $+0.0019$ | $+0.00000$ |
| 128 | $+0.0094$ | $+0.00006$ |
| 512 | $+0.0131$ | $+0.00012$ |

**Рост сигнала с $k$ — диагноз, а не тренд.** Локальная структура ловилась
бы уже тридцатью двумя соседями; монотонный рост к пятистам означает
регрессию к всё более широкому среднему. Локальности в представлении нет.

Оговорка: эмбеддинг обучался под прогнозные головы, а не как метрика
близости. Отрицателен первый уровень; обучение представления специально
под retrieval (contrastive/metric loss) им не опровергнуто. Но прайор
на SVGP над этим же представлением снижается сильно: если 512 соседей
в 32-мерном whitened пространстве не находят локальности, гауссов процесс
будет дорогим способом получить тот же ноль.

---

## Massive supervised pretraining: представление улучшилось, сигнал — нет

Самый крупный эксперимент ветки. Семь запросов, 23 раздельные цели,
случайный срез на пользователя за эпоху, канальное разложение
Search/Catalog, цели перехода и времени до следующей покупки. Учителя-GBDT
нет вовсе: для любого $T$ с наблюдаемым будущим все цели считаются напрямую.
226 тысяч примеров за эпоху, 25 эпох на фолд.

### Метрика: отрицательно

| Фолд | новый энкодер | старый |
| ---: | ---: | ---: |
| 0 | $+0.00032$ | $+0.00026$ |
| 1 | $+0.00065$ | $+0.00088$ |
| $\alpha$ по фолдам | 0.388 / 0.559 (**44 %**) | 0.350 / 0.360 (3 %) |

Формально шлюз пройден, но согласованность $\alpha$ — главный аргумент
за исходный Gap-GRU — заметно хуже.

Решает маржинальный ресурс поверх базы, куда старое нейронаправление
уже входит:

| | значение |
| :--- | ---: |
| $\rho$ с ошибкой | $+0.0091$ (у старого $+0.033$) |
| корреляция со старым направлением | $+0.667$ |
| сольно | $+0.00007$ |
| **маржинально** | **$+0.00003$** |

Направление и слабее, и на две трети дублирует имеющееся. Те $+0.00065$ —
выигрыш над слабой базой (OOF-пара, 1.68112); поверх сильной остаётся
$+0.00007$.

### Геометрия представления: положительно

| $k$ | старый | новый |
| ---: | ---: | ---: |
| 32 | $+0.00000$ | $+0.00012$ |
| 128 | $+0.00006$ | $+0.00013$ |
| 512 | $+0.00012$ | $+0.00011$ |

Кривая развернулась: была монотонно растущей — то есть локальности нет,
работает только регрессия к широкому среднему, — стала плоской с максимумом
на умеренном $k$. При $k = 32$ новый энкодер даёт столько же, сколько старому
требовалось 512 соседей.

Меняется и форма кривой обучения этапа B: старый энкодер выходил на максимум
к четвёртой эпохе и монотонно умирал, новый растёт восемь эпох до более
высокого пика ($+0.00063$ против $+0.00055$).

### Поправка к замеру: сравнивались не те состояния

В диагностике retrieval «нового энкодера» загружался чекпоинт **после
этапа B**, а не после этапа A. То есть измеренная геометрия этап B пережила,
и вывод «геометрия улучшилась, а остаток ею не воспользовался — это
забывание» из данных не следовал.

Прямое сравнение двух состояний на одних пользователях одной процедурой:

| Линейная проба | после A | после B | изменение |
| :--- | ---: | ---: | ---: |
| $Z_{30}$ | 0.4585 | 0.4600 | $+0.0015$ |
| $C_{30}$ | 0.3679 | 0.3714 | $+0.0035$ |
| $Z^{\text{search}}$ | 0.4566 | 0.4562 | $-0.0004$ |
| $Z^{\text{cat}}$ | 0.1904 | **0.1103** | **$-0.0801$** |

Забывание есть, но **избирательное**: этап B слегка улучшает то, что нужно
для $z$, и выбрасывает канальную структуру каталога, которую остаточная
цель не вознаграждает.

При этом представление перестраивается почти полностью — медианный косинус
между состояниями A и B равен **0.0598**. Это объясняет провал претрейна
точнее: этап B разворачивает пространство почти ортогонально и оставляет
только предсказывающее $z$. Геометрия этапа A до финального направления
почти не доживает — не потому, что портится, а потому, что переписывается.

Следствие для регуляризации: сохранение вспомогательных голов на этапе B
имеет смысл не как лечение забывания полезного, а как **якорь,
ограничивающий разворот**.

### Вывод

Претрейн улучшил **организацию** информации, а не её количество: потолок
retrieval остался $\approx +0.00013$ при любом $k$. Локальность появилась,
но сигнала в окрестности столько же, сколько раньше извлекалось усреднением
по пятистам соседям.

Это снижает прайор сразу для нескольких отложенных веток. Ядерные методы,
metric learning и SVGP опирались на предпосылку, что нужна лучшая геометрия;
геометрия улучшена, а извлекаемое не выросло.

**Формулировать вывод надо узко.** Не доказано, что история пользователя
больше ничего не содержит. Доказано другое, более конкретное:

> из **текущей 300-дневной** событийной истории существенно больше сигнала
> получить не удалось даже после качественного улучшения представления.

Разница существенна, и её показывает сам же успех годового аналога: он
использовал дни 44…73, которых 300-дневное окно не видит вовсе, и дал
почти ортогональный вклад. Значит копать надо **за границей окна и в
известных свойствах будущего календаря**, а не глубже внутри Gap-GRU.

---

## Спектроскопия лагов: найдена не сезонность, а калибровка масштаба

Первый прогон спектра дал широкий отрицательный знак на всех лагах 30…270
и пики на 150…240. Соблазн был прочитать это как временную структуру.
Правильное прочтение другое, и оно проверяется прямо.

Если база слегка неверно зависит от масштаба пользователя, то есть
$z \approx \beta x$, а $\hat z_0 \approx \hat\beta x$ при $\hat\beta > \beta$,
то $e = (\beta - \hat\beta)x + \varepsilon$, и **любой** исторический прокси
масштаба даст $\operatorname{Cov}(e, A_L) < 0$. Один общий знак на всём
диапазоне — именно эта сигнатура.

Проверка: разложить вектор лагов на уровень $\frac1K\sum_L A_L$ и форму
$A_L - \text{уровень}$, базы на обоих якорях сделать одинаковыми
по конструкции.

| | $\alpha$ (348) | маржин | $\alpha$ (378) | маржин |
| :--- | ---: | ---: | ---: | ---: |
| **уровень** | $-0.0189$ | $+0.00030$ | $-0.0249$ | $+0.00056$ |
| форма, лаг 60 | $+0.0006$ | $+0.00000$ | $+0.0204$ | $+0.00029$ |
| форма, лаг 180 | $+0.0010$ | $+0.00000$ | $-0.0149$ | $+0.00016$ |
| контраст 150-240 vs 30-60 | $+0.0047$ | $+0.00002$ | $-0.0150$ | $+0.00020$ |

**Пики исчезли.** Форма не переносится: знаки прыгают почти на каждом лаге.
Уровень переносится и даёт устойчивую поправку.

Масштабная поправка не поглощается ничем из найденного:

| База | маржинально |
| :--- | ---: |
| слабая (OOF-пара) | $+0.00056$ |
| сильная, нейронаправление в базисе | $+0.00056$ |
| сильная, нейронаправление $+$ годовое | $+0.00056$ |

Корреляция с годовым направлением $+0.0098$, с нейронаправлением $+0.122$,
ортогональная доля $0.992$.

**Методический урок.** Первое прочтение спектра было неверным дважды:
я счёл широкий отрицательный знак указанием на календарную структуру и
сравнивал колонки, измеренные относительно РАЗНЫХ баз (на 378 с проекцией
на нейронаправление, на 348 без неё). Разделение на уровень и форму при
симметричных базах — обязательный шаг перед любым выводом из лагового скана.

---

## Почему умер v21: не база, а якорь

Первое объяснение провала масштабной поправки — «она исправляла дефект
валидационного суррогата, а настоящий ансамбль этого дефекта не имеет» —
звучало убедительно и оказалось **неверным**.

Проверка: индекс инвариантности к базе
$S = \operatorname{std}_j(\alpha_j)/|\operatorname{mean}_j \alpha_j|$,
где $\alpha_j$ считается на одном якоре относительно разных баз. Порог
калибруется не выдумкой, а на собственных контролях — двух положительных
и одном отрицательном.

| Направление | hurdle LGB | смесь | **direct** | смесь+direct | $S$ |
| :--- | ---: | ---: | ---: | ---: | ---: |
| нейро (контроль +) | $+0.3906$ | $+0.4377$ | $+0.4201$ | $+0.4289$ | 0.042 |
| годовой (контроль +) | $+0.6796$ | $+0.7225$ | $+0.9778$ | $+0.8501$ | 0.144 |
| масштаб (контроль −) | $-0.0226$ | $-0.0209$ | $-0.0207$ | $-0.0208$ | **0.037** |

В панель специально добавлена `direct`-регрессия — MSE прямо на $\log(1+y)$
без разложения на маржи, то есть модель с иной по построению зависимостью
от масштаба. Масштабное направление осталось **самым устойчивым из трёх**.
Гипотеза о зависимости от базы опровергнута.

Настоящая причина восстанавливается из параболы лидерборда:

| Якорь | $\alpha$ масштабного направления |
| ---: | ---: |
| 348 | $-0.0189$ |
| 378 | $-0.0209$ |
| **408 (боевой)** | $\approx -0.0026$ |

Знак сохранился, **величина упала на порядок**. Эффект существовал на двух
исторических якорях и практически исчез на боевом окне.

### Три гипотезы, две отброшены

**Зависимость от базы — нет.** Индекс $S = \operatorname{std}_j(\alpha_j)/
|\operatorname{mean}_j \alpha_j|$ по панели баз, включая `direct`-регрессию
с иной по построению зависимостью от масштаба: $S = 0.037$ у масштабного
направления против $0.042$ у нейро и $0.144$ у годового. Оно оказалось
**самым инвариантным из трёх**.

**Сдвиг популяции — почти нет.** $\alpha$ есть величина, взвешенная
популяцией якоря, а численность растёт 218 400 → 233 152 → 250 000.
Перевзвешивание популяции 378 под 408 через логистическую регрессию
по семи наблюдаемым осям (различимость 0.632) меняет $\alpha$
с $-0.02089$ на $-0.01889$ — 10 % при падении в восемь раз.

**Настоящая причина — эффекта как условной структуры не существует.**
По квинтилям исторического масштаба:

| Квинтиль | якорь 348 | якорь 378 | |
| ---: | ---: | ---: | :--- |
| 1 | $+0.05873$ | $-0.11983$ | **разный** |
| 2 | $+0.00749$ | $-0.02734$ | **разный** |
| 3 | $-0.06061$ | $-0.02936$ | совпал |
| 4 | $-0.00306$ | $-0.04473$ | совпал |
| 5 | $-0.01532$ | $+0.03892$ | **разный** |

Знаки расходятся в трёх стратах из пяти, и внутри одного якоря эффект
меняет знак между квинтилями.

**Главный методический вывод.** Совпадение знака агрегатного коэффициента
на двух якорях может возникнуть из **усреднения нестабильных
разнонаправленных подэффектов**. Именно это обмануло шлюз: агрегат дал
$-0.0189$ и $-0.0209$, выглядел устойчивым, а составляющие его страты
были несогласованы.

Отсюда третье условие допуска: **стабильность знака внутри страт**,
а не только у агрегата.

**И поправка к формулировке про механизм.** Механизм не гарантирует
переносимость — он даёт сильный априорный аргумент за неё. Годовой аналог
обещал $+0.00037$ на валидации и дал $+0.00028$ в вершине: перенос хороший,
но не тождественный. Верить нужно и статистике, и объяснению, а не
переходить от первого ко второму после неудачи.

Практическое следствие: для кандидата без физического механизма двух
якорей мало, нужен третий, а его у нас нет по построению — 408 и есть
цель. Такие направления либо отправляются как ставка, либо не отправляются
вовсе.

---

## Структурированные остаточные головы: точное разложение не помогает

Тождество $\mathbb{E}[z \mid x] = p\,m$ точное, поэтому ошибку базы можно
факторизовать строго, а не эвристически. Две головы с нулевой
инициализацией в offset-параметризации от честных OOF-величин:

$$\operatorname{logit} p = \operatorname{logit} p_0 + \delta_p, \qquad
m = m_0 + \delta_m$$

Гипотеза: одна свободная голова получает смесь двух разных ошибок в одном
шумном числе; раздельные градиенты должны дать более структурированное
обучение.

| Фолд | `residual=p` | `residual=m` | `residual=pm` | свободная |
| ---: | ---: | ---: | ---: | ---: |
| 0 | $+0.00024$ | $+0.00033$ | $+0.00039$ | $+0.00026$ |
| 1 | $+0.00074$ | $+0.00063$ | $+0.00057$ | $+0.00088$ |
| $\alpha$ по фолдам | 0.558 / 0.274 | 0.594 / **1.464** | 0.419 / 0.312 | **0.350 / 0.360** |

Ни один вариант не превзошёл свободную голову на фолде 1, и **все три хуже
по согласованности $\alpha$**: расхождение вдвое у `p`, в 2.5 раза у `m`
против 3 % у свободной. Механизм правдоподобен — ограниченная голова видит
половину ошибки и компенсирует масштабом, а величина компенсации зависит
от якоря.

Разложение $d = (p-p_0)m_0 + p_0(m-m_0) + (p-p_0)(m-m_0)$, тождество
проверено до $7.8\cdot10^{-7}$:

| Компонента | std | $\rho(e)$ | маржинально |
| :--- | ---: | ---: | ---: |
| $d_p$ | 0.0843 | $+0.0074$ | $+0.00002$ |
| $d_m$ | 0.0839 | $+0.0123$ | $+0.00009$ |
| $d_\times$ | 0.0058 | $-0.0040$ | $+0.00001$ |
| целиком | 0.1453 | $+0.0113$ | $+0.00007$ |

**Вопрос «сигнал в экстенсивной или интенсивной марже» поставлен неверно.**
Компоненты практически равны по величине и скоррелированы между собой
на $+0.52$: это не две оси ошибки, а одна, проходящая через обе части
склейки. Перекрёстный член пренебрежим (4 % величины).

Точность математического разложения не переводится в пользу для остаточного
обучения — свободная голова находит ту же ось и делает это устойчивее.

---

## Селективная дистилляция: переносимость починена, нового направления нет

Последняя правдоподобная версия ветки: «полезная информация в предобученном
энкодере есть, но этап B её не удерживает».

Постановка исправлена по существу. Прямая привязка $\|h_B - h_A\|^2$
запрещала бы полезный поворот базиса, а он безвреден: **линейный
CKA(A, B) = 0.7376** при контролях 1.0000 на случайном повороте и 0.0004
на шуме. Косинус 0.0598 в исходных координатах, на который я ссылался
раньше, был почти целиком поворотом — метрика мерилась в неинвариантных
координатах.

Поэтому дистиллируется **функция**, и только та, что действительно просела:
каталожная голова ($-0.0801$ по линейной пробе, тогда как $Z_{30}$ и
$C_{30}$ после этапа B стали лучше). $\lambda$ калибруется отношением норм
градиентов по медиане двенадцати батчей — разброс отношения оказался
четырёхкратным (0.1…0.4), так что одной точки было бы мало.

**Первый критерий пройден:**

| | $\alpha_0 / \alpha_1$ | расхождение | фолд 0 | фолд 1 |
| :--- | ---: | ---: | ---: | ---: |
| свободная голова | 0.350 / 0.360 | 3 % | $+0.00026$ | $+0.00088$ |
| **дистилляция** | **0.425 / 0.359** | **17 %** | $+0.00029$ | $+0.00081$ |
| претрейн без якоря | 0.388 / 0.559 | 44 % | $+0.00032$ | $+0.00065$ |

Формально порог **не пройден**: заранее было задано
$\delta_\alpha \le 0.15$, получено $0.17$. Результат хорош относительно
44 % без якоря, но заданный порог есть заданный порог.

И причинность не доказана: из-за OOM батч менялся с 2048 на 1024, поэтому
часть сдвига $0.388/0.559 \to 0.425/0.359$ может идти от другой траектории
оптимизации, а не от дистилляции. Корректная формулировка — **селективная
дистилляция совместима с улучшением переносимости**, но причина не
установлена.

**Второй критерий не пройден:** маржинальный ресурс $+0.00009$ при пороге
$2\cdot10^{-4}$, корреляция со старым направлением $+0.704$ при пороге $0.5$.

Именно эта часть закрывает ветку надёжно, и для неё причинность первой
доказывать не требуется: даже улучшенный кандидат **в значительной степени
дублирует существующий сигнал** ($\rho = 0.704$) и оставляет недостаточный
независимый остаток.

### Линия вокруг текущего энкодера закрыта

Четыре независимых захода упёрлись в один потолок:

| Заход | Результат |
| :--- | :--- |
| массивный претрейн, 23 цели | геометрия лучше, маржинально $+0.00003$ |
| структурированные головы $p$, $m$, $pm$ | нового направления нет, $\alpha$ хуже |
| retrieval по 2.4 млн состояний | потолок $\approx +0.00013$ при любом $k$ |
| селективная дистилляция | переносимость починена, маржинально $+0.00009$ |

Утверждение узкое и потому сильное: **из текущей 300-дневной событийной
истории больше сигнала извлечь не удалось ни улучшением представления,
ни изменением параметризации остатка, ни непараметрическим поиском
по популяции, ни защитой представления от перезаписи.**

Дальнейшая работа имеет смысл только с новыми компонентами $X$.

---

## Годовой согласованный фильтр: что на самом деле ловил YoY

Работающий годовой эксперт использован как зонд структуры данных. Три
гипотезы, разделённые заранее заданными тестами.

**$H_1$, локальная особенность ровно на 365 днях — отвергнута.** Окна
$G_{365}$ и $G_{364}$ совпадают на 29 днях из 30, их разность есть
$g_{14} - g_{44}$, то есть краевой шум. Контраст
$C_{365} = X_{365} - (\frac67 X_{364} + \frac17 X_{371})$ даёт
$\rho(e) = -0.0007$ и нулевой маржинальный ресурс. Профиль по лагам
350…378 плоский.

**$H_3$, прокси медленного уровня — отвергнута, и способом неожиданным.**
Проекция $X_{365}$ на долгосрочный уровень (четыре неперекрывающихся
старых блока) должна была убить сигнал. Вместо этого ковариация выросла
**втрое**: $0.0272 \to 0.0829$ при падении дисперсии всего до 0.64.
Это supressor-эффект — уровень активно **скрывал** временной контраст,
а не порождал его.

**$H_2$, широкая годовая сезонность — сильно поддержана.** Профиль
ковариаций по одиннадцати одинаково очищенным блокам
$C_j = \frac{K}{K-1}(B_j - \bar B)$ подогнан заранее заданным шаблоном
$\cos(2\pi(t_j - t_*)/365)$ с фазой, жёстко привязанной к целевому периоду:

| Шаблон | corr | $R^2$ |
| :--- | ---: | ---: |
| **годовая гармоника** | $+0.9912$ | **0.9824** |
| квадратичный | $+0.7778$ | 0.6049 |
| линейный тренд | $-0.5447$ | 0.2967 |

Гладкая смена знака профиля, которую легко принять за форму траектории,
есть в точности то, что предсказывает широкая годовая гармоника.

### Согласованный фильтр

$$d_{\text{annual}}(u) = \sum_j w_j\,(B_{uj} - \bar B_u), \qquad
w_j = \cos\!\left(\frac{2\pi(t_j - t_*)}{365}\right)$$

Ни одного обучаемого коэффициента, фаза не подгоняется. Сначала явно
убирается уровень пользователя, затем оставшаяся форма проецируется
на годовую гармонику.

| Направление | сольно | маржинально сверх $[d_{16}, d_{\text{yoy}}]$ | $\rho$ с YoY |
| :--- | ---: | ---: | ---: |
| **годовая гармоника** | $+0.00188$ | **$+0.00141$** | $+0.387$ |
| квадратичный | $+0.00131$ | $+0.00105$ | $+0.232$ |
| линейный | $+0.00045$ | $+0.00012$ | $-0.503$ |

Маржинальный ресурс вдвое больше того, что дал сам годовой аналог при
первом появлении, и в семь раз выше порога.

**Оговорка.** Квадратичный контроль даёт $+0.00105$ — не пренебрежимо.
На одном годе данных одиннадцать блоков покрывают ровно один цикл,
поэтому годовая гармоника и общая низкочастотная кривизна не разделимы
в принципе. Гармоника подходит лучше, но $H_2$ против $H_4$ не закрыт.
Линейный тренд при этом почти пуст — дело не в монотонном росте активности.

**Что старый эксперт делал неявно.** Член $R - R_l$ частично снимал
уровень, но косвенно и не полностью. Новый признак использует всю годовую
траекторию и отделяет уровень от формы явно.

### Результат на лидерборде

$1.6487893 \to \mathbf{1.6479970}$, прирост $+0.00079$ — крупнейший
с момента появления Gap-GRU. Сам годовой аналог дал на лидерборде
$+0.00028$ (v18 → v20), то есть фильтр больше в **2.85 раза**. Отношение
«вчетверо» относится к валидационным ресурсам ($0.00141$ против $0.00037$),
но не к лидерборду.

**Консерватизм окупился численно.** Коэффициент был взят как $0.6$ от
измеренного, потому что второго честного якоря для годового признака
не существует. Парабола по двум точкам лидерборда даёт вершину при
$0.695$ от измеренного:

| $a$ (в единицах применённого) | 0.00 | **1.00** | 1.16 | 1.50 | 2.00 |
| :--- | ---: | ---: | ---: | ---: | ---: |
| RMSLE | 1.6487893 | **1.6479970** | 1.6479818 | 1.6480518 | 1.6484073 |

Полный измеренный коэффициент соответствует $a = 1.67$ и дал бы примерно
$1.64815$ — то есть осторожность принесла **$+0.00015$**. Вершина добавляет
лишь $+0.0000152$, пересылать нечего.

Это третий случай подряд, когда внутривыборочный коэффициент оказывался
завышенным: v19 промахнулся вдвое, v21 — в восемь раз, здесь — в полтора.
Для направлений с единственным валидационным якорем занижение
коэффициента стало правилом, а не перестраховкой.

---

## Сводка сессии

| Сабмит | RMSLE | Что добавлено |
| :--- | ---: | :--- |
| v10 | 1.6508203 | ансамбль LightGBM |
| v15 | 1.6502841 | CatBoost как второе семейство |
| v16 | 1.6493404 | Gap-GRU на остатке |
| v17 | 1.6491770 | ансамбль трёх нейросетевых сидов |
| v18 | 1.6490671 | аналитическая комбинация двух направлений |
| v20 | 1.6487893 | годовой аналог, масштаб из параболы |
| **v22** | **1.6479970** | **годовой согласованный фильтр** |
| лидер | 1.6458638 | отставание $0.00213$ |

Суммарно $+0.00282$ за сессию.

**Что сработало.** Четыре направления: второе семейство бустинга, остаточная
нейросеть, календарная сезонность и её согласованный фильтр. Плюс приём, резко сокративший
число замеров: ошибка квадратична вдоль любой прямой в $z$-шкале, а матрица
Грама считается прямо из csv, поэтому измерений нужно ровно столько, сколько
направлений.

**Формулировать это надо точно.** Коэффициенты комбинируются аналитически
в $z$-пространстве, и сеточного перебора нет ни разу. Но публичный
лидерборд при этом **используется как метка** — параболы v19/v20 и v22
восстанавливают именно public-optimal коэффициент. Это использование
разметки, пусть и в предельно низкой размерности.

**Что не сработало.** Циклы покупок, направленный лосс, усреднение
чекпоинтов, лишние сиды, массивный претрейн, структурированные головы
$p/m$, retrieval по 2.4 млн состояний, когортный перенос, селективная
дистилляция, масштабная поправка, плотные срезы, market-state.

**Главный методический вывод.** Критерий отбора эволюционировал трижды.
Сначала величина выигрыша — обманула на когортной карте, где агрегатный
знак совпал при разнонаправленных стратах. Потом ортогональность — обманула
на масштабной поправке, ортогональной на 0.99 и умершей на боевом окне.
Работающий критерий требует трёх свойств сразу: **стабильность знака
$\alpha$ между фолдами, маржинальный ресурс после проекции на найденные
направления, и механизм, объясняющий повторяемость.** Годовой фильтр проходит
маржинальный ресурс и механизм; **стабильность $\alpha$ у него не проверена
и частично не выполняется** (см. ниже). Ни одно из закрытых направлений
не проходило больше двух свойств.

### Разделение годовой гармоники и квадратичной кривизны

Шаблоны сами по себе коррелированы на $+0.80$, поэтому сравнивать их
ресурсы по отдельности нельзя. Тест Фриша–Во–Ловелла:

| | сверх базиса | сверх базиса $+$ конкурент | осталось |
| :--- | ---: | ---: | ---: |
| **годовая гармоника** | $+0.00141$ | $+0.00041$ | **29 %** |
| квадратичная | $+0.00105$ | $+0.00005$ | 4 % |

Годовая поглощает 96 % ресурса квадратичной, обратно — 71 %. Уникальный
вклад годовой фазы сверх общего изгиба составляет $+0.00041$; остальные
71 % — общая низкочастотная структура, которую оба шаблона ловят по-разному.

Побочно подтверждено, что $\sum_j (w_j - \bar w) = 0$ делает явное вычитание
среднего блока избыточным (расхождение $6.7\cdot10^{-7}$): фильтр по
построению уничтожает постоянный пользовательский уровень.

### Временной перенос: знак да, величина нет

Одиннадцатиблочную версию на якоре 348 не построить — левая граница данных.
Блоки $j = 2\ldots10$ существуют одновременно на 318, 348, 378 и 408,
поэтому конструкция буквально одна и та же. База симметрична (OOF-пара).

| Якорь | $\alpha$ | $\beta = \mathrm{Cov}/\mathrm{std}$ | ресурс |
| ---: | ---: | ---: | ---: |
| 318 | $+0.00591$ | $+0.01665$ | $+0.00008$ |
| 348 | $+0.00509$ | $+0.01426$ | $+0.00006$ |
| **378** | $+0.01581$ | $+0.04436$ | $+0.00059$ |

Знак совпадает на всех трёх якорях. **Величина не переносится:** разброс
$\beta$ равен $0.544$ при пороге $0.2$, на 378 эффект втрое сильнее.

Два прочтения, неразделимые наличными данными: либо сигнал действительно
усиливается к февралю–марту, либо $+0.00059$ на 378 частично отражает то,
что гипотеза сформулирована после просмотра этого якоря. **Лидерборд
говорит в пользу первого:** v22 перенёсся на окно 409–438, календарно
близкое к 378. Будь эффект артефактом выбора гипотезы, на 408 он бы
не воспроизвёлся.

Практический вывод: направление реально, но **привязано к календарной
позиции**, а не универсально по времени. Это согласуется с самой гипотезой
$H_2$ — годовая гармоника с фиксированной фазой и обязана работать
по-разному в разных точках года.

**И про shrinkage.** Три случая ($s \approx 0.5$, $0.12$, $0.70$) не
образуют закона. В v21 направление само оказалось нестабильной агрегатной
регулярностью — это другой режим отказа. Для содержательных одноякорных
направлений $s < 1$ остаётся консервативным априорным выбором, а не
измеренной величиной.

### Факторизация годового фильтра по величинам: закрыта

Форма зафиксирована, меняется только наблюдаемая величина. Ступень 1
(общая опора 318/348/378) пройдена пятью из шести; ступень 2 — поверх
базиса с уже отправленным годовым фильтром по GMV:

| Величина | маржинально | $\rho$ с annual-GMV |
| :--- | ---: | ---: |
| orders | $+0.00007$ | $+0.890$ |
| buydays | $+0.00005$ | $+0.896$ |
| searches | $+0.00001$ | $+0.500$ |
| gmv_search | $+0.00001$ | $+0.972$ |

Тест Фриша–Во–Ловелла между GMV и orders: $+0.00003$ и $+0.00007$,
производная величина «сезонный размер корзины» $d_{\text{gmv}} -
\gamma d_{\text{ord}}$ — $+0.00003$. Разделения нет.

Хорошо описывается латентной моделью $d_q = \lambda_q s + \varepsilon_q$:
одна скрытая годовая компонента, измеренная разными агрегатами. `searches`
с $\rho = 0.50$ независим в основном своей шумовой частью.

**Вывод узкий:** среди проверенных агрегатов годовой предиктивный сигнал
практически одномерен. Расширять эту ось по семантике нечем; возможное
улучшение — только снижение ошибки измерения самого годового фактора.

Оговорка: в `existing` входили $[d_{16}, d_{\text{yoy}}, d_{\text{annual}}]$,
а не буквально полный боевой базис. На вывод это не влияет — добавление
столбцов может только сильнее съесть и без того микроскопические величины.

---

## День 3: восемь закрытых веток и два стратегических сброса

### v23: девятое применение параболы, точность $1.5\cdot10^{-7}$

Вершина параболы по двум точкам ($a^{*} = 1.159$ в единицах применённой
поправки) предсказывала $1.6479818$, замер дал $\mathbf{1.6479820}$.
Расхождение $1.5\cdot10^{-7}$ — лучшая точность за все применения приёма.

### Закрытые ветки

| Ветка | маржинально | знак между фолдами |
| :--- | ---: | :--- |
| длинная история в GBDT | $+0.00004$ | совпал |
| годовой признак внутри модели | $+0.00001$ | — |
| пользовательская модуляция поправки | $+0.00001$ | **разный** |
| блочный DCT-Ridge | $+0.00010$ | совпал |
| momentum-контрасты | $+0.00003$ | **разный** |
| дискретные режимы активности | $+0.00000$ | **разный** |
| плотная нарезка как направление | $+0.00006$ | — |
| многомасштабный трансформер | $+0.00001$ | **разный** |

### Дискретные режимы: нестационарен сам эффект

Восемь состояний по бинарному паттерну покупок в трёх последних блоках.
Средний остаток каждого состояния **меняет знак между якорями** при
десятках тысяч пользователей в ячейке:

| Состояние | $n$ | $\mu_{318}$ | $\mu_{348}$ | $\mu_{378}$ |
| :--- | ---: | ---: | ---: | ---: |
| 000 нет покупок | 40 679 | $-0.036$ | $+0.031$ | $+0.060$ |
| 011 недавно начал | 17 509 | $+0.054$ | $-0.018$ | $-0.068$ |
| 111 стабильно активен | 79 247 | $+0.026$ | $-0.034$ | $-0.015$ |

Это не шум оценки. Отображение «режим пользователя → систематическая
ошибка» **само нестационарно**, поэтому lookup не переносится. Дробление
здесь не помогло бы: восемь грубых состояний ведут себя так же, как
сто пятьдесят в когортной карте.

### Собственный остаток пользователя не воспроизводится

| Пара якорей | сырой | после годовой поправки |
| :--- | ---: | ---: |
| 318~348 | $-0.0154$ | $-0.0158$ |
| 348~378 | $-0.0172$ | $-0.0176$ |
| 318~378 | $+0.0040$ | $-0.0020$ |

Смежные окна дают устойчивую **антиперсистентность** около $-0.016$,
дальние — ноль.

**Формулировать это надо узко.** Закрыта гипотеза о ПЕРЕНОСЕ прошлой ошибки
пользователя: $e_{t+1}(u) \approx \lambda e_t(u)$ не работает. Но из
$\mathrm{Corr}(e_t, e_{t+1}) \approx 0$ **не следует** $E[e_{t+1} \mid x_t] = 0$ —
это разные утверждения. Retrieval как оценка $E[e \mid x]$ по текущему
состоянию математически не закрыт; он имеет низкий приоритет по прежним
замерам (потолок $\approx +0.00013$ при любом $k$), а не по этому.

### Карта закрытых веток на конец дня 3

| Ветка | Статус |
| :--- | :--- |
| перенос личного остатка во времени | **закрыта** измерением |
| плотный бэкбон | **закрыта** парным контролем |
| непрерывная и дискретная временная форма остатка | практически закрыта, восемь тестов |
| годовая ось (частота, величина, модуляция) | практически исчерпана |
| PatchFormer | провалил перенос, остался один тест недообученности |
| retrieval по $E[e \mid x]$, другие семейства, глобальное состояние | низкий приоритет, но **не** доказанно пусты |

### Плотный ансамбль: плотность против разнообразия

Ключ — **парный контроль при равном числе моделей**. Без него отрицательный
результат был бы нечитаем: неизвестно, проиграла плотность или размер
ансамбля.

| Бэкбон | без поправок | с поправками v22 |
| :--- | ---: | ---: |
| плотный 3+3 | 1.68088 | 1.67847 |
| **разреженный 3+3** | 1.68082 | **1.67830** |
| полный v22 (30 моделей) | 1.67988 | 1.67753 |

Одиночные плотные LightGBM **лучше** разреженных на $+0.00023$
(1.68132/1.68172/1.68136 против 1.68185/1.68165/1.68158), а ансамбль из них
**хуже**. Механизм: модели на перекрывающихся окнах сильнее скоррелированы,
и усреднение выигрывает меньше. Плотность улучшает одиночную модель
и ухудшает разнообразие, которое стоит дороже.

У CatBoost плотность не помогает и одиночным моделям (1.68142/1.68143/1.68139
против 1.68138) — упорядоченный бустинг уже устойчив к зависимым строкам.

Смесь плотного бэкбона с полным v22 даёт минимум $1.67747$ при
$\lambda = 0.2$ против $1.67753$ — $+0.00006$ на семи точках подгонки
одного якоря.

### Многомасштабный трансформер

770 тысяч параметров, два потока (26 недельных патчей и 12 месячных),
календарная фаза от центра целевого окна, явное разложение level/shape,
запросы смещены прогнозом деревьев, голова поправки с нулевой
инициализацией. Три решения выведены из измерений: supressor-эффект
уровня, провал блочного Ridge на уровневых признаках, работающая фаза
годового фильтра.

| Фолд | $\alpha$ | сольно | маржинально |
| ---: | ---: | ---: | ---: |
| 0 → 348 | $-0.610$ | $+0.00031$ | $+0.00026$ |
| 1 → 378 | $+0.103$ | $+0.00017$ | $+0.00001$ |

Знак не переносится. Лосс вышел на плато к десятой эпохе из двадцати,
так что недообученность маловероятна, но полностью не исключена.

## TabM как третье семейство — закрыто, и вместе с ним пересмотр шлюза

Прямая регрессия `z = log(1+y)` сетью TabM (BatchEnsemble, 776 481 параметр,
32 члена) на тех же 183 признаках. Два честных временных фолда.

### Первый заход дал ложный проход

| | фолд 348 | фолд 378 |
|---|---|---|
| TabM standalone | 1.79321 | 1.73447 |
| база | 1.75169 | 1.67905 |
| rho(ошибок) | +0.9724 | +0.9595 |
| alpha | +0.0792 | +0.0914 |
| маржинально | +0.00031 | +0.00058 |

Знак alpha совпал, обе величины выше порога 2e-4 — шлюз пройден.
Аналитический оптимум подтверждал согласованность геометрии:
lam* = (s_b^2 - rho·s_b·s_T) / (s_b^2 + s_T^2 - 2·rho·s_b·s_T) давал
0.080 и 0.104 против измеренных 0.0792 и 0.0914.

### Ошибка в цели

Вычиталось `l_plus` — условное среднее по покупавшим, — вместо
безусловного `l`. На якоре 198 это 4.335 против 2.556, сдвиг 1.779.
У непокупателя цель становилась -4.335 вместо нуля, а при прогнозе
прибавлялось `l`, то есть другая величина. `DirectGBDT` вычитает
безусловный уровень; `l_plus` нужен голове регрессии hurdle, которая
учится только на y>0.

Веса якорей и стандартизация признаков были на месте изначально.

### После исправления направление умирает

| | фолд 348 | фолд 378 |
|---|---|---|
| TabM standalone | 1.75351 | 1.68365 |
| база (точная v22) | 1.75169 | 1.67753 |
| rho(ошибок) | +0.9975 | +0.9968 |
| lam* сырой | +0.2932 | **-0.0596** |
| маржинально | +0.00038 | **+0.00003** |

Качество выросло на 0.040 и 0.051 и встало вровень с `DirectGBDT`
(1.68365 против 1.68295) — диагноз подтверждён. Но 1-rho упало
с 0.0405 до 0.0032, в 12.7 раза, сырой оптимальный вес стал
отрицательным. Обмен «качество против независимости» съел эффект
целиком.

### Механизм ложного прохода

Разбиение Cov(e, d⊥) по группам (точное, модельно-независимое):

| направление | Cov(e,d⊥) | вклад y=0 | вклад y>0 | G |
|---|---|---|---|---|
| v1 (баг) | 0.02117 | +0.04311 | -0.02194 | 0.00194 |
| v2 (фикс) | 0.00121 | +0.00430 | -0.00309 | 0.00010 |

Весь ресурс создавался на непокупателях. База не предсказывает
точный ноль никогда (тождество E[z|x] = p·m > 0), выдавая на
непокупателях в среднем 1.4719 при 43.81 % нулей в истине.
Смещённая цель превращала TabM в оператор зануления: 37.72 %
точных нулей, аффинная аппроксимация d ≈ -0.872 - 0.212·base
с R² = 0.48 — то есть усадка к среднему.

### Усадка проверена отдельно и отвергнута

Ранговый сплайн delta(rank(base)), 5 параметров, применяется
level-free. Между половинами пользователей на 378 даёт +0.00063,
воспроизводя весь ресурс v1 без сети. Но перенос ВПЕРЁД НА ОДИН
ЯКОРЬ на общей конструкции базы (OOF-пара p0·m0):

| обучено → оценено | база | k=1 | k=2 | k=4 | k=8 |
|---|---|---|---|---|---|
| 258 → 288 | 1.74190 | +0.00002 | -0.00055 | -0.00061 | -0.00065 |
| 288 → 318 | 1.74096 | +0.00017 | +0.00013 | +0.00011 | +0.00010 |
| 318 → 348 | 1.75169 | -0.00083 | -0.00083 | -0.00087 | -0.00088 |
| 348 → 378 | 1.68115 | +0.00051 | +0.00050 | +0.00045 | +0.00041 |

Среднее при k=4 равно -0.00023, знаки «- + - +». Пара 348→378 —
один из четырёх переходов и как раз благоприятный; соседний
318→348 даёт ту же величину с обратным знаком.

### Внутренний ансамбль не работает

K-кривая на 378 (среднее по случайным подмножествам членов):
1.68498 / 1.68438 / 1.68398 / 1.68380 / 1.68370 / 1.68365 при
K = 1, 2, 4, 8, 16, 32. Весь ансамбль из 32 членов даёт +0.00133,
шаг 16→32 даёт +0.00005. Адаптеры r_k, s_k схлопываются
(разброс членов 0.375 при инициализации, 0.059 после обучения) —
это фактически один MLP с дорогой обёрткой. Читать это как
«внутренний ансамбль исчерпал сидовое разнообразие» нельзя:
он его не создал.

### Что это меняет в методологии

Двухфолдовый шлюз (совпадение знака alpha на 348 и 378 плюс порог
2e-4) пропустил направление, которое четырёхфолдовый перенос
отвергает. Оба условия шлюза выполнялись. Пара (348, 378)
оказалась коррелированной по эффекту усадки. Для направлений,
действующих через калибровку прогноза, а не через новый признак,
двух фолдов недостаточно.

### Лидерборд разрешил спор: знак alpha был неверен

Направление TabM-v1 отправлено на боевое окно при lam = 0.05
(демеанированное, как d16 и dann). D = E[d²] = 0.385926 посчитано
по ВСЕМ 250 000 тестовых пользователей. Public score считается
на скрытой подвыборке, поэтому замер даёт не C_pub, а
  Ĉ = C_pub + (lam/2)·(D_all - D_pub),
то есть C восстановлен с поправкой от неизвестного D_pub, а не точно.
Поправка пренебрежима: чтобы истинное C_pub оказалось хотя бы нулём,
при lam = 0.05 потребовалось бы D_pub = 0.9473 против D_all = 0.386,
то есть public-подвыборка со средним d² в 2.45 раза выше. Вывод о
знаке от этого не зависит.

v24_tabm005 = 1.6487003817 против v23 = 1.6479820, то есть +0.00072.
Это хуже даже сценария C = 0, для которого предсказывалось +0.00029.

Разворот: C = (MSE0 + lam²D - MSE(lam)) / (2·lam) = -0.014035,
откуда alpha = C/D = -0.0364.

| | alpha |
|---|---|
| фолд 348 | +0.0792 |
| фолд 378 | +0.0914 |
| public leaderboard | **-0.0364** |

Public leaderboard — не «истина», а ещё одна выборка примерно из
50 000 пользователей; private может отличаться. Обозначать эту
величину следует alpha_public. Здесь это не спасает TabM: даже
доступный ресурс при оптимальном отрицательном коэффициенте
составляет около 1.5e-4 RMSLE.

Двухфолдовый шлюз ошибся не в величине, а в ЗНАКЕ. Четырёхфолдовый
тест переноса усадки предсказал это верно: знаки «- + - +», среднее
-0.00023.

Полный ресурс направления при оптимальном отрицательном весе
составляет C²/D = 0.00051 в MSE, то есть +0.000155 RMSLE, при
SE(C) около 0.0045 на 50 000 публичных пользователей. Встречный
сабмит не оправдан.

### Протокол отбора после TabM

Старый шлюз спрашивал «существует ли хороший oracle-lambda на каждом
периоде». Нужен другой вопрос: «переносится ли коэффициент, который
мы могли знать ДО этого периода». Поэтому на каждом периоде хранятся
C_j = E[e_j·d_j], D_j = E[d_j²], alpha_j = C_j/D_j, но решение
принимается по реальному результату одного заранее выбранного
коэффициента, dM_j(lam) = 2·lam·C_j - lam²·D_j.

1. Пост-калибровки и функции уже имеющегося прогноза — минимум
   четыре walk-forward периода, коэффициент заморожен по предыдущим.
2. Новое семейство моделей — четыре walk-forward оценки качества и
   blend с текущим backbone. Совпадение alpha не требуется; нужны
   положительный совокупный blend и отсутствие катастрофического
   отдельного периода.
3. Новый признак X — одна фиксированная модель и четыре временные
   абляции base против base + X. Смена learner одновременно с
   признаками запрещена.
4. Календарные механизмы — phase-aware проверка. Требовать
   одинакового знака на четырёх произвольных месяцах от годового
   эффекта математически неверно.
5. Public leaderboard — дополнительная независимая проверка знака,
   но не источник «истинного» коэффициента, тем более что v23 уже
   частично настроен по public-параболам.

Четыре последовательных перехода — не универсальный шлюз: они тоже
не независимы, пользователи и обучающие интервалы пересекаются.

### Вывод для методологии

Совпадение знака alpha на якорях 348 и 378 плюс порог 2e-4 —
недостаточное условие. Эти два якоря соседние и делят шесть из
семи обучающих якорей, поэтому по эффектам, действующим через
калибровку прогноза, они почти не независимы. Для таких
направлений нужен перенос минимум на четырёх переходах, и
решающим следует считать средний знак по ним, а не совпадение
на паре.

Прогонять заново все 23 отрицательные ветки не нужно. Большинство
закрыто по более сильным причинам: ресурс порядка 1e-5, сильная
избыточность относительно существующих направлений, прямой sign
flip или явный провал механизма. Пересмотра заслуживают только
пограничные кандидаты с содержательным механизмом.

Все компоненты, реально вошедшие в v23, подтверждены не только
двухфолдовым шлюзом, но и public leaderboard: CatBoost +0.00054,
Gap-GRU +0.00094, годовой фильтр +0.00079. Основу не пересобираем;
новые кандидаты заходят только как доказанно независимый компонент.

## XGBoost третьим семейством — потолок 1.3e-4

Walk-forward на 288/318/348/378, один сид, без HPO, те же 183
признака и та же схема отбора пользователей. Коэффициент смеси
оценивается ТОЛЬКО по первым трём якорям и без изменений
применяется к 378.

| якорь | база | direct-XGB | rho | C | D | alpha |
|---|---|---|---|---|---|---|
| 288 | 1.74190 | 1.74730 | +0.9947 | +0.00675 | 0.03237 | +0.2087 |
| 318 | 1.74096 | 1.74484 | +0.9959 | +0.00576 | 0.02506 | +0.2297 |
| 348 | 1.75169 | 1.75383 | +0.9977 | +0.00321 | 0.01392 | +0.2303 |
| 378 | 1.68115 | 1.68336 | +0.9979 | +0.00236 | 0.01214 | +0.1942 |

lam по 288/318/348 равен +0.22027, перенос на 378 даёт +0.00013
при оракуле +0.00014, то есть 93 % доступного. Это ровно то, чего
не хватило TabM, где замороженный коэффициент прилетел с обратным
знаком. Hurdle-XGB слабее: +0.00001 при оракуле +0.00005.

Три сида НЕ помогают: перенос +0.00013 / +0.00010 / +0.00008. На
обучающих якорях усреднение отрабатывает как предсказано (C растёт,
D падает, alpha поднимается с 0.21 до 0.36), но на 378 alpha стоит
(0.194 -> 0.208). Замороженный lam уезжает до 0.306 против истинного
0.208, перелёт 47 %. Теряется не ресурс, а калибровка веса:
D(2·lam·alpha - lam²) = 0.00846·(0.12711 - 0.09364) = 2.83e-4 MSE.

Ключевое здесь — дрейф alpha между якорями измерен количественно.

## Сходимость аппроксиматоров

| кандидат | standalone на 378 | rho с базой |
|---|---|---|
| DirectGBDT | 1.68295 | — |
| direct-XGB, 1 сид | 1.68336 | +0.9979 |
| TabM после исправления | 1.68365 | +0.9968 |
| direct-XGB, 3 сида | 1.68262 | — |

Три реализации бустинга и нейросеть приходят в диапазон
1.6826…1.6837 при корреляции остатков выше 0.996. Похоже,
E[z|X] по текущим 183 признакам практически исчерпан, и следующая
ставка должна быть в X, а не в f.

## Две новые семьи признаков — обе отрицательны

Протокол: одна фиксированная модель (LightGBM hurdle, сид 42),
четыре якоря, семьи проверяются раздельно, гиперпараметры не
трогаются. Признаки считаются по каждому якорю свои, включая
обучающие: признак на якоре a видит только дни < a. Тренд в
глобальном шоке ПРИЧИННЫЙ — центрированное окно затянуло бы в
g_t для t < a информацию о днях после a.

| якорь | base | shock | | intent | |
|---|---|---|---|---|---|
| 288 | 1.74285 | 1.74299 | -0.00014 | 1.74308 | -0.00023 |
| 318 | 1.74251 | 1.74282 | -0.00031 | 1.74277 | -0.00026 |
| 348 | 1.75224 | 1.75263 | -0.00039 | 1.75246 | -0.00022 |
| 378 | 1.68201 | 1.68193 | +0.00009 | 1.68154 | +0.00047 |
| среднее | | | **-0.00019** | | **-0.00006** |

Знаки у обеих: «- - - +».

Прямая диагностика подтверждает. Сняв с невязки полином от базы,
корреляции: шоки от +0.0032 до +0.0090, воронка от -0.0038 до
+0.0013. Все 16 вместе линейно объясняют R² = 0.000298 невязки,
то есть +0.00025 RMSLE — и это ПОТОЛОК в выборке, на одном якоре,
при 16 свободных параметрах.

Содержательно: все восемь признаков шока имеют один знак
(вероятность 2/256 при независимом шуме), то есть эффект
«пользователь, чьи корзины и запросы подскакивали в дни общего
всплеска, покупает чуть больше» реален, но стоит около 6e-5 на
признак и почти целиком уже сидит в базе. Воронка не даёт ничего:
корреляции ±0.004, знаки смешанные, ist_cart_21 ровно ноль.

### Якорь 378 систематически благоприятен

378 — единственный положительный якорь у ОБЕИХ семей, и его shape
равен 1.682 против 1.742–1.752 у остальных трёх. Стандартная
одноякорная валидация сидит именно на 378 и показала бы для
воронки +0.00047, то есть выше порога 2e-4. Это второй ложный
проход подряд, который дала бы проверка на 378.

### Разрешение абляции измерено — и оно равно порогу решения

Пять сидов на якоре 378, парное сравнение (base и base+X обучаются
на одном сиде, разность берётся внутри сида).

base: 1.68201 1.68184 1.68186 1.68201 1.68176, std 0.00010

| семья | парные разности | среднее | std | t |
|---|---|---|---|---|
| shock | +0.00013 -0.00040 -0.00013 -0.00027 -0.00028 | **-0.00019** | 0.00020 | -2.08 |
| intent | +0.00047 +0.00006 -0.00031 +0.00007 +0.00006 | +0.00007 | 0.00028 | 0.57 |

У шоков среднее отрицательно, но t = -2.08 при ЧЕТЫРЁХ степенях
свободы соответствует p ~ 0.11: корректная формулировка —
«убедительного положительного эффекта нет», а не «эффект
отрицателен». Воронка при t = 0.57 — чистый ноль.

Пять сидов НЕ сделали эти средние истинными эффектами. Здесь
измерена дисперсия оценивания, а не получен оракул.

Одиночный замер в четырёхъякорной абляции дал на 378 шокам +0.00009
при истинном -0.00019 и воронке +0.00047 при истинном +0.00007. В
ОБОИХ случаях знак одного сида не совпал с правдой, а +0.00047
оказался максимумом из пяти розыгрышей.

ГЛАВНАЯ ЦИФРА ДНЯ: sigma парной абляции равна 2.0-2.8e-4 на
ячейку, то есть ровно порог, по которому принимаются решения.
Она впервые позволяет проектировать эксперименты по мощности,
а не вычитывать смысл в четвёртой значащей цифре shape.

Область её применимости узкая: она относится к схеме
Delta_s = shape(base_s) - shape(base+X_s), где заново обучаются
стохастические деревья. Она НЕ обесценивает детерминированные
направления, прямые корреляционные тесты, крупные sign-flip,
результаты с несколькими независимыми механизмами и проверки
лидербордом. Четырёхъякорная
таблица со знаками «- - - +» у обеих семей была недостаточно
мощной, и её знаки — по большей части шум.

Чтобы разрешить эффект 2e-4 при sigma = 2.5e-4 на уровне t = 2,
нужно SE = 1e-4, то есть около ШЕСТИ сидов на ячейку: при четырёх
якорях это 24 пары моделей на семью.

К требованию переноса во времени добавляется требование мощности:
разрешение эксперимента объявляется ДО запуска. Иначе четыре якоря
вместо двух дают четыре зашумлённых числа вместо двух.

Оговорка про XGBoost: там C и D оценивались на прогнозах по 233
тысячам пользователей, а не на разностях shape между обучениями,
поэтому сидовым шумом этот замер страдает меньше. Но вывод
«три сида ухудшают перенос» теперь стоит считать неподтверждённым:
возможная часть эффекта — шум калибровки lambda, а не механизм.

## Денежная механика покупочного цикла

Гипотеза: 183 признака — интегралы по фиксированным календарным
окнам, а совместного распределения P(v_j, g_j | история) в них нет.
38 признаков в трёх группах: размер покупочного события,
связь интервал -> размер следующей покупки, положение внутри
собственного цикла.

Покупочный день определён однозначно: gmv > 0 и to_ord > 0
совпадают на всех 4 736 907 строках панели, расхождений нет в обе
стороны. На пользователя приходится около 19 покупочных дней.

### Главная предпосылка не воспроизводится

corr(log gap, log размер) = **-0.0258** на 4 517 739 парах покупок.
В литературе по CLV более длинный межпокупочный интервал
сопровождается БОЛЬШЕЙ покупкой; здесь связь слабо отрицательна и
по величине нулевая.

### Два потолка

Линейно, после снятия с невязки полинома от базы: максимальная
корреляция отдельного признака 0.0097 (mv_max), все 38 вместе дают
R² = 0.000395, то есть +0.00033 RMSLE — уже ниже объявленной планки
5e-4, и это оценка В ВЫБОРКЕ.

Нелинейно, LightGBM на 400 деревьев на невязке базы, честно между
половинами пользователей:

| блок | признаков | объяснённая доля | потолок |
|---|---|---|---|
| денежный цикл | 38 | **-0.0098** | 0 |
| шоки+воронка (контроль) | 16 | -0.0093 | 0 |

Предсказывает хуже константы, и контроль — блок, уже проваленный
в абляции, — даёт то же число.

Ограничение теста: он предполагает АДДИТИВНОСТЬ. Признак может
ничего не давать в предсказании невязки в одиночку и работать
внутри основной модели во взаимодействии со 183 существующими.
Поэтому полная парная абляция запускается всё равно.

## Percentile/rank — переоткрыт и закрыт корректно

Ретроаудит после замера шума вернул эту ветку из «закрытых» в
«неразрешённые»: старый результат -0.0003 был одиночной абляцией,
то есть внутри sigma. Механизм у неё к тому же отличается от всех
остальных проверенных — ранг не добавляет информации о
пользователе, а меняет систему отсчёта, в которой дерево режет уже
имеющуюся. Именно поэтому диагностика «корреляция с невязкой»,
закрывшая шоки и воронку за секунды, к рангу НЕПРИМЕНИМА: внутри
якоря ранг монотонен исходному признаку.

Дизайн объявлен до запуска: 24 признака зафиксированы заранее без
свипа по вариантам, шесть парных сидов, якоря 318 и 348, якорь 378
оставлен нетронутым подтверждающим. Порог +1.5e-4 при t >= 1.5.

Ранг считается ВНУТРИ каждого якоря: r = (rank_A(x) - 0.5)/N_A.
Ранжирование по объединению якорей вернуло бы ту самую зависимость
от общего уровня, ради устранения которой признак и строится.

| якорь | среднее по 6 парам |
|---|---|
| 318 | -0.00011 |
| 348 | +0.00007 |
| **pooled, 12 пар** | **-0.00002** |

std 0.00023, SE 0.00007, t = -0.31. Порог НЕ пройден, ветка
закрыта. Старый -0.0003 был шумом, но истина оказалась нулём, а не
пропущенным плюсом.

Побочно: std парной разности 0.00023 на ДРУГОЙ паре якорей и другой
семье признаков воспроизводит сегодняшнюю оценку sigma = 2.0-2.8e-4.
SE 0.00007 означает разрешение около 1.4e-4 — ровно проектное.

Механизм не подтвердился и по форме: при работающей инвариантности
эффект ожидался БОЛЬШЕ на раннем якоре, где обучающая выборка
дальше от валидационной по составу. Вышло -0.00011 на 318 против
+0.00007 на 348. Обе величины внутри шума, так что это не довод
против, но и подпорки нет.

### Денежный цикл — абляция подтвердила оба потолка

Шесть парных сидов на 318 и 348, якорь 378 не тронут, 221 признак
против 183.

| якорь | среднее по 6 парам |
|---|---|
| 318 | +0.00001 |
| 348 | +0.00001 |
| **pooled, 12 пар** | **+0.00001** |

std 0.00016, SE 0.00004, t = +0.24. Разрешение около 8e-5 при
t = 2, то есть это не «не нашли», а «нашли ноль с точностью до
восьми стотысячных».

### Диагностика на невязке работает как фильтр

| семья | потолок на невязке | парная абляция |
|---|---|---|
| шоки | ~0 | -0.00019 |
| воронка | ~0 | +0.00007 |
| денежный цикл | -0.0098 | +0.00001 |

Три совпадения из трёх. LightGBM на невязке базы честно между
половинами пользователей стоит минуту и предсказывает исход
получасовой абляции. Ограничение остаётся прежним: тест
предполагает аддитивность и не увидел бы признак, работающий
ТОЛЬКО во взаимодействии с существующими 183. Но за три проверки
такого случая не встретилось.

Практическое правило: сначала потолок за минуту, и только при
ненулевом потолке — 24 обучения.

### Годовые праздники: объяснение есть, рычага нет

Боевое окно d = 409..438 содержит 23 февраля (d = 418) и 8 марта
(d = 431). В сами праздники спрос НИЖЕ тренда (z = -0.1 и -0.6),
всплеск идёт до них: d = 46..52 держит +0.5..+1.6, а d = 59 и 61
дают +2.0 и +2.1 — десятка крупнейших дней года наравне с 11.11 и
предновогодней неделей.

Это объясняет, почему годовой фильтр дал +0.00079, но нового
направления не даёт:

* dann использует 30-дневные блоки j = 2..10 (апрель 2025 —
  январь 2026); вся двугорбая структура укладывается внутрь одного
  блока и на этом разрешении невидима;
* прямой прошлогодний аналог окна уже подаётся отдельно как dyoy
  (v19/v20), это A_ = w(T+1-365, T+30-365);
* форма внутри целевого окна на СУММУ за 30 дней не влияет вовсе;
* уровень окна — это ручка a_m = 0.18, подобранная по лидерборду;
* годовая ось ранее показана одномерной (FWL, корреляции 0.89-0.97).

## Цена подгонки к публичному лидерборду

v23 собран девятью применениями параболы, то есть девять
коэффициентов подогнаны к публичной части из 50 000 пользователей
при приватной в 200 000. Ожидаемая потеря на приватной части от
подгонки одного коэффициента:

    E[(alpha_hat - alpha)^2]·D = Var(e·d) / (N_pub · D)

| направление | D | alpha | цена | доля от вклада |
|---|---|---|---|---|
| Gap-GRU d16 | 0.01520 | +0.4377 | +0.000022 | 2.6 % |
| годовой dann | 18.42202 | +0.0183 | +0.000020 | 1.1 % |
| вес CatBoost | 0.00725 | +0.1340 | +0.000022 | **56.2 %** |

Экстраполяция на девять коэффициентов: **+0.000193**. На порядок
меньше отставания 0.0021 и на порядок меньше вклада самих
направлений. Риск потерять набранное на приватной части не
подтверждается.

### Правило, которое из этого следует

Цена одного подогнанного коэффициента ПОСТОЯННА и равна примерно
2.2e-5 независимо от того, сколько он приносит: она определяется
размером публичной выборки, а не величиной эффекта. У всех трёх
направлений она совпала.

Поэтому подгонять по лидерборду коэффициент для направления
дешевле 4.4e-5 — чистый минус. Строка «вес CatBoost» это и
показывает: вклад +0.00004 при цене +0.000022.

Это ретроспективно оправдывает порог 2e-4 с запасом почти в пять
раз: он выбирался из устойчивости знака, а оказался ещё и
экономически верным.

Оговорки: столбец «вклад» — оракульные C²/D на валидационном
якоре, а не измеренный перенос (для годового фильтра +0.00185
против реальных +0.00079 на лидерборде). Весь расчёт предполагает,
что публичная и приватная части суть случайное разбиение одной
популяции.

## Сброс к простому: 55 базовых представлений

Четыре блока, не покрытые 183 признаками: профиль по дням недели,
свёрнутый по составу БУДУЩЕГО окна (не anchor_doy: модель узнаёт
не «сейчас январь», а «пользователь покупает по субботам, впереди
пять суббот»); возрасты последних 1/2/3/5 событий; тип дня по
каналам; разброс и серии.

Уточнение по данным: search и cat в панели БИНАРНЫЕ флаги, а
searches — счётчик. Состояний дня четыре: только поиск 69 %,
только каталог 4 %, оба 11.5 % и НИ ОДНОГО 15 % — дни активности
без обоих флагов.

| якорь | среднее по 6 парам |
|---|---|
| 318 | -0.00001 |
| 348 | +0.00006 |
| **pooled, 12 пар** | **+0.00003** |

std 0.00018, SE 0.00005, t = +0.49. Порог не пройден.

Парный разброс на 318 составил всего 0.00006 против 0.0002 у
прежних блоков: 55 признаков не разбавляют выбор сплитов, в
отличие от восьми признаков шока.

## Гребневой потолок — ОДНОСТОРОННИЙ фильтр

Честный Ridge между половинами пользователей на якоре 378 дал:

| блок | признаков | вне выборки | потолок | абляция |
|---|---|---|---|---|
| простые представления | 55 | +0.000767 | +0.00064 | **+0.00003** |
| шоки+воронка | 16 | +0.000103 | +0.000086 | ~0 |
| денежный цикл | 38 | +0.000003 | +0.000003 | +0.00001 |

Нулевой потолок означает нулевую абляцию — 3 случая из 3.
Ненулевой потолок НЕ означает ненулевую абляцию — 1 из 1.

Механизм тот же, что у ранговой усадки: структура, найденная
поперёк пользователей на одном якоре, не обязана существовать при
переносе на следующий. Там было +0.00063 поперёк против -0.00023
во времени.

Инструмент годится, чтобы отсеивать, и не годится, чтобы отбирать.

Отдельно: прежний фильтр на LightGBM с 400 деревьями непригоден
даже для отсева слабых эффектов — он дал -0.0075 там, где Ridge
находит +0.00077. Для трёх ранее закрытых блоков вывод не
меняется, Ridge подтверждает их ноль независимо.

## Факторизация головы регрессии — первый архитектурный прирост

На покупателях log Y = log N + log(Y/N) ТОЧНО, поэтому в бесконечно
гибком пределе факторизация не меняет байесовский прогноз вовсе:

    E[log N | X,Y>0] + E[log(Y/N) | X,Y>0] = E[log Y | X,Y>0].

Значит любой выигрыш обязан быть эффектом КОНЕЧНОЙ ёмкости — двум
более гладким функциям проще, чем одной. Поэтому контроль обязан
иметь тот же бюджет деревьев, иначе измеряется их число.

### Этап 1: против согласованного контроля, walk-forward

Целевое окно (A, A+30]; ёмкости выровнены.

| сравнение | 288 | 318 | 348 | 378 | alpha | замор. lam | перенос | оракул |
|---|---|---|---|---|---|---|---|---|
| buy 200+200 против 400 | +0.00043 | +0.00072 | +0.00017 | +0.00020 | 0.60-0.88 | 0.735 | +0.00028 | +0.00029 |
| buy 400+400 против 800 | +0.00074 | +0.00088 | +0.00045 | +0.00024 | 0.63-0.82 | 0.755 | +0.00036 | +0.00038 |
| ord 400+400 против 800 | +0.00044 | +0.00092 | +0.00042 | +0.00036 | 0.62-0.79 | 0.689 | +0.00049 | +0.00050 |

Замороженный коэффициент забирает 95-98 % оракула — лучший перенос
за всё время; у XGBoost было 93 % при вчетверо меньшей величине.
Двенадцать сравнений положительны, но они статистически ЗАВИСИМЫ:
те же пользователи, те же цели, близкие модели. Настоящее
свидетельство — сохранение знака на четырёх последовательных
якорях внутри каждой конструкции плюс качество переноса.

Контроль деградирует от 400 к 800 (1.74246 -> 1.74324 на 288), а
факторизация от 200+200 к 400+400 теряет меньше: компоненты
устойчивее к росту ёмкости, что и означает их гладкость.

Специфичность не подтвердилась: ord при равной ёмкости выигрывает
не хуже buy. Работает факторизация как таковая, а не разложение
именно по покупочным дням.

### Этап 2: прямая замена production-головы, без всякого lambda

Классификатор не тронут, X, якоря, веса и офсеты те же, бюджет
равен: 600 деревьев против 300+300. Офсеты факторизованы по якорю
взвешенно, как anchor_offsets: модели учат u - mu_u(A) и
v - mu_v(A), уровень возвращается суммой mu_u + mu_v последнего
якоря.

    Delta_A = shape(p_old·m_old) - shape(p_old·m_fact)

| | 348 | 378 |
|---|---|---|
| p_old·m_old | 1.75221 | 1.68163 |
| замена на buy | **+0.00049** | **+0.00022** |
| замена на ord | **+0.00060** | **+0.00025** |

Оба якоря положительны, обе величины выше 2e-4, вариант не
выбирался по подтверждающему якорю. Для сравнения: направления в
v23 дали +0.00054 (CatBoost), +0.00079 (годовой), +0.00094
(Gap-GRU) — и каждое ценой подгоняемого коэффициента, а здесь
новых параметров нет вовсе.

### Оговорка про log(1+Y) снята эмпирически

delta = log(1+Y) - log(Y) на положительных: mean 0.041, std 0.131,
p90 0.099, p99 0.258 при std(log(1+Y)) = 1.336. То есть 10 %
масштаба ошибки, и это систематическая кривизна, а не шум.

Но склейка log(1+e^s) уже применяет delta В ТОЧКЕ ПРОГНОЗА;
остаётся лишь зазор Йенсена от условной дисперсии, так как
f(s) = log(1+e^s) выпукла с f'' = sigma(1-sigma). Явная поправка
0.5·f''·Var ухудшила на 2e-5 на 348 и не изменила ничего на 378 —
причём Var была взята полной вместо условной, то есть поправка
была завышена примерно в полтора раза. Эффект второго порядка,
оговорка закрыта.

### Репликация по сидам: подтверждена с запасом

Сид передаётся как random_state ОТДЕЛЬНЫМ аргументом, в reg_params
его нет. Первая попытка репликации этого не делала и дала пять
побитово одинаковых лесов — то есть при дефолтном сиде bagging и
feature_fraction воспроизводимы до последнего знака.

Пять сидов, парно, голова 600 против 300+300:

| | 348 | 378 |
|---|---|---|
| buy | +0.00037 (t = 8.9) | +0.00031 (t = 4.9) |
| ord | +0.00048 (t = 14.7) | +0.00026 (t = 5.6) |

Двадцать парных разностей из двадцати положительны, ни одна ниже
1.4e-4. Одиночный замер +0.00049 на 348 был удачным сидом, истина
+0.00037. Сидовый шум головы существует (std 0.00011), но парный
дизайн его гасит: SE 0.00004.

### На ансамбле эффект схлопывается

Шесть LGB бэкбона (история 240/300/365 × сиды 42/7) переобучены
целиком, включая СТАРУЮ голову, чтобы контроль был согласованным.
CatBoost-часть взята сохранённой. Якорь 378.

| вариант LGB | только LGB | бэкбон 0.4/0.6 | поверх v23 | Δ |
|---|---|---|---|---|
| old (контроль) | 1.68060 | 1.67982 | 1.67746 | — |
| buy | 1.68041 | 1.67978 | 1.67742 | **+0.00005** |
| ord | 1.68046 | 1.67975 | 1.67739 | **+0.00007** |
| сохранённый v23 | 1.68100 | 1.67988 | 1.67753 | |

Опасение, что годовой фильтр и Gap-GRU чинят ту же ошибку,
ОПРОВЕРГНУТО: при переходе от бэкбона к v23 разница сохраняется
полностью (+0.00004 -> +0.00005 и +0.00007 -> +0.00007).

Потеря механическая и раскладывается на два множителя:

    0.00031 --[усреднение 6 моделей]--> 0.00019 --[вес 0.4]--> 0.00007

Усреднение съедает 40 %: факторизация чинит отчасти ту же ошибку,
что и сам ансамбль. Остальное режет вес 0.4, поскольку двенадцать
CatBoost остались с обычной головой.

Оговорка: переобученный контроль (1.68060) лучше сохранённого
бэкбона (1.68100) на 4e-4, единственное отличие — отключённая
ранняя остановка. Сравнение old-против-fact внутренне корректно,
но абсолютные числа не переносятся на боевой рецепт напрямую.

Проекция при распространении на CatBoost: около +0.00017,
впритык к порогу 2e-4, а не выше него.

### CatBoost не факторизуется — знак переворачивается

Три сида, парно, тот же режим:

| CatBoost | 348 | 378 |
|---|---|---|
| buy | -0.00013 (t = -2.4) | +0.00016 (t = +9.0) |
| ord | +0.00003 (t = +1.5) | -0.00001 (t = -0.4) |

Знак buy переворачивается между якорями, причём обе величины
значимы внутри своего якоря: сиды меряют сидовый шум, а не
межъякорную устойчивость.

Из данных следует ровно одно: эффект переносится для LightGBM и
не переносится для CatBoost. Объяснение через гладкость
симметричных деревьев — отдельная гипотеза, не проверенная здесь,
и выдавать её за подтверждение механизма нельзя.

Факт для протокола: production-голова CatBoost и без факторизации
сильнее LightGBM (1.75186 против 1.75200-1.75232).

### v25 на лидерборде: 1.6480404, и это упор в разрешение

v25 = v23 + 0.4·(z_LGB_fact - z_LGB_old), коэффициент единица по
построению, направление демеанировано, корреляция z с v23 = 0.999953.

Отклик +0.0000584, то есть ХУЖЕ. Разворот НЕ точен: D посчитано по
всем 250 000 тестовых пользователей, а public считается на скрытой
подвыборке, поэтому восстанавливается не C_pub, а
C_pub + 0.5·(D_all - D_pub) при неизвестном D_pub. Числа ниже
справедливы лишь при D_pub ~ D_all. Точен только сам public score.

| | значение |
|---|---|
| C на лидерборде | +2.51e-5 |
| alpha = C/D | +0.104 |
| C на валидации 378 | +2.45e-4 |
| alpha на валидации | +0.859 |
| SE(C) при 50 000 публичных | **1.28e-4** |
| расхождение | **1.71 sigma** |

Наблюдённое C отстоит от валидационного на 1.71 sigma и от НУЛЯ на
0.2 sigma. Замер не отличает «эффект есть» от «эффекта нет».

### Разрешение публичного лидерборда — жёсткий предел

Сам public score измерен точно. Неопределённость возникает не от
численной точности лидерборда, а оттого, что public — конечная
подвыборка пользователей: SE(C) = 1.28e-4 при 50 000 — это оценка
ВЫБОРОЧНОЙ неопределённости при условии репрезентативного
разбиения. Практически: по одному такому изменению нельзя надёжно
судить о переносе на private, если эффект порядка 1e-4 и меньше.

Отсюда: стратегия «накопить много мелких направлений» не работает
не потому, что направлений нет, а потому, что каждое отдельное
нельзя подтвердить. Все прошлые успехи были выше этого порога:
CatBoost +0.00054, годовой фильтр +0.00079, Gap-GRU +0.00094.

Лучший результат остаётся v23 = 1.6479820.

## Пакет вариантов головы: работает только замена

Три сида, парно, якоря 348 и 378.

| вариант | 348 | 378 |
|---|---|---|
| B полная замена 300+300 | +0.00034 (t 9.1) | +0.00034 (t 3.4) |
| D факторизация как ДВА ПРИЗНАКА | -0.00021 (t -3.2) | -0.00021 (t -1.7) |
| E Poisson-счётчик вместо L2 на log N | -0.00166 | -0.00201 |
| C гибрид с замороженным lam | — | +0.00025 |
| собственный lam гибрида | 0.683 | 0.725 |

D — идея дать прямой голове предсказания log N и log(Y/N) как две
learned-координаты, чтобы сохранить верную цель log(1+Y). Признаки
строились leave-one-anchor-out против утечки. Устойчиво ВРЕДИТ, и
одинаково на обоих якорях, так что дело не в перекосе шума между
обучающими и валидационными признаками: две координаты просто
разбавляют выбор сплитов среди 183, как раньше это делали восемь
признаков шока.

E: цель головы логарифмическая, а Poisson оптимизирует
правдоподобие на шкале самого N; после логарифма от предсказанного
среднего калибровка в лог-шкале теряется.

C: lam переносится хорошо (0.683 -> 0.725, расхождение 6 %), и
оптимум лежит МЕЖДУ прямой и факторизованной головой, а не в
полной замене.

### Разбавление подтверждено на масштабе трёх сидов

Контроль гибрида — усреднённая по трём сидам прямая голова,
1.68144, тогда как отдельные сиды дают 1.68162-1.68178. Усреднение
ТРЁХ моделей само даёт +0.00024, и выигрыш факторизации против
усреднённой базы падает с +0.00034 до +0.00025.

Значит факторизация чинит ту же негладкость, что и усреднение.
Это свойство эффекта, а не дефект реализации, и оно объясняет,
почему на полном ансамбле из шести моделей с весом 0.4 остаётся
измеренные +0.00005.

## Приватная устойчивость: защищать нечего, добирать нечего

Симуляция самого разбиения на якоре 378: подгонка трёх
коэффициентов на случайных 20 % (50 000), оценка на остальных 80 %,
200 повторов.

    цена подгонки на приватной части: +0.000083 ± 0.000077
                                      (p95 +0.000224)

Совпадает с аналитической оценкой 2.2e-5 на коэффициент, то есть
6.6e-5 на три, полученной другим способом.

Бутстрап по пользователям, 300 выборок: вклад поправок v23 равен
-0.00236 ± 0.00015, отрицательных 0 из 300. Консервативить
коэффициенты незачем.

### Валидационный оптимум против развёрнутого

| ось | в v23 | оптимум на 378 |
|---|---|---|
| d16 Gap-GRU | 0.350 | 0.468 |
| dann годовой | 0.0104 | 0.0189 |
| вес CatBoost против LGB | 0.000 | +0.261 |

Первые две подбирались ПО ЛИДЕРБОРДУ, то есть по тестовому окну,
поэтому расхождение с якорем 378 — временной дрейф, и доверять
надо развёрнутым значениям.

### Ось смеси LGB/CatBoost закрыта

| доля LGB | 0.40 | 0.30 | 0.20 | 0.139 | 0.05 | 0.00 |
|---|---|---|---|---|---|---|
| Δ к 0.4/0.6 | — | +0.00007 | +0.00010 | +0.00010 | +0.00007 | +0.00003 |

Кривая плоская: весь размах от 0.4 до 0.0 составляет 1e-4. Оптимум
около 0.18-0.20 стоит +0.00010, ниже разрешения. Знак сдвига
неустойчив по якорям: +0.063 на 288, -0.028 на 318, +0.030 на 348.
Смесь 0.4/0.6 практически оптимальна.

### Замечание о решающем правиле

Отдельная оптимизация под RMSLE вместо MSE в z бессмысленна:
RMSLE ЕСТЬ RMSE в z, поэтому E[z|x] = p·m и есть оптимальное
правило. Выигрыш возможен только от миска­либровки, а она уже
измерена ранговым сплайном и во времени не переносится.

## Персональный baseline как offset таргета — закрыто

Идея: positive-head учится на z - l_plus(A), где l_plus — ОДНА
константа на якорь, тогда как сильнейшая координата пользователя
(его GMV за предыдущие 30 дней) лежит внутри X, и дерево строит
почти гладкую зависимость кусочно-постоянными листьями. Вынести её
в функциональную форму:

    r = z - beta·b - mu_A,   прогноз m = beta·b + mu_last + f(X)

beta оценивается только по обучающим якорям с центрированием ВНУТРИ
якоря, иначе календарный уровень попадёт в beta.

### Гладкая часть реальна и устойчива

| | gmv_7 | gmv_30 | gmv_90 | ord_30 | ordDays_90 |
|---|---|---|---|---|---|
| 348 | 0.0508 | -0.0668 | 0.1798 | 0.3811 | 0.0483 |
| 378 | 0.0515 | -0.0692 | 0.1881 | 0.3834 | 0.0308 |

Коэффициенты совпадают с точностью до нескольких процентов, включая
знак подавления у gmv_30 при наличии gmv_90 и ord_30. Наибольший вес
получает ЧИСЛО ЗАКАЗОВ за 30 дней, а не сумма.

Один член снимает 14.8 % дисперсии положительного таргета
(std остатка 1.2302 против 1.3324), пять членов — 21.3 % (1.1822).

### И это не даёт ничего

| вариант | 348 | 378 |
|---|---|---|
| B offset gmv_30 | -0.00023 (t -2.8) | +0.00004 (t +1.3) |
| C линейный skip из пяти членов | +0.00000 (t +0.1) | -0.00028 (t -5.3) |

Знаки переворачиваются, C на 378 уверенно вредит.

Вывод: снятие 15-21 % дисперсии аналитически не освобождает дереву
ёмкость. Модель НЕ ограничена в аппроксимации гладкой части —
лестница из 600 деревьев по 63 листа справляется с монотонной
зависимостью не хуже явной линейной формы. Гипотеза «дерево тратит
сплиты на то, что задаётся формулой» опровергнута прямым замером.

Это сужает объяснение факторизации головы: там выигрыш был, значит
дело не в гладкости отображения X -> y вообще.

## Декомпозиции таргета: горизонт и канал — обе закрыты

Три сида, парно, якорь 348, равный бюджет 600 деревьев.
Горизонт: 3 x 200 на GMV в декадах (1-10), (11-20), (21-30).
Канал: 2 x 300 на gmv_search и gmv_cat.

Доли нулей СРЕДИ ПОКУПАТЕЛЕЙ: декады 0.395 / 0.404 / 0.380,
поиск 0.023, каталог 0.844.

| склейка | горизонт 348 | горизонт 378 | канал 348 | канал 378 |
|---|---|---|---|---|
| сумма expm1 без поправки | -0.01434 | — | **-0.00029** | — |
| лог-линейный комбинатор | **-0.00580** | **-0.00272** | -0.00284 | -0.00236 |
| сумма expm1 с поправкой масштаба | -0.02282 | -0.01774 | -0.01279 | -0.01245 |

Восемь замеров, все отрицательные, все на порядок-два выше
разрешения эксперимента. Обе декомпозиции закрыты окончательно.

### Почему поправка масштаба ухудшает

Поправки вышли крупными: декады 3.70 / 3.98 / 3.86, поиск 1.76,
каталог 7.21. Разрыв Йенсена определяется УСЛОВНОЙ ДИСПЕРСИЕЙ
log Y, а не долей нулей: при Var(log Y|X) ~ 1.4 переход exp от
условного среднего логарифма занижает среднее примерно вдвое даже
без нулей.

Но shape по построению НЕ ЧУВСТВУЕТ УРОВЕНЬ, поэтому равномерное
занижение всех компонент поглощалось как сдвиг. Домножение на
поправку убирает безвредный сдвиг и оставляет нелинейную часть
смещения, которая как раз портит форму.

Отсюда общее правило: при валидации по форме правильная склейка —
лог-линейный комбинатор, чьи свободный член и коэффициенты сами
поглощают уровень и масштаб. Поправки масштаба контрпродуктивны.

### Гипотеза о неоднородности горизонта не опровергнута

Коэффициенты комбинатора для декад: [0.196, 0.174, 0.519] при
свободном члене 2.252. Дальняя декада получает почти втрое больший
вес, чем средняя — если бы функция прогноза была однородна по
горизонту, коэффициенты вышли бы примерно равными.

То есть неоднородность, похоже, есть, но эксплуатировать её
делением таргета невыгодно: каждая декада вчетверо труднее полной
суммы (40 % нулей против 0 % среди покупателей), а деревьев на неё
втрое меньше. Потеря ёмкости на порядок превышает выигрыш от
специализации.

Каталожная голова, вопреки ожиданию, не пустая: при 84 % нулей она
получает в комбинаторе вес 0.314 против 0.875 у поисковой.

## Где живёт выигрыш факторизации, и почему им нельзя воспользоваться

Разложение улучшения MSE по стратам, якорь 378 (на 348 картина та
же и резче):

| страта | доля | вклад |
|---|---|---|
| непокупатели y=0 | 0.438 | **-40.8 %** |
| покупатели z в [3.0, 4.5] | 0.224 | **+117 %** |
| покупатели, верхний квинтиль z > 5.3 | 0.112 | **-39.2 %** |

Факторизация помогает на МЕЛКИХ И СРЕДНИХ покупателях (Y ~ 20-90) и
вредит на непокупателях и на крупном хвосте. Объяснение: у
покупателя с одним заказом log N = 0 и всё предсказание сводится к
log AOV, где разложение максимально информативно; у крупного
покупателя N велико и изменчиво, и разложение добавляет шум.

### Вентиль по предсказанному уровню не работает

Вес lam(m_hat) обучен на 288+318 по восьми квантильным корзинам,
применён к 348 и 378:

| якорь | контроль | lam единый | вентиль | разница |
|---|---|---|---|---|
| 348 | 1.75180 | +0.00027 | +0.00027 | -0.00000 |
| 378 | 1.68158 | +0.00027 | +0.00028 | +0.00001 |

Оптимальные веса по корзинам лежат в узкой полосе 0.53-1.07 без
структуры.

Причина в том, что отрицательные страты определены величинами,
НЕДОСТУПНЫМИ в момент прогноза: y=0 и верхний квинтиль ИСТИННОГО z.
По наблюдаемому p0 все пять квинтилей дают положительный вклад
(+4.8 %, +8.1 %, +45 %, +10.9 %, +31.3 % на 348) — отрицательной
страты просто нет. Верхний квинтиль истинного z размазан по всем
корзинам предсказанного уровня, и вентиль его не выделяет.

Побочно: единый lam = 0.78, оценённый на 288+318, переносится на
оба поздних якоря с одинаковым +0.00027.

## Счётчик покупочных дней: ресурс есть, перевозчик дороже

Propensity-head учится на одном бите C = 1[N_buy > 0]. Прошлый
Poisson-эксперимент этого НЕ проверял: он обучался на X[Y > 0], то
есть нулевые выбрасывались до обучения и различение Y=0 против Y>0
улучшить физически не мог.

Здесь на ВСЕХ пользователях N = число различных дней с GMV > 0 в
окне (0..30), LGBM с objective='poisson', score s = log(1 + lam).
Никакого p = 1 - exp(-lam). Prequential: счётчик на якоре A учится
только на якорях a + 30 <= A.

Разметка действительно богаче бита: среднее N = 1.92-2.00, доля
N = 0 около 0.39, то есть у покупателей в среднем ~3.2 покупочных
дня с содержательным разбросом.

### Диагностика: знак не согласован

| якорь | corr(s, logit p0) | C | alpha |
|---|---|---|---|
| 288 | +0.9884 | +1.24e-3 | +0.466 |
| 318 | +0.9885 | +1.32e-3 | +0.517 |
| 348 | +0.9899 | **-5.65e-4** | **-0.304** |
| 378 | +0.9886 | +7.47e-4 | +0.324 |

corr(s, logit p0) = 0.989 на всех якорях: счётчик упорядочивает
пользователей почти тождественно биту, std(s_perp) = 0.087.

### Полная склейка с matched-control

Три сравнения обязательны, иначе легко переоткрыть закрытую ветку
аффинной калибровки p:

| | 348 | 378 |
|---|---|---|
| (1) p0 m0 | 1.75169 | 1.68115 |
| (2) sigma(a + b logit p0) m0 | 1.75221 (-0.00052) | 1.68121 (-0.00006) |
| (3) sigma(a + b logit p0 + c s) m0 | 1.75216 (-0.00046) | 1.68097 (+0.00017) |
| **цена счётчика (3)-(2)** | **+0.00005** | **+0.00024** |

Против matched-control знак СОГЛАСОВАН (оба плюс), коэффициент c
устойчив (0.491 и 0.372) — информация в числе покупочных дней
действительно есть сверх p0.

Против базы знак ПЕРЕВОРАЧИВАЕТСЯ, потому что доставка требует
аффинной перекалибровки p0, а она сама теряет -0.00052 и -0.00006,
непредсказуемо и в среднем больше, чем счётчик приносит. Калибратор
при этом находит почти тождество (b = 1.039 и 1.001) и всё равно
теряет — независимое воспроизведение уже известного факта, что
аффинная калибровка p между якорями не переносится.

Итог: ресурс есть, но меньше стоимости единственного известного
способа его забрать.

## Частота предсказуема, но коллинеарна; выигрыш идёт по AOV

Два замера, оба опровергают прежние утверждения в этом журнале.

### (1) N при N > 0 предсказуемо

Счётчик, обученный ТОЛЬКО НА ПОКУПАТЕЛЯХ, prequential:

| якорь | R^2 по log(1+N) | corr | AUC(p0) на бите |
|---|---|---|---|
| 348 | **+0.404** | +0.677 | 0.848 |
| 378 | **+0.405** | +0.676 | 0.854 |

Ожидание было, что R^2 окажется низким и ветка закроется по существу
данных. Неверно: частота предсказуема прилично.

Ограничение НЕ в данных. Частота предсказуема, но НЕ НЕЗАВИСИМО: её
предсказание почти коллинеарно предсказанию факта покупки
(corr(s, logit p0) = 0.989). Кто склонен покупать, тот склонен
покупать часто; отдельного измерения почти нет.

### (2) Выигрыш факторизации структурирован по AOV, а не по N

Прежнее объяснение «помогает там, где log N ~ 0» НЕ подтвердилось.
Вклад по числу покупочных дней не убывает:

| страта | 348 | 378 |
|---|---|---|
| N=0 | -94.3 % | -40.8 % |
| N=1 | +98.1 % | +31.6 % |
| N=2 | +37.5 % | +54.0 % |
| N=3-4 | +28.9 % | +53.5 % |
| N>=5 | +29.8 % | +1.6 % |

По среднему чеку AOV = Y / N картина чистая и монотонная:

| квинтиль AOV | 348 | 378 |
|---|---|---|
| низший | +116 % | +46 % |
| 2-й | +79 % | +50 % |
| 3-й | +87 % | +48 % |
| 4-й | -5 % | +17 % |
| **высший** | **-83 %** | **-19 %** |

Факторизация помогает на малом чеке и вредит на крупном, монотонно и
на обоих якорях. Дело в размере корзины, а не в числе заказов.

Это открывает то, чего не было при прошлой попытке вентиля: тогда
гейт строился по предсказанному УРОВНЮ m и не выделял вредную
страту. Теперь есть хорошая оценка N (R^2 = 0.40), значит доступен
предсказанный AOV = m - log(1 + N), а именно по нему структура и
монотонна.

## Вентиль по предсказанному AOV — тоже ничего, и почему

AOV_hat = m_ctl - log(1 + lam_hat), счётчик обучен только на
покупателях, prequential. Вентиль на 288+318 -> 348, 378.

| якорь | corr(AOV_hat, AOV) | std AOV_hat | std AOV |
|---|---|---|---|
| 288 | +0.398 | 0.447 | 1.012 |
| 318 | +0.406 | 0.451 | 1.009 |
| 348 | +0.410 | 0.460 | 1.032 |
| 378 | +0.419 | 0.457 | 1.025 |

| якорь | контроль | lam единый | вентиль | разница |
|---|---|---|---|---|
| 348 | 1.75180 | +0.00028 | +0.00029 | +0.00001 |
| 378 | 1.68158 | +0.00028 | +0.00028 | +0.00000 |

Веса по корзинам неструктурированы (0.909, 0.577, 0.696, 0.870,
0.711, 0.689, 0.843, 0.761 вокруг единого 0.766), хотя по ИСТИННОМУ
AOV структура строго монотонна. Корреляции 0.41 не хватает.

Избирательное применение факторизации закрыто дважды, двумя разными
вентильными переменными, по одной причине: вредная страта
определяется величиной, которую нельзя достаточно точно оценить
заранее.

### Побочный результат: где на самом деле потолок

Предсказуемость трёх компонент у покупателя:

| компонента | качество | остаточный std |
|---|---|---|
| факт покупки | AUC 0.85 | — |
| частота log(1+N) | R^2 = 0.40 | ~0.42 |
| средний чек log AOV | R^2 ~ 0.17 | ~0.93 |

Непредсказуемая часть GMV покупателя примерно на порядок по
дисперсии определяется РАЗМЕРОМ КОРЗИНЫ, а не частотой покупок.
Это и есть содержательный ответ, почему потолок там, где он есть:
почти вся оставшаяся ошибка сидит в компоненте, которая хуже всего
предсказывается признаками пользователя.

## v26: полная замена LGB-части (без ранней остановки + факторизация)

Смена режима: ветка обязана заканчиваться сабмитом, а не таблицей.

### Что было упущено в v25

В `fact_prod.py` ОБЕ стороны разности уже были с фиксированными 600
деревьями, поэтому v25 = v23 + 0.4(z_fact_noES - z_old_noES) забрал
только факторизационную половину. Переход noES остался неиспользованным.

Замер на 378 поверх точного v23:

| вариант LGB | поверх v23 | Δ |
|---|---|---|
| сохранённый v23 (ранняя остановка) | 1.67753 | — |
| old, 600 без ES | 1.67746 | +0.00007 |
| ord, 600 без ES + факторизация | 1.67739 | **+0.00014** |

### Построение

Три прогноза на боевом якоре 408 по production-сетке (история
240/300/365 x сиды 42/7, ровно шесть LGB, состав сверен по
сохранённому бэкбону):

    z_prod   ранняя остановка 100, eval_frac 0.12
    z_old    фиксированные 600 без ES
    z_alt    600 без ES + факторизованная голова ord (300+300)

Ранняя остановка срабатывает на 166-297 деревьях, то есть
альтернативная модель ВТРОЕ больше production, а не «чуть дольше
училась».

Направление демеанировано: уровень v23 задан ручкой a_m = 0.18 по
лидерборду, валидировалась форма.

### Сверка корректности

Факторизационная компонента совпала с сохранённой из v25:
corr = +1.0000, std 0.01558 против 0.01558. Значит production-сторона
воспроизведена точно и разность построена та же самая плюс новая
половина.

| компонента | std | среднее |
|---|---|---|
| noES | **0.02023** | +0.00104 |
| fact | 0.01558 | -0.00567 |
| всего | 0.02360 | -0.00463 |

Половина noES БОЛЬШЕ факторизационной, и они слабо антикоррелированы.

### Состав сабмитов и что о них уже известно

| файл | сдвиг std | corr с направлением v25 | публично |
|---|---|---|---|
| v25 (только fact) | 0.01558 | +1.00 | **1.6480404**, хуже v23 на +0.000058 |
| `submission_v26b_noes_only.csv` | 0.02023 | **-0.15** | не измерено |
| `submission_v26_noes_fact_lgb.csv` | 0.02360 | **+0.53** | не измерено |

Существенно: v26 на половину состоит из направления, публично уже
давшего минус. Офлайн обе половины оценены одинаково (+0.00007), но
публично измерена только одна, и знак у неё перевернулся. v26b
изолирует непроверенную половину и почти ортогонален отвергнутой.

### Результат: класс закрыт параболой, без единой лишней абляции

| сабмит | RMSLE | против v23 |
|---|---|---|
| v23 | **1.6479820** | — |
| v25, только факторизация | 1.6480404 | +0.0000584 |
| v26b, только noES | 1.6480902 | +0.0001082 |
| v26, полная замена | 1.6481186 | +0.0001366 |

Три замера ВЗАИМНО СОГЛАСОВАНЫ: предсказание RMSLE(v26) из двух
половин по квадратичной модели даёт 1.6481198 против замеренных
1.6481186, расхождение 1.2e-6. Парабола описывает всё корректно.

| направление | C | a* | потолок C²/D |
|---|---|---|---|
| только fact | +2.5e-5 | 0.103 | +0.0000008 |
| только noES | +2.6e-5 | 0.064 | +0.0000005 |
| полная замена | +5.3e-5 | 0.096 | +0.0000016 |

Знак у всех трёх ПРАВИЛЬНЫЙ (C > 0), но оптимальный масштаб около
одной десятой применённого, а максимум при идеальном коэффициенте —
+1.6e-6 RMSLE, то есть шестой знак.

Офлайн-оценка +0.00014 завышена ВДЕСЯТЕРО по коэффициенту. Четвёртый
подряд случай завышения внутривыборочного коэффициента: v19 вдвое,
v21 в восемь раз, годовой фильтр в полтора, здесь в десять.

Существенно, что noES-половина, изолированная в v26b и почти
ортогональная факторизационной, дала СВОЙ независимый минус. То есть
дело не в том, что v26 тащил отвергнутую компоненту: обе половины
по отдельности публично отрицательны при положительном офлайне.

**Класс модификаций текущей LGB-головы закрыт.** Три публичных замера,
взаимно согласованных, с восстановленным потолком в шестом знаке.
Следующий сабмит должен быть существенно другой моделью, а не ещё
одной настройкой этой головы.

## v27_longmoney: беcкэповая история — крупнейшая находка после годового фильтра

### Аудит 183 признаков нашёл две дыры

Поправка к первому, поверхностному аудиту: признаков среднего чека НЕ
один. Есть `gmv_mean_pos_{7,14,30,60,90,180}` и блок
`buy_gmv_{mean,std,max,last,last3,trend}` — около четырнадцати. Мой
греп их не поймал из-за имён, и утверждение «AOV представлен одним
признаком» было неверным.

Настоящие дыры другие.

**Первая: max_history ОБРЕЗАЕТ пожизненные агрегаты.** Замер прямой:

| | якорь 198 | якорь 378 |
|---|---|---|
| h=300: hist_span / gmv_hist | 195 / 595.6 | 296 / **895.8** |
| h=420: hist_span / gmv_hist | 195 / 595.6 | 373 / **1067.7** |

Боевая модель с h=300 не видит на 378 первые 78 дней, на 408 — первые
108. Ровно ту область, где годовой фильтр нашёл сигнал.

**Вторая: сырые пожизненные суммы несопоставимы между якорями.**
`gmv_hist` на 198 покрывает 198 дней, на 348 — все 300. Обучение идёт
по объединённой выборке, и один столбец означает там разное.

### Результат: +0.0025 на двух якорях

Восемь признаков «на день наблюдения» (life_gmv_rate, life_aov,
life_buy_rate, life_ord_rate, life_act_frac, life_early_share,
life_tenure, life_rel_recent) плюс десять рыночно-нормированных
(rel_*). Парный контроль, два сида, LGB-hurdle с боевой ранней
остановкой.

| вариант | 348 | 378 |
|---|---|---|
| +рынок | **-0.00015** | ~-0.00002 |
| **+жизнь** | **+0.00249** | — |
| +оба | +0.00257 | +0.00276 |

Рыночная нормировка не даёт НИЧЕГО на обоих якорях — гипотеза
«модель не знает глобального уровня» опровергнута. Весь эффект несёт
блок life_*.

Важность подтверждает: новый блок при доле признаков 0.090 забирает
0.156-0.178 важности в РЕГРЕССИИ (в классификаторе лишь 0.03), и почти
всё — один `life_gmv_rate` (0.15).

### Почему это не противоречит тому, что h=365 не выигрывал у h=300

Если бы дело было в «больше дней истории», длинное окно давно бы
победило (1.68315 против 1.68291 — h=300 даже чуть лучше).

Механизм другой: чтобы получить отношение gmv_hist/hist_span, дереву
надо СНАЧАЛА расщепиться по hist_span (несколько дискретных значений
якоря), а затем в каждой ветке использовать свои пороги. Это умножает
нужную глубину. Skip-эксперимент показал, что дерево не нуждается в
помощи с ГЛАДКОЙ функцией внутри одного якоря — но здесь требуется
взаимодействие с дискретной переменной якоря, а это другой случай.

Отсюда объяснение, почему три дня оптимизации головы не дали ничего:
улучшалась модель, которой подавали признаки с плавающим смыслом.

### Боевое направление на 408 и v27_life

Шесть комбинаций (история 240/300/365 x сиды 42/7), оба семейства,
старая и новая версия каждой — 24 обучения. Веса боевые 0.4/0.6.

| | std | среднее |
|---|---|---|
| LGB | 0.09618 | -0.01083 |
| CatBoost | 0.09055 | -0.00983 |
| итоговое (демеанировано) | **0.08998** | -4e-18 |

corr(LGB, CatBoost) = **+0.876**: оба семейства двигаются согласованно
и почти одинаково по величине. Опасение, что вклад несёт только LGB,
не подтвердилось — оно возникло из непостоянства std(z) у CatBoost,
но std(z) есть разброс ПРОГНОЗОВ, а не качество, и на боевом якоре
качество не измеряется вовсе.

Для масштаба: направление v26 имело std 0.0236, v25 — 0.0156. Это
вчетверо больше.

Поправка перераспределяет, а не сдвигает уровень:

| квинтиль базового z | средний сдвиг |
|---|---|
| 0.01-0.74 | +0.0062 |
| 0.74-1.58 | +0.0069 |
| 1.58-2.63 | -0.0011 |
| **2.63-3.88** | **-0.0109** |
| 3.88-8.48 | -0.0011 |

Поднимает слабых, опускает четвёртый квинтиль — меняет ранжирование
в середине, где по старой карте остатков лежит две трети SSE.

### Предварительное разделение механизмов

Сдвиг std(z) у LGB по глубине окна: h=240 даёт -0.0101 и -0.0071,
h=300 даёт -0.0073 и -0.0080, h=365 — -0.0053 и -0.0074.

При h=365 обрезка почти не срабатывает, и сдвиг наименьший, но НЕ
исчезает. Это указывает, что работают оба механизма — снятие обрезки
и нормировка единиц, — причём нормировка большая часть. Вывод
предварительный: std(z) диагностика, а не выигрыш. Разделит его тест
capped-rate против full-rate.

### Семантические дефекты блока, к исправлению в v28

* `life_aov` считается как GMV/buydays, а не GMV/orders — переименовать
  в `life_gmv_per_buyday`, настоящий AOV добавить отдельно;
* `life_act_frac` — доля дней с ПОИСКОМ, а не активность в смысле
  основного пайплайна;
* `rel_ord` нормируется на рыночных покупателей, а не заказы —
  но rel_* блок и так даёт ноль, поэтому удаляется целиком;
* `life_rel_recent` — непрозрачная смесь единиц, удаляется.

Правильная параметризация для v28: систематическая нормировка ВСЕХ
накопительных величин на день наблюдения (GMV/day, orders/day,
buydays/day, active_days/day, searches/day, carts/day) плюс условные
(GMV/buyday, GMV/order, orders/buyday, tenure/days).

### Публичный замер: направление верное, применено вчетверо сильнее нужного

v27_life при a = 1 дал **1.6492598** против v23 1.6479820, то есть
+0.00128 ХУЖЕ. Офлайн обещал +0.0025 лучше при четырёх согласованных
парных разностях — крупнейшее расхождение офлайна с публикой за проект.

Парабола по двум точкам (D считается точно из csv):

| | значение |
|---|---|
| D (кривизна) | 0.008096 |
| C (проекция на остаток) | **+0.001941** |
| a* | **+0.240** |
| потолок при a* | **1.6478408** |

C положительна и в 37 раз больше, чем у направления v26 (+0.000053).
Сигнал настоящий и крупный; ошибка в масштабе, а не в знаке.

| a | 0.00 | 0.10 | **0.24** | 0.50 | 1.00 |
|---|---|---|---|---|---|
| RMSLE | 1.6479820 | 1.6478888 | **1.6478408** | 1.6480071 | 1.6492598 |

**Методический вывод.** В журнале есть правило: для направлений с
единственным валидационным якорем коэффициент занижать. Я счёл, что
два согласованных якоря его отменяют. НЕ ОТМЕНЯЮТ: 348 и 378 делят
почти всю обучающую выборку (198-318 против 198-348), поэтому их
согласие не есть независимое подтверждение МАСШТАБА — оно
подтверждает только знак и наличие эффекта.

Это пятый подряд случай завышения внутривыборочного коэффициента:
v19 вдвое, v21 в восемь раз, годовой фильтр в полтора, v26 вдесятеро,
здесь вчетверо. Ни разу внутривыборочный коэффициент не оказался
заниженным.

Практическое правило на будущее: применять НЕ БОЛЕЕ 0.3 от
измеренного офлайн масштаба для любого нового направления, независимо
от числа согласованных валидационных якорей.

### v28 при a = 0.24: НОВЫЙ ЛУЧШИЙ РЕЗУЛЬТАТ 1.6478363

Предсказание параболы 1.6478408, замер 1.6478363 — расхождение
4.6e-6. Десятое применение приёма.

Три точки на одной прямой позволяют ПРОВЕРИТЬ квадратичную модель,
а не только пользоваться ею:

| источник оценки | C | a* |
|---|---|---|
| по точке a = 0.24 | +0.001972 | +0.2436 |
| по точке a = 1.00 | +0.001941 | +0.2398 |

Согласие 1.6 %. Подогнанная по трём точкам кривизна 0.008177 против
точной из csv 0.008096 — расхождение 1.0 %. Вершина a* = 0.2424 даёт
1.6478362, то есть мы УЖЕ в ней: переход с 0.24 на 0.2424 не даёт
ничего.

| сабмит | RMSLE | к v23 |
|---|---|---|
| v23 | 1.6479820 | — |
| v27 (a = 1) | 1.6492598 | -0.0012778 |
| **v28 (a = 0.24)** | **1.6478363** | **+0.0001457** |
| лидер | 1.6458638 | отставание 0.0019725 |

**Следствие для следующего хода.** Потолок этого направления
C²/D = 0.000465 по MSE выбран полностью. Больше из него
масштабированием не извлечь — нужно ДРУГОЕ, более чистое направление
по той же оси. Это ровно то, что даёт систематическая нормировка всех
накопительных величин с исправленной семантикой: если блок станет
менее шумным и точнее выровненным с остатком, вырастет C, а с ним и
потолок.

### Геометрия d_life против полного v23: провал v27 был предсказуем офлайн

Направление считалось тем же ансамблем и с теми же весами (0.4 LGB +
0.6 CatBoost), база — точная реконструкция v23 на 378.

| | C | V | alpha* | прирост shape |
|---|---|---|---|---|
| d_life сырое | +3.480e-3 | 7.455e-3 | **0.467** | +0.00048 |
| d_life ортогональное | +1.597e-3 | 4.731e-3 | **0.338** | **+0.00016** |

Решающая строка: **при alpha = 1 офлайн ТОЖЕ хуже** — 1.67768 против
1.67753. Провал v27 был предсказуем этим замером.

**Ортогональный прирост +0.00016 против публичного +0.000146.**
Расхождение 10 %. Аппарат предсказал исход; ошибка была в том, что я
применил его после сабмита, а не до.

**Почему переносится четверть:**

| корреляция d_life с | |
|---|---|
| годовым фильтром | **+0.5985** |
| Gap-GRU | +0.0762 |

Блок пожизненных ставок и годовой согласованный фильтр ловят во
многом одно и то же — оба опираются на длинную историю. Три четверти
работы уже делал годовой фильтр.

**Главная методическая ошибка.** Выигрыш мерился против ГОЛОГО LGB
(+0.0025), а надо против ПОЛНОГО v23 (+0.00048 в оптимуме, минус при
alpha = 1). Разница в пять раз, и она вся объясняется тем, что в v23
уже сидят направления, чинящие ту же ошибку.

Правило: для любого кандидата в production обязателен замер
маржинального ресурса ПОВЕРХ полного текущего сабмита, а не поверх
одиночной модели. Прирост над одиночной моделью не является оценкой
production-выигрыша даже по порядку величины.

### Тест механизма: работает лишняя история, а не только единицы

Пять вариантов, два сида, два якоря, LGB-hurdle с боевой ранней
остановкой.

| вариант | признаков | 348 | 378 |
|---|---|---|---|
| +r_capped | 1 | -0.00001 | +0.00004 |
| **+r_full** | 1 | +0.00046 | **+0.00122** |
| +чистый full | 10 | +0.00222 | +0.00212 |
| +чистый оба | 20 | +0.00213 | +0.00230 |

**r_capped даёт ноль по построению.** При A >= 300 обрезанное окно
всегда ровно 300 дней, поэтому log(1 + GMV_300/300) есть МОНОТОННОЕ
преобразование имеющегося gmv_hist. Для дерева это тот же признак.
Тест в этом виде НЕ разделяет «лишние дни» и «меняющийся знаменатель»,
потому что у capped знаменатель не меняется.

**Разделение даёт сравнение якорей.** Один r_full несёт пятую часть
эффекта на 348 и более половины на 378. За границей обрезки на 348
лежит 48 дней, на 378 — 78. Зависимость от объёма невидимой истории
означает, что работает именно она.

Оговорка к прежнему выводу: из corr(full, capped) = 0.9978-0.9993 я
заключил, что охваты неразличимы. Неверно: 0.1-0.4 % остаточной
дисперсии хватило, потому что различие систематическое, а не шумовое.
Высокая корреляция признаков не означает взаимозаменяемости.

### Чистка семантики СДЕЛАЛА ХУЖЕ

Геометрия обоих блоков на одной базе, поверх точного v23:

| | исходный life (8) | чистый rate (10) |
|---|---|---|
| сырое C | 3.480e-3 | 2.603e-3 |
| **C ортогональное** | **1.597e-3** | 1.102e-3 |
| ортогональный прирост | **+0.00016** | +0.00010 |
| corr с годовым фильтром | 0.599 | 0.551 |
| ортогональная доля | 0.797 | 0.830 |

Чистый блок ортогональнее, но потерял столько сигнала, что
маржинальный ресурс упал в полтора раза. Ожидание «меньше шума — выше
потолок» не подтвердилось: признаки, сочтённые дефектными, несли
реальный сигнал — вероятнее всего life_early_share, форма траектории,
которой в чистом блоке нет вовсе.

Практический итог оси: ~+0.00015 маржинально, и это УЖЕ ВЗЯТО в v28
(+0.000146 публично против +0.00016 офлайн). Систематическая
нормировка как отдельный ход закрыта.

## Глубина истории как конфигурация: LGB-выигрыш есть, поверх v28 его нет

Идея: не добавлять признаки, а снять обрезку у самих 183. Боевой
состав 240/300/365, данные допускают 420.

Оговорка к чистоте замера: max_history меняет не только глубину, но и
НАБОР признаков (183 при h<=300, 193 при h>=365 — добавляется блок
365-дневного окна). Поэтому причинное утверждение — «глубокая история
и признаки глубокого окна», а не строго «именно старые дни».

### Одиночные модели и ансамбли

| h | 348 | 378 |
|---|---|---|
| 240 | -0.00415 | -0.00382 |
| 300 | — | — |
| 365 | +0.00212 | +0.00073 |
| 420 | +0.00197 | +0.00031 |

| ансамбль | 348 | 378 |
|---|---|---|
| (240,365,420) | +0.00075 | +0.00042 |
| **(300,365,420)** | **+0.00118** | **+0.00060** |

Знак сохранился, величина на 378 ровно вдвое меньше.

Интерпретация: на 378 h=420 ПОДДИНОЧКЕ хуже h=365 (+0.00031 против
+0.00073), значит выигрыш ансамбля идёт в основном от УДАЛЕНИЯ слабого
h=240 (отстаёт на 0.004 на обоих якорях), а не от углубления.

### Поверх v28 ресурса нет

Измерение поверх ТЕКУЩЕГО ЛУЧШЕГО, а не поверх v23: v28 уже содержит
0.24 life-направления, а life и depth исправляют одно и то же.

| | значение |
|---|---|
| d_depth = 0.4 (z^{300,365,420} - z^{240,300,365}) | std 0.02599 |
| C поверх v28 | +1.538e-4 |
| alpha* | +0.2276 |
| **оракульный прирост** | **+0.00001** |
| при alpha = 1 | **-0.00011** |

corr(depth, life) = **+0.6705**, corr(depth, annual) = +0.5029,
corr(depth, GRU) = +0.0687.

Ветка закрыта. Относительно v23 тот же замер показал бы заметный
плюс — измерение поверх v28 сэкономило сабмит.

### Коэффициент переноса офлайн -> паблик откалиброван

Офлайн v28 лучше v23 на +0.00037 (1.67753 -> 1.67716), публично на
+0.000146. **Отношение 0.39.**

Шестое подряд завышение внутривыборочной оценки и первая его
количественная оценка на согласованной паре замеров. Практическое
следствие: офлайновый маржинальный прирост надо умножать примерно на
0.4, и кандидат ниже 5e-4 офлайн не даёт различимого эффекта на
лидерборде.

## Гиперпараметры LightGBM: ось закрыта полностью

Пять параметров, прямо перечисленных в разделе «не проверено» с
начала проекта: min_child_samples, max_bin, feature_fraction,
bagging_fraction, lambda_l1. На фоне TabM, PatchFormer и массивного
предобучения — единственная совсем нетронутая ось.

Меняется ровно один параметр за раз от боевой конфигурации, два
якоря, два сида, ранняя остановка боевая. 60 обучений.

| вариант | Δ 348 | Δ 378 | знак |
|---|---|---|---|
| feat_frac=0.65 | -0.00002 | +0.00028 | РАЗНЫЙ |
| feat_frac=0.5 | -0.00007 | +0.00026 | РАЗНЫЙ |
| min_child=50 | -0.00012 | +0.00017 | РАЗНЫЙ |
| feat_frac=0.95 | -0.00010 | +0.00012 | РАЗНЫЙ |
| max_bin=511 | -0.00015 | +0.00012 | РАЗНЫЙ |
| l1=10.0 | -0.00009 | +0.00005 | РАЗНЫЙ |
| l1=0.5 | -0.00011 | -0.00003 | оба - |
| l1=2.0 | -0.00019 | +0.00004 | РАЗНЫЙ |
| min_child=100 | -0.00009 | -0.00008 | оба - |
| bag_frac=1.0 | -0.00026 | +0.00008 | РАЗНЫЙ |
| min_child=800 | -0.00014 | -0.00009 | оба - |
| min_child=400 | -0.00010 | -0.00015 | оба - |
| max_bin=127 | -0.00025 | -0.00003 | оба - |
| max_bin=63 | -0.00007 | -0.00022 | оба - |
| bag_frac=0.6 | -0.00045 | -0.00001 | оба - |

**Положительных на обоих якорях НЕТ ВОВСЕ.** Восемь со скачущим
знаком, семь отрицательных на обоих. Максимум по модулю 0.00028 при
пороге осмысленности 5e-4.

Боевая конфигурация — локальный оптимум по всем пяти осям
одновременно. Подозрение «max_bin так никто и не прогнал» снято.

Про разрешение: сидовый шум головы std = 0.00011, то есть SE по двум
сидам около 8e-5, и эффекты в 2e-4 различимы на грани. Но добавление
сидов ничего бы не изменило — положительных на обоих якорях нет
вообще, а не «есть, но незначимые».

Побочно: на 348 bag_frac показал единственную в переборе «настоящую
форму кривой» (0.6 даёт -0.00045 и 1.0 даёт -0.00026 при согласованных
сидах, минимум ровно в боевых 0.8). На 378 это не воспроизвелось
(-0.00001 и +0.00008). Ещё один случай, когда согласованность сидов
внутри якоря выдаёт себя за структуру.

## Временная ранняя остановка: работает она, а не веса

Две смены схемы обучения во времени, обе не проверялись.

**Веса.** Обе головы получают w(a) = 2^(-age/90), причём 90 никогда не
выбиралось систематически. Сезонность в отобранной популяции почти
целиком живёт в интенсивной марже m, тогда как p стабильнее, поэтому
одинаковая скорость забывания не обоснована.

**Ранняя остановка.** Сейчас идёт по СЛУЧАЙНЫМ 12 % строк из
объединённого трейна: сложность выбирается на случайной подвыборке
прошлого, а оценивается на будущем. Временная схема — обучение до
предпоследнего якоря, остановка на последнем, затем refit на всех с
фиксированным k.

### Результат: контроль дал однозначный ответ

| политика | h_p | h_m | Δ 348 | Δ 378 | среднее |
|---|---|---|---|---|---|
| **A** | 90 | 90 | +0.00018 | +0.00025 | **+0.00022** |
| B | 180 | 60 | +0.00028 | +0.00022 | +0.00025 |
| C | inf | 60 | +0.00019 | +0.00027 | +0.00023 |
| D | 180 | 45 | +0.00012 | +0.00013 | +0.00012 |

Все четыре положительны на обоих якорях. Но политика A — это ТЕ ЖЕ
ВЕСА, только временная остановка, и она даёт практически весь эффект.
B опережает A на +0.00003, C на +0.00001 — втрое меньше сидового
разброса внутри ячейки (у B на 348 сиды дали +0.00054 и +0.00001).

Вывод: работает ГЕОМЕТРИЯ ВЫБОРА ЧИСЛА ДЕРЕВЬЕВ, а не скорость
забывания. Согласуется с историей проекта: веса и калибровки никогда
не переносились, число деревьев переносилось.

### Механизм подтверждён восемью замерами

Временная остановка систематически берёт МЕНЬШЕ деревьев у
классификатора:

| | база | временная |
|---|---|---|
| 348, сид 42 | 210 | 164 |
| 348, сид 7 | 197 | 167 |
| 378, сид 42 | **297** | **160** |
| 378, сид 7 | 216 | 169 |

Разрыв тем больше, чем больше якорей в обучении (на 378 их шесть
против пяти на 348). Случайные 12 % содержат «сестёр» тех же
пользователей из соседних якорей, перестают быть отложенной выборкой,
и остановка срабатывает слишком поздно. Тот же механизм разбирался в
журнале при уплотнении якорей, но применительно к штатной схеме его
не проверяли.

Регрессия почти не затронута (159 -> 159, 189 -> 150, 159 -> 186,
170 -> 142): переобучение на случайной подвыборке сильнее бьёт по
классификатору.

### Но на ансамбле эффект исчезает полностью

| | значение |
|---|---|
| LGB-ансамбль, старая ES -> новая | 1.68016 -> 1.68018, **-0.00002** |
| C поверх v28 | +4.98e-5 |
| прирост при alpha* | **+0.00000** |
| при alpha = 1 | -0.00002 |

Standalone +0.00022 на одиночных моделях обращается в НОЛЬ на
ансамбле из шести.

Причина та же, что журнал уже фиксировал для факторизации:
«усреднение съедает 40 %, потому что факторизация чинит отчасти ту же
ошибку, что и сам ансамбль». Здесь съедает все 100 %. Усреднение
гасит переобучение классификатора — то самое, которое чинит
правильный выбор числа деревьев. Два лекарства от одной болезни.

Ортогональность подтвердилась ровно как предсказано: corr с life
+0.031, с годовым -0.018, с GRU -0.054. Направление ни с чем не
пересекается — но ортогональность направления без сигнала ничего не
стоит.

**Правило, которое из этого следует.** Прежде чем мерить кандидата
поверх production, надо проверить, не чинит ли он ту же ошибку, что и
УСРЕДНЕНИЕ. Кандидаты класса «правильнее выбрать сложность одной
модели» подозрительны по построению: ансамбль уже делает это
статистически. Проверка дешёвая — сравнить прирост на одиночной
модели и на ансамбле ДО всякой геометрии.

Диагностика остаётся ценной сама по себе: временная остановка берёт
меньше деревьев у классификатора в восьми парах из восьми, разрыв от
25 % до 46 %, и растёт с числом якорей. Дефект случайной остановки
реален; ансамбль его компенсирует.

## Аудит сабмитов: хеджировать нечем, v28 доминирует

Ни одной новой модели. Шесть кандидатов, расхождение на 408 и
бутстрап public/private на 378, где есть разметка.

Реконструкция на 378: v23 из сохранённого бэкбона плюс боевые
направления; v28 = v23 + 0.24 (d_life - mean); v25 = v23 + 0.4
(zl_ord - zl_old); v22 = v23 с годовым коэффициентом, делённым на
1.159. Проверка последней: предсказанный std расхождения 0.00612
против фактического на 408 0.00707, отношение 0.87 — допущение
«между v22 и v23 менялся только годовой коэффициент» верно
приблизительно.

### Разнообразия нет

Корреляция ОШИБОК на 378 у всех четырёх >= 0.99987. Ошибаются на
одних и тех же пользователях в одну сторону.

| пара на 408 | std(d) | corr(z) |
|---|---|---|
| v22 ~ v23 | 0.00707 | 0.999991 |
| v23 ~ v28 | 0.02159 | 0.999911 |
| v18 ~ v28 | 0.08456 | 0.998638 |

Структурно далеки только v18/v20, но они хуже публично на 1.2e-3 и
9.5e-4.

### Бутстрап 20/80, 1000 итераций

| кандидат | P(лучше v28 на private) | E[разница] |
|---|---|---|
| v22 | **0.000** | -0.000504 |
| v23 | **0.000** | -0.000367 |
| v25 | **0.000** | -0.000306 |

Ни в одной итерации никто не обошёл v28. Лучшая пара — любая с v28,
и её результат В ТОЧНОСТИ равен v28 в одиночку (1.688769). Второй
кандидат не добавляет ничего.

### Публичный лидерборд надёжнее, чем предполагалось

P(лучший по public = лучший по private) = **0.995**, цена выбора по
публичной части +2e-6.

Бутстрапный шум разницы std ~ 3e-5 на 80 % выборки, то есть ~6e-5 на
20 %. Расхождение v23 и v28 равно 1.5e-4 — вдвое-втрое выше шума и
потому измеримо. Расхождение v22 и v23 равно 1.5e-5 — неразрешимо.

Прежняя тревога про «перестановки в шестом знаке» верна только для
пар вроде v22/v23. Прирост v28 был настоящим.

**Оговорка.** Бутстрап моделирует ТОЛЬКО шум выборки, а не
структурный сдвиг между публичной и приватной частью. Если приватная
часть отличается систематически, анализ этого не покажет.

## Смесь со СТАРЫМ сабмитом: разнообразие всё-таки нашлось

Аудит показал, что близкие кандидаты (v22/v23/v25) не диверсифицируют:
корреляция ошибок >= 0.99987. Но v18 и v20 структурно далеки
(corr(z) с v28 равна 0.9986 и 0.9992), хотя публично хуже на 1.2e-3 и
9.5e-4.

Смесь z = z_v28 + a (z_j - z_v28) считается тем же приёмом: D точно из
csv, C восстанавливается из двух публичных замеров.

| кандидат | D | C | a* | потолок | к v28 |
|---|---|---|---|---|---|
| **v18** | 0.007150 | **+0.001546** | 0.2162 | **1.6477349** | **+0.000101** |
| v20 | 0.004390 | +0.000624 | 0.1421 | 1.6478094 | +0.000027 |
| v22 | 0.000689 | +0.000080 | 0.1157 | 1.6478335 | +0.000003 |
| v23 | 0.000466 | -0.000007 | -0.0149 | 1.6478363 | 0 |
| v25 | 0.000695 | +0.000011 | 0.0160 | 1.6478362 | 0 |

Структурная непохожесть v18 конвертируется в ресурс, несмотря на то
что сам он заметно хуже.

### Двумерные смеси брать НЕЛЬЗЯ

Они обещают втрое больше (v18+v22 даёт потолок 1.6475099, +0.000326),
но это артефакт обусловленности. Шум публичного замера даёт
δC ~ 1e-4, отсюда отношение сигнал/шум:

| направление | C | C/δC |
|---|---|---|
| v18 | +0.001546 | **15** |
| v20 | +0.000624 | 6 |
| v22 | +0.000080 | **0.8** |
| v23 | -0.000007 | 0.07 |

У v22/v23/v25 коэффициент МЕНЬШЕ собственного шума. В двумерных
решениях именно они получают веса -2.169 и -3.084: почти вырожденная
матрица Грама (v18~v20 коррелируют на 0.9998) вытягивает огромные
противонаправленные коэффициенты из чистого шума.

### v29

a = 0.216, предсказание 1.6477349. Полоса чувствительности при
δC = 1e-4: [1.6477218, 1.6477480] — ОБА конца лучше v28 на
0.9-1.1e-4. Улучшение устойчиво к шуму оценки.

### v29 = 1.6477316544, новый лучший; линия v18 исчерпана

Предсказание 1.6477349, замер 1.6477317, расхождение 3.2e-6.
Одиннадцатое применение приёма.

Три точки на линии v28 -> v18 позволяют ПРОВЕРИТЬ модель:

| источник | C | a* |
|---|---|---|
| по точке a = 0.216 | +0.001570 | +0.2197 |
| по точке a = 1.000 | +0.001546 | +0.2162 |

Согласие 1.6 %, подогнанная кривизна 0.007212 против точной 0.007150
(+0.9 %). Вершина a* = 0.2187 даёт 1.6477316 — мы В НЕЙ, из этой линии
больше ничего не извлечь.

### Экстраполяция прочь от старых версий

Смеси от новой базы v29:

| направление | C | C/δC | a* | потолок | к v29 |
|---|---|---|---|---|---|
| **v22** | -0.000347 | **3.5** | -1.92 | 1.6475293 | +0.000202 |
| v23 | -0.000318 | 3.2 | -1.69 | 1.6475685 | +0.000163 |
| v25 | -0.000296 | 3.0 | -0.70 | 1.6476689 | +0.000063 |
| v27 | +0.000954 | 9.5 | +0.14 | 1.6476919 | +0.000040 |
| v20 | -0.000529 | 5.3 | -0.22 | 1.6476966 | +0.000035 |

Отрицательные C означают, что помогает движение ПРОЧЬ от старых
версий, то есть экстраполяция накопленных улучшений дальше.

### Выбор усадки по худшему случаю

При C/δC = 3.5 полный a* держится лишь до одной сигмы:

| k | a | ожидаемо | -1σ | -2σ |
|---|---|---|---|---|
| 0.4 | -0.77 | 1.6476021 | 1.6476488 | **1.6476955** |
| **0.5** | -0.96 | 1.6475799 | 1.6476382 | 1.6476965 |
| 0.6 | -1.15 | 1.6475617 | 1.6476317 | 1.6477016 |
| 1.0 | -1.92 | 1.6475293 | 1.6476459 | **1.6477626** |

При k = 1 на двух сигмах результат ХУЖЕ нынешнего. Наилучший худший
случай при k ~ 0.4-0.5.

v30 собран при k = 0.5: ожидаемо 1.6475799, при -2σ 1.6476965 —
хуже v29 стать не может. Осторожность стоит десятой доли ожидаемого
выигрыша, что оправдано экстраполяцией за пределы измеренного
диапазона и пятью завышенными коэффициентами в истории проекта.

### v30 = 1.6475805017; неопределённость C измерена и оказалась мала

Предсказание 1.6475799, замер 1.6475805, расхождение 5.5e-7.
Двенадцатое применение приёма.

Три точки на линии v29 -> v22:

| источник | C | a* |
|---|---|---|
| по точке a = -0.961 | -0.000346 | -1.9159 |
| по точке a = +1.000 | -0.000347 | -1.9219 |

**Согласие 0.3 %**, кривизна сходится до 0.6 %. Оценка δC = 1e-4 была
СИЛЬНО завышена: два независимых публичных замера дают C с точностью
до 1e-6.

Осторожность с k = 0.5 стоила +0.0000494 — ровно то, что осталось
недобранным на этой линии. Ex ante решение было верным (тогда
неопределённость не была измерена), но теперь держаться за неё
незачем.

### Направления от v30 почти вырождены

| кандидат | C | a* | потолок | к v30 |
|---|---|---|---|---|
| **v28** | -0.000255 | -0.769 | **1.6475209** | **+0.0000596** |
| v22 | -0.000339 | -0.489 | 1.6475302 | +0.0000503 |
| v23 | -0.000331 | -0.500 | 1.6475303 | +0.0000502 |
| v29 | -0.000166 | -0.994 | 1.6475305 | +0.0000500 |

Все дают примерно +0.00005, потому что это по сути ОДНО направление —
продолжение той же экстраполяции.

v31 собран в полной вершине (a = -0.7692). Вершина плоская: при
δC = 3e-5 разница между a = -0.54 и -0.77 составляет 1e-6.

**Про исчерпание оси.** Отношение сигнал/шум падает с каждым шагом:
у v18 было 15, у v22 3.5, здесь 2.6. Прирост упал с +0.000101 до
+0.00006 — сопоставимо с шумом публичной части (~6e-5). Это конечный
ресурс, и он почти выбран.

## Канальные окна — пусто; набор признаков насыщен

Систематический пробел: ось окон 7/14/30/60/90/180 применена к GMV,
заказам, корзинам и поискам, но НЕ к каналам и воронке.
`_channel_recency_exprs` берёт одно окно cw, поэтому gmv_search_c,
gmv_cat_c, s2o_c, c2o_c, s2c_c, c2c_c, phantom_c, weekend_days_c
существуют только как накопленные.

Блок: 12 величин (включая долю поиска в GMV и конверсию s2o внутри
окна) x 4 окна = 48 признаков.

| якорь | база | +канал | Δ | парно |
|---|---|---|---|---|
| 348 | 1.75221 | 1.75251 | **-0.00030** | -0.00021, -0.00040 |
| 378 | 1.68192 | 1.68188 | +0.00005 | +0.00031, -0.00021 |

Знак между якорями разный, на 378 сиды разнонаправлены. Закрыто.

**Проверка данных.** Сырой train.parquet содержит ровно 18 колонок
уровня «пользователь x день» (event_date, user_id, search, cat,
has_*, *_to_cart, *_to_ord, gmv_search, gmv_cat, to_cart, to_ord,
gmv, searches). Товарного уровня нет, скрытого источника нет —
панель использует всё.

**Общий вывод по признакам.** Девять подряд отрицательных проверок:
персональный offset, линейный skip, горизонт, канал (декомпозиция
таргета), рыночная нормировка, чистая нормировка ставок, гиперпараметры,
временная ES на ансамбле, канальные окна. Набор из 183 признаков
насыщен: добавление семантически новых величин из тех же наблюдаемых
не даёт ничего.

Единственное, что за три дня сработало, — блок беcкэповой истории
(+0.000146 публично), и он же оказался на 60 % скоррелирован с годовым
фильтром.

## Sequence-ветка: токены были обрезаны дважды

Аудит перед архитектурным перебором вскрыл два ограничения, о которых
в журнале не было записи.

**Первое: токены физически обрезаны при сборке.** `x_a378.npy` имеет
форму (233152, **192**, 20), максимум длин ровно 192. Прежняя оценка
«max_len=192 покрывает 89.5 % пользователей» относилась к усечённым
данным. На сырой истории до 378 медиана 92 события, p90 = 234,
p99 = 329, максимум 379 — целиком помещаются лишь **82.25 %**.

**Второе, важнее: `build_tokens --max-history 300`.** Последовательность
обрезана тем же трёхсотдневным потолком, который уже оказался дефектом
для деревьев. Сеть тоже не видела дальнюю историю.

Пересборка с `--max-len 384 --max-history 420` дала усечение **0.0 %**
на 378 и 0.2 % на 408, размер 24 ГБ, время — секунды.

### Точный контроль воспроизводится лишь частично

Прежний запуск без `--init-from` был бы не контролем, а другой моделью:
победивший рецепт включал этап A (`pretrain_evt`) и заморозку энкодера
на две эпохи. Перезапущено правильно.

| | A_exact | исторический (gapgru_evt.json) |
|---|---|---|
| фолд 0 (348) | +0.00018 (alpha 0.226) | +0.000263 (alpha 0.350) |
| фолд 1 (378) | +0.00049 (alpha 0.460) | +0.000881 (alpha 0.360) |

Величина воспроизводится на 60-70 %, но СОГЛАСОВАННОСТЬ alpha
утрачена: 0.226 против 0.460 (вдвое) там, где исторически было 3 %.
Причина — недетерминизм GPU-обучения. Контроль пригоден для сравнения
с C, но сравнивать по alpha нельзя, только по маржинальному ресурсу.

**Поправка к прежнему выводу.** Усреднение чекпоинтов на фолде 1 дало
+0.00058 против +0.00049 у одиночного, то есть ЛУЧШЕ, а на фолде 0
хуже (+0.00007 против +0.00018). Значит приём лежит на уровне шума, и
прежний вывод журнала о его вреде был получен на одной реализации.

**Поправка к диагностике по эпохам.** Прежнее «переносимый сигнал
максимален на 4-й эпохе и монотонно умирает» не подтвердилось: кривая
колеблется (фолд 0: +0.00028, +0.00015, +0.00009, +0.00016, +0.00027,
+0.00040 — лучшая последняя; фолд 1: -0.00001, +0.00011, +0.00021,
+0.00036, +0.00023, +0.00027 — лучшая восьмая). Скрипт берёт лучшую
эпоху, поэтому на результат это не влияет.

## Gap-GRU с длинным контекстом: рекорд standalone, ноль поверх стека

Токены пересобраны без обеих обрезок (384 события, 420 дней истории,
усечение 0.0 %). Тот же претрейн, та же архитектура — меняется ТОЛЬКО
объём видимой истории.

### Standalone — лучший результат sequence-ветки за проект

| | фолд 0 (348) | фолд 1 (378) |
|---|---|---|
| A_exact (контроль) | +0.00018 | +0.00049 |
| исторический | +0.00026 | +0.00088 |
| **C (длинный контекст)** | **+0.00053** | **+0.00111** |

C превосходит и контроль, и исторический рекорд на обоих фолдах.
Перенос 109 % и 131 % — на тестовом якоре больше, чем на якоре
подбора. В семи промежуточных сравнениях из восьми C выше контроля.

### Поверх v28 — ноль

| | значение |
|---|---|
| C сырое, alpha* | +0.164 · прирост **+0.00008** |
| C ортогональное | +0.101 · прирост **+0.00002** |
| corr(C, старый GRU) | **+0.5814** |
| вес проекции на старый GRU | +0.468 |

Замена старого направления на новое ДЕЛАЕТ ХУЖЕ:

| конфигурация | shape 378 |
|---|---|
| база без нейронаправления | 1.67797 |
| + старый GRU при 0.35 | 1.67716 |
| + старый GRU в оптимуме 0.43 | **1.67713** |
| + новый C в оптимуме 0.41 | **1.67745** |
| + оба вместе | 1.67708 (+0.00008 к v28) |

### Главный вывод: standalone НЕВЕРНО РАНЖИРУЕТ кандидатов

Вчера установлено, что величина standalone-выигрыша не переносится на
сильную базу (life: +0.0025 -> +0.00016). Сегодня сильнее: не
переносится и ПОРЯДОК. Модель, вдвое лучшая при исправлении слабой
OOF-пары, оказывается ХУЖЕ старой при работе поверх современного
стека.

Механизм: старый GRU не просто слабее — вокруг него строился весь
стек. Годовой фильтр и life подбирались при уже включённом
нейронаправлении и заняли ту часть остатка, которую новая сеть
пытается объяснить заново.

Практическое правило: кандидат, обучаемый на остатке УСТАРЕВШЕЙ базы,
не может быть оценён никакой standalone-метрикой. Требуется прямое
измерение поверх текущего стека, и оно должно предшествовать
интерпретации, а не следовать за ней.
